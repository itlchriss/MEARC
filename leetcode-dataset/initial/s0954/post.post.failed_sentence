Given an integer array of even length `arr`, return `true` _if it is possible to reorder_ `arr` _such that_ `arr[2 initial prepare.sh run.sh i + 1] = 2 initial prepare.sh run.sh arr[2 initial prepare.sh run.sh i]` _for every_ `0 <= i < len(arr) / 2`_, or_ `false` _otherwise_.

