To determine if it is possible to rearrange the even-length integer array `arr` in a way that satisfies the condition `arr[2*i + 1]` equals `arr[2*i]` for every `0 <= i < len(arr) / 2`, the method `canReorderDoubled` should return `true`. Otherwise, if the condition is not met for any case, the method should return `false`.
