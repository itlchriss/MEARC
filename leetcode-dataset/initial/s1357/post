cashier.getBill([1,2],[1,2]); // return 500.0. 1<sup>st</sup> customer, no discount. cashier.getBill([3,7],[10,10]); // return 4000.0. 2<sup>nd</sup> customer, no discount. cashier.getBill([1,2,3,4,5,6,7],[1,1,1,1,1,1,1]); // return 800.0. 3<sup>rd</sup> customer, 50% discount. cashier.getBill([4],[10]); // return 4000.0. 4<sup>th</sup> customer, no discount. cashier.getBill([7,3],[10,10]); // return 4000.0. 5<sup>th</sup> customer, no discount. cashier.getBill([7,5,3,1,6,4,2],[10,10,10,9,9,9,7]); // return 7350.0. 6<sup>th</sup> customer, 50% discount. cashier.getBill([2,3,5],[5,3,2]); // return 2500.0. 6<sup>th</sup> customer, no discount.
initial prepare.sh run.sh `double getBill(int[] product, int[] amount)` Returns the final total of the bill with the discount applied (if any). Answers within <code>10<sup>-5</sup></code> of the actual value will be accepted.
