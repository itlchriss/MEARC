To determine if a move is legal in the given Java method, you need to change the color of the cell at position `(rMove, cMove)` to `color`. If this change is made successfully, the method should return `true`. In any other case, the method should not return `false`.
