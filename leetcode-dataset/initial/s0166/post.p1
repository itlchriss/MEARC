When the `numerator` and `denominator` of a fraction are provided as two integers, the algorithm should return the fraction in string format. If there are multiple valid fractions, any of them can be returned.
