The CustomStack object performs the pop operation, which removes and returns the top element from the stack. In this case, the top element is 2, and the stack is updated to contain only [1]. 
After performing the pop operation on the CustomStack object, the resulting value is the top element of the stack, which is 103. Additionally, the stack is modified to contain [201, 202]. 
The CustomStack object executes the pop operation again, returning the top element of the stack, which is 202. The stack is then updated to contain only [201]. 
After performing the pop operation on the CustomStack object, the resulting value is the top element of the stack, which is 201. Furthermore, the stack is modified to contain [101]. 
Since the stack is empty, the CustomStack object returns -1.
