An integer array `nums` is provided as a parameter to the method `productExceptSelf`, which returns an array `answer`. The elements of `answer` are calculated by multiplying all the elements of `nums` except the element at index `i`.
