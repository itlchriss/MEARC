The `query` function in the `majorityChecker` class is designed to find the element in the subarray `arr[left .  .  . right]` that occurs at least `threshold` times. If no such element exists, the function returns -1. The `query` function is initially prepared in the `prepare . sh` and `run . sh` files.

For example, when using the `query` function with the parameters `0`, `5`, and `4`, it returns `1`. Similarly, when using the parameters `0`, `3`, and `3`, the function returns `-1`. Lastly, when using the parameters `2`, `3`, and `2`, the function returns `2`.

This algorithmic approach ensures that the `query` function in the `majorityChecker` class effectively resolves the given problem, providing the expected results based on the specified parameters.
