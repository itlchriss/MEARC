Return _an array of the index of the left-most letter being stamped at each turn_. If we cannot obtain `target` from `s` within `10 initial prepare.sh run.sh target.length` turns, return an empty array.
Return _an array of the index of the left-most letter being stamped at each turn_. If we cannot obtain `target` from `s` within `10 initial prepare.sh run.sh target.length` turns, return an empty array.
