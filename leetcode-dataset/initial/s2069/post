robot.getPos(); // return [4, 0] robot.getDir(); // return "East" robot.getPos(); // return [1, 2] robot.getDir(); // return "West"
initial prepare.sh run.sh `int[] getPos()` Returns the current cell the robot is at, as an array of length 2, `[x, y]`. initial prepare.sh run.sh `String getDir()` Returns the current direction of the robot, `"North"`, `"East"`, `"South"`, or `"West"`.
