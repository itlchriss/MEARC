initial prepare.sh run.sh If `version1 < version2`, return `-1`. initial prepare.sh run.sh If `version1 > version2`, return `1`. initial prepare.sh run.sh Otherwise, return `0`.
_Return the following:_
