stockPrice.current(); // return 5, the latest timestamp is 2 with the price being 5. stockPrice.maximum(); // return 10, the maximum price is 10 at timestamp 1. stockPrice.maximum(); // return 5, the maximum price is 5 after the correction. stockPrice.minimum(); // return 2, the minimum price is 2 at timestamp 4.
initial prepare.sh run.sh `int current()` Returns the **latest price** of the stock. initial prepare.sh run.sh `int maximum()` Returns the **maximum price** of the stock. initial prepare.sh run.sh `int minimum()` Returns the **minimum price** of the stock.
