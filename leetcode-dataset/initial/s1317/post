Given an integer `n`, return _a list of two integers_ `[A, B]` _where_: The test cases are generated so that there is at least one valid solution. If there are many valid solutions you can return any of them.

