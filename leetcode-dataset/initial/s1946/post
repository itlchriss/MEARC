"832" is the largest number that can be created, so return it. "934" is the largest number that can be created, so return it. **Explanation:** "5" is already the largest number that can be created, so return it.
Return _a string representing the **largest** possible integer after **mutating** (or choosing not to) a **single substring** of_ `num`.
