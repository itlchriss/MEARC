The sum of the two integers appended is 2 + 3 = 5, so we return 5. The sum of the six integers appended is 1 + 2 + 3 + 4 + 7 + 8 = 25, so we return 25.
Return _the sum of the_ `k` _integers appended to_ `nums`.
