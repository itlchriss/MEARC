initial prepare.sh run.sh `int pop()` Removes the element on the top of the stack and returns it. myStack.top(); // return 2 myStack.pop(); // return 2 myStack.empty(); // return False
initial prepare.sh run.sh `int top()` Returns the element on the top of the stack. initial prepare.sh run.sh `boolean empty()` Returns `true` if the stack is empty, `false` otherwise.
