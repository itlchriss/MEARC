initial prepare.sh run.sh `int popFront()` Removes the **front** element of the queue and returns it. If the queue is empty, return `-1`. initial prepare.sh run.sh `int popMiddle()` Removes the **middle** element of the queue and returns it. If the queue is empty, return `-1`. initial prepare.sh run.sh `int popBack()` Removes the **back** element of the queue and returns it. If the queue is empty, return `-1`. initial prepare.sh run.sh Popping the middle from `[1, 2, 3, 4, 5, 6]` returns `3` and results in `[1, 2, 4, 5, 6]`. q.popFront(); // return 1 -> [4, 3, 2] q.popMiddle(); // return 3 -> [4, 2] q.popMiddle(); // return 4 -> [2] q.popBack(); // return 2 -> [] q.popFront(); // return -1 -> [] (The queue is empty)

