To find the length of the longest substring between two equal characters in a given string `s`, excluding the two characters themselves, the algorithm returns an integer value. If there is no such substring, the algorithm returns `-1`.
