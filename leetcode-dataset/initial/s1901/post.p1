To find a peak element in a `m x n` matrix `mat` (where no two adjacent cells are equal), the algorithm returns a length 2 array `[i, j]`. The matrix is 0-indexed, meaning the first row and column have an index of 0.
