initial prepare.sh run.sh `int insert(int v)` Inserts a `TreeNode` into the tree with value `Node.val == val` so that the tree remains complete, and returns the value of the parent of the inserted `TreeNode`. cBTInserter.insert(3); // return 1 cBTInserter.insert(4); // return 2 cBTInserter.get\_root(); // return [1, 2, 3, 4]
initial prepare.sh run.sh `TreeNode get_root()` Returns the root node of the tree.
