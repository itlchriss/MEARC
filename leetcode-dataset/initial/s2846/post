initial prepare.sh run.sh Queries are **independent** of each other, meaning that the tree returns to its initial state** on each new query.
Return _an array_ `answer` _of length_ `m` _where_ `answer[i]` _is the answer to the_ <code>i<sup>th</sup></code> _query._
