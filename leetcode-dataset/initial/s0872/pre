
For example, in the given tree above, the leaf value sequence is `(6, 7, 4, 9, 8)`. Return `true` if and only if the two given trees with head nodes `root1` and `root2` are leaf-similar. initial prepare.sh run.sh Both of the given trees will have values in the range `[0, 200]`.
