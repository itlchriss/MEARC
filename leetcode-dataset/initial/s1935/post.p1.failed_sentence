 `text`, which represents a string of words separated by a single space, and `brokenLetters`, which represents a string of distinct letter keys that are broken. The function calculates and returns the number of words in `text` that can be fully typed using the keyboard, without mentioning the specific method used.
