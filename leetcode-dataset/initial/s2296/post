textEditor.deleteText(4); // return 4 textEditor.cursorRight(3); // return "etpractice" textEditor.cursorLeft(8); // return "leet" textEditor.deleteText(10); // return 4 textEditor.cursorLeft(2); // return "" textEditor.cursorRight(6); // return "practi"
initial prepare.sh run.sh `int deleteText(int k)` Deletes `k` characters to the left of the cursor. Returns the number of characters actually deleted. initial prepare.sh run.sh `string cursorLeft(int k)` Moves the cursor to the left `k` times. Returns the last `min(10, len)` characters to the left of the cursor, where `len` is the number of characters to the left of the cursor. initial prepare.sh run.sh `string cursorRight(int k)` Moves the cursor to the right `k` times. Returns the last `min(10, len)` characters to the left of the cursor, where `len` is the number of characters to the left of the cursor.
