initial prepare.sh run.sh `bool hasFrequency(int frequency)`: Returns `true` if there is a number in the data structure that occurs `frequency` number of times, otherwise, it returns `false`.
initial prepare.sh run.sh `bool hasFrequency(int frequency)`: Returns `true` if there is a number in the data structure that occurs `frequency` number of times, otherwise, it returns `false`. frequencyTracker.hasFrequency(2); // Returns true, because 3 occurs twice frequencyTracker.hasFrequency(1); // Returns false, because the data structure is empty frequencyTracker.hasFrequency(2); // Returns false, because the data structure is empty frequencyTracker.hasFrequency(1); // Returns true, because 3 occurs once
