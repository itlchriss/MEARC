/*@
    public model pure boolean _at_least(int[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length >= x;
        }
        return false;
    }
    public model pure boolean _at_least(long[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length >= x;
        }
        return false;
    }
    public model pure boolean _at_least(short[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length >= x;
        }
        return false;
    }
    public model pure boolean _at_least(double[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length >= x;
        }
        return false;
    }
    public model pure boolean _at_least(float[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length >= x;
        }
        return false;
    }
@*/