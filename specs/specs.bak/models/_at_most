/*@
    public model pure boolean _at_most(int[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length <= x;
        }
        return false;
    }
    public model pure boolean _at_most(long[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length <= x;
        }
        return false;
    }
    public model pure boolean _at_most(short[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length <= x;
        }
        return false;
    }
    public model pure boolean _at_most(double[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length <= x;
        }
        return false;
    }
    public model pure boolean _at_most(float[] array, int x, String _compareType) {
        if (_compareType == "elements") {
            return array.length <= x;
        }
        return false;
    }
@*/