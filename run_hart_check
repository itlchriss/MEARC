PYCMD="python"
OPENJML="../openjml-0.17-a15/openjml "
ESC_FLAG="-esc"
BIGINT_FLAGS="--spec-math bigint --code-math bigint"


rm -rf ./fmcheck/input/hart/*
for entry in `find ./test -type f -name "Solution.java" | sort | head -100`
do
    jmls=()
    # src="${entry//Solution.java/}"
    src=$(dirname "$entry")
    path="${entry//Solution.java/gpt-results}"
    # mkdir -p $path/build
    filename=`echo $(dirname "$entry") | sed 's/.*\///' `
    mkdir -p ./fmcheck/input/hart/$filename
    $PYCMD scripts/insertjml.py $src hart > ./fmcheck/input/hart/$filename/Solution.java
    # $OPENJML ./fmcheck/hart/$filename/Solution.java $BIGINT_FLAGS --timeout 180 > ./fmcheck/hart/$filename/check
    $OPENJML ./fmcheck/input/hart/$filename/Solution.java $BIGINT_FLAGS --timeout 180 > ./fmcheck/input/hart/$filename/p2check
done

