- type: postcondition
  specification: if the length_of parameter x is the length_of parameter y and the
    elements_of parameter x are deeply_equal_to the elements_of parameter y, the result
    is a true_value.
