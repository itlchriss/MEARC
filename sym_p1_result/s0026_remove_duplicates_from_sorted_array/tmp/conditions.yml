ensures:
- Since it is impossible to change the length of the array in languages , you must instead have the result be placed in the first part of the array param_nums.
- More formally , if exist expr_a elements after removing the duplicates , then the first expr_b elements of param_nums should hold the final result.
- Return expr_a after placing the final result in the first expr_b slots of param_nums.
- 'Explanation: Your function should the result is k = 2 , with the first 2 elements of nums being 1 and 2 respectively.'
- It does not matter what you leave beyond the the result ised k (hence they are underscores).
- 'Explanation: Your function should the result is k = 5 , with the first 5 elements of nums being 0 , 1 , 2 , 3 , and 4 respectively.'
- It does not matter what you leave beyond the the result ised k (hence they are underscores).
requires:
- Given the _type_integer array param_nums sorted in ascending_order , remove the duplicates [in-place](https://en.wikipedia.org/wiki/In-placealgorithm) such that each unique element appears only once.
- The relative order of the elements should be kept the same.
- It does not matter what you leave beyond the first expr_a elements.
- Do not allocate extra space for another array.
- You must do this by modifying the input array [in-place](https://en.wikipedia.org/wiki/In-placealgorithm) with O(1) extra memory.
- Custom Judge:.
- The judge will test your solution with the following code:.
- int[] nums = [...]; // Input array.
- int[] expectedNums = [...]; // The expected answer with correct length.
- int k = removeDuplicates(nums); // Calls your implementation.
- assert k == expectedNums.length;.
- for (int i = 0; i < k; i++) {.
- assert nums[i] == expectedNums[i];.
- '}.'
- If all assertions pass , then your solution will be accepted.
- Example 1:.
- 'Input: nums = arr_a.'
- 'Output: 2 , nums = [1 , 2 , \].'
- Example 2:.
- 'Input: nums = arr_a.'
- 'Output: 5 , nums = [0 , 1 , 2 , 3 , 4 , \ , \ , \ , \ , \].'
- Constraints:.
- <code>0 <= nums.length <= 3 10<sup>4</sup></code>.
- ' expr_a.'
- param_nums is sorted in ascending_order .
