ensures:
- The type_integer_ type_array_  param_num_ is the digits of an type_integer_ in left
  to right order.
- The type_integer_ type_array_  param_num_ does not contain any leading zeros except
  for the zero itself.
- The type_integer_  param_k_ is greater_than_or_equal to 1 and is less_than_or_equal
  to 10000.
- The length of the type_integer_ type_array_ result is equal to the length of the
  type_integer_ type_array_  param_num_ or one greater if there is a carry from the
  addition.
- Each element in the type_integer_ type_array_ result is a digit between 0 and 9.
- The type_integer_ type_array_ result is the type_array_ form of the type_integer_
  expr0.
requires: []
