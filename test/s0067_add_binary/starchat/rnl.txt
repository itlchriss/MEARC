
- The length of the string parameter `a` is less than or equal to 10000 and is greater than or equal to 1.
- The length of the string parameter `b` is less than or equal to 10000 and is greater than or equal to 1.
- The string parameter `a` and `b` consist only of '0' or '1' characters.
- The string parameter `a` and `b` do not contain leading zeros except for the zero itself.
- The string result is a binary string and does not contain leading zeros except for the zero itself.
- If the string parameters `a` and `b` are equal to "11" and "1", the string result is equal to "100".
- If the string parameters `a` and `b` are equal to "1010" and "1011", the string result is equal to "10101".

Example