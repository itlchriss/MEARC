- The string parameter `s` is a valid expression consisting of integers and operators `('+', '-', '*', '/')` separated by spaces.
- The integer division should truncate toward zero.
- The intermediate results will be in the range of `[-2^31, 2^31 - 1]`.
- The integer result is guaranteed to fit in a 32-bit integer.
- If the string parameter `s` is equal to "3+2*2", the integer result is equal to 7.
- If the string parameter `s` is equal to "3/2", the integer result is equal to 1.
- If the string parameter `s` is equal to "3+5 / 2", the integer result is equal to 5.
- The length of the string parameter `s` is less than or equal to 3 times 10^5 and is greater than or equal to 1.
- All integers in the expression are non-negative integers in the range [0, 2^31 - 1].