- The string parameter `s` is valid and represents a mathematical expression.
- The integer result is within the range of [-2^31, 2^31 - 1].
- The integer division truncates towards zero.
- The method does not use any built-in function that evaluates strings as mathematical expressions.
- If the string parameter `s` is equal to "3+2*2", the integer result is equal to 7.
- If the string parameter `s` is equal to "3/2", the integer result is equal to 1.
- If the string parameter `s` is equal to "3+5 / 2", the integer result is equal to 5.
- The string parameter `s` consists of integers and operators ('+', '-', '*', '/') separated by spaces.
- All integers in the expression are non-negative and within the range [0, 2^31 - 1].
- The answer fits in a 32-bit integer.