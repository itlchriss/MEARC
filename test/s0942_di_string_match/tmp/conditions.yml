ensures:
- The type_string_  param_s_ is not null.
- The length of the type_integer_ type_array_ result is equal to the length of the
  type_string_  param_s_.
- The type_integer_ type_array_ result contains a permutation of expr0 integers of
  all the integers in the range expr1.
- The type_integer_ type_array_ result can be represented as a type_string_ expr0
  of length expr1 where:.
- '    - If `s[i] == str0expr0perm[i] < perm[i + 1]`.'
- '    - If `s[i] == str0expr0perm[i] > perm[i + 1]`.'
- If there are multiple valid permutations of expr0 ,  any of them can be returned.
requires: []
