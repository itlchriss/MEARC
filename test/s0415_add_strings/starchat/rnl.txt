
- The length of the string parameter `num1` is less than or equal to 10000 and is greater than or equal to 1.
- The length of the string parameter `num2` is less than or equal to 10000 and is greater than or equal to 1.
- The string parameter `num1` consists of only digits.
- The string parameter `num2` consists of only digits.
- The string parameter `num1` and `num2` don't have any leading zeros except for the zero itself.
- The string result is a non-negative integer represented as a string.
- The string result is the sum of the non-negative integers represented by the string parameters `num1` and `num2`.
- If the string parameters `num1` and `num2` are equal to "11" and "123", the string result is equal to "134".
- If the string parameters `num1` and `num2` are equal to "456" and "77", the string result is equal to "533".
- If the string parameters `num1` and `num2` are equal to "0" and "0", the string result is equal to "0".

Example