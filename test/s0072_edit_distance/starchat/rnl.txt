
- The length of the string parameter `w1` is less than or equal to 500 and is greater than or equal to 0.
- The length of the string parameter `w2` is less than or equal to 500 and is greater than or equal to 0.
- The string parameter `w1` consists of only lowercase English letters.
- The string parameter `w2` consists of only lowercase English letters.
- The integer result is greater than or equal to 0 and is less than or equal to the sum of the lengths of the string parameters `w1` and `w2`.
- If the string parameters `w1` and `w2` are equal to "horse" and "ros", the integer result is equal to 3.
- If the string parameters `w1` and `w2` are equal to "intention" and "execution", the integer result is equal to 5.

Example