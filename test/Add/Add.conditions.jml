\exists int i; 0 <= i < collection.size(); collection[i] == e
((collection.size() != \old(collection).size()) || (\exists int i; 0 <= i < collection.size(); collection[i] != \old(collection)[i])) ==> (\result==true)
