ensures:
- The type_integer_ type_array_  param_arr_ must have a length greater_than_or_equal
  to 3 and less_than_or_equal to 50000.
- All values in the type_integer_ type_array_  param_arr_ is greater_than_or_equal
  to negative 10000 and less_than_or_equal to 10000.
- The type_boolean_ result is equal to the true literal if the type_array_ expr0 can
  be partitioned into three non-empty parts with equal sums according to the given
  condition.
- The type_boolean_ result is equal to the false literal if the type_array_ expr0
  cannot be partitioned into three non-empty parts with equal sums according to the
  given condition.
requires: []
