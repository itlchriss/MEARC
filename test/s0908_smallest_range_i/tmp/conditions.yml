ensures:
- The type_integer_ type_array_  param_nums_ is not null.
- The type_integer_ type_array_  param_nums_ length is greater_than_or_equal to 1
  and is less_than_or_equal to 10000.
- All values in the type_integer_ type_array_  param_nums_ are greater_than_or_equal
  to 0 and are less_than_or_equal to 10000.
- The type_integer_  param_k_ is greater_than_or_equal to 0 and is less_than_or_equal
  to 10000.
- The type_integer_ result is the minimum score of the type_integer_ type_array_ expr0
  after applying the mentioned operation at most once for each index in it.
requires: []
