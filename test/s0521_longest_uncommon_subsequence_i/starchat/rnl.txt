- The length of the string parameters `a` and `b` is less than or equal to 100 and is greater than or equal to 1.
- The string parameters `a` and `b` consist of only English lowercase letters.
- If the integer result is greater than or equal to 1, the string parameter `a` or `b` is not a subsequence of the other string parameter.
- If the integer result is equal to -1, the string parameter `a` is a subsequence of the string parameter `b` and the string parameter `b` is a subsequence of the string parameter `a`.
- If the string parameters `a` and `b` are equal to "aaa", the integer result is equal to -1.
- If the string parameters `a` and `b` are equal to "aba", the integer result is equal to 3.
- If the string parameters `a` and `b` are equal to "aaa", the string parameter `b` is equal to "bbb", the integer result is equal to 3.
- If the string parameters `a` and `b` are equal to "aaa", the string parameter `b` is equal to "aaa", the integer result is equal to -1.

Example