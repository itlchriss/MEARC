- The length of the string parameter `s` is less than or equal to 10000 and is greater than or equal to 1.
- The string parameter `s` contains English letters (upper-case and lower-case), digits, and spaces `' '`.
- There is at least one word in `s`.
- The string result is a string of the words in reverse order concatenated by a single space.
- The reversed string should not contain leading or trailing spaces.
- Multiple spaces between two words in the reversed string should be reduced to a single space.