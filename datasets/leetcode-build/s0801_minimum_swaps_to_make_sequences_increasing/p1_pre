The lengths of `listA` and `listB` are the same.
The length of `listA` and `listB` is at least 2.
The elements of `listA` and `listB` are non-negative integers.
The elements of `listA` and `listB` are within the range of 0 to 2 * 10^5.
