Preconditions:
1. The input array `points` must have a length of 3.
2. Each element in the `points` array must have a length of 2.
3. The values of `x` and `y` in each point must be between 0 and 100 (inclusive).

Postconditions:
1. The method should return `true` if the points form a boomerang.
2. The method should return `false` if the points do not form a boomerang.
3. A boomerang is defined as a set of three points that are all distinct and not in a straight line.