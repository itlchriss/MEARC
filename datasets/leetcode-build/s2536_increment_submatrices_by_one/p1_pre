The input integer `n` is a positive integer.
The input 2D integer array `queries` is not null.
The length of `queries` is greater than or equal to - The values of `row1_i`, `col1_i`, `row2_i`, and `col2_i` in each query are valid indices within the range of the matrix `mat`.
