The matrix `mat` is modified by adding 1 to every element in the submatrix specified by each query.
The dimensions of the modified matrix `mat` remain the same as the original matrix.
The modified matrix `mat` is returned as the output.
