The input arrays `arr1` and `arr2` are not null.
The lengths of `arr1` and `arr2` are between 1 and 1000, inclusive.
The elements of `arr2` are distinct.
Each element in `arr2` is also present in `arr1`.
