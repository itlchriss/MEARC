The input strings `board` and `hand` are not null.
The length of `board` is between 1 and 16, inclusive.
The length of `hand` is between 1 and 5, inclusive.
The characters in `board` and `hand` are limited to `'R'`, `'Y'`, `'B'`, `'G'`, and `'W'`.
The initial row of balls on the board does not have any groups of three or more consecutive balls of the same color.
