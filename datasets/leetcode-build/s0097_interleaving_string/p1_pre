1. The input strings `s1`, `s2`, and `s3` are not null.
2. The lengths of `s1`, `s2`, and `s3` are within the specified constraints.
3. `s1`, `s2`, and `s3` consist only of lowercase English letters.
