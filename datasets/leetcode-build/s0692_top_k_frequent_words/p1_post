The method returns a list of strings.
The returned list contains the `k` most frequent strings from the input array `words`.
The strings in the returned list are sorted by their frequency from highest to lowest.
If two strings have the same frequency, they are sorted by their lexicographical order.
