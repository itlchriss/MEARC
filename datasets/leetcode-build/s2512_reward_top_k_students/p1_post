The method returns an array of length `k`.
The array contains the IDs of the top `k` students after ranking them in non-increasing order by their points.
If multiple students have the same points, the one with the lower ID ranks higher.
