Preconditions:
- The input array `rollMax` must have a length of - The elements of `rollMax` must be integers.
- The elements of `rollMax` must be between 1 and 15 (inclusive).
- The input integer `n` must be between 1 and 5000 (inclusive).

Postconditions:
- The method must return an integer.
- The returned integer must be the number of distinct sequences that can be obtained with exactly `n` rolls.
- The returned integer must be modulo 10^9 + 7.