Preconditions:
1. The input string `s` is not null.
2. The input string `s` contains only lowercase English letters.
3. The input integer `k` is greater than or equal to 1.
4. The length of the input string `s` is greater than or equal to `k`.

Postconditions:
1. The output is an integer representing the minimal number of characters that need to be changed to divide the string into `k` non-empty disjoint substrings, each of which is a palindrome.
2. The output is non-negative.