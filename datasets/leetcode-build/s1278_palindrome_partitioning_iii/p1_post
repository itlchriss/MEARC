1. The output is an integer representing the minimal number of characters that need to be changed to divide the string into `k` non-empty disjoint substrings, each of which is a palindrome.
2. The output is non-negative.
