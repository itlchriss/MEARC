Preconditions:
- The input arrays `aliceValues` and `bobValues` are not null.
- The length of `aliceValues` is equal to the length of `bobValues`.
- The length of `aliceValues` is greater than 0.
- The values in `aliceValues` and `bobValues` are integers.
- The values in `aliceValues` and `bobValues` are between 1 and 100 (inclusive).

Postconditions:
- The method returns an integer value representing the result of the game.
- The returned value is 1 if Alice wins, -1 if Bob wins, and 0 if the game results in a draw.
- The method does not modify the input arrays `aliceValues` and `bobValues`.