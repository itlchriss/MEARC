The method returns a boolean value (`true` or `false`).
If `n` is fascinating, the method returns `true`.
If `n` is not fascinating, the method returns `false`.
The resulting number after concatenation contains all the digits from 1 to 9 exactly once.
The resulting number after concatenation does not contain any 0's.
