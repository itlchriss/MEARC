The input array `words` is not null.
The integers `left` and `right` are valid indices within the range of the `words` array.
The strings in the `words` array consist of only lowercase English letters.
