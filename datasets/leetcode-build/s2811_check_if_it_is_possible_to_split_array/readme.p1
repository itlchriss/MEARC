Preconditions:
- The input list `nums` must not be null.
- The input list `nums` must have a length greater than or equal to 1.
- The input integer `m` must be greater than or equal to 1.

Postconditions:
- The method returns `true` if it is possible to split the array into `n` non-empty arrays, otherwise it returns `false`.