Preconditions:
- The input strings `a` and `b` are not null.
- The lengths of `a` and `b` are both greater than 0.

Postconditions:
- The method returns an integer representing the minimum number of operations needed to achieve the goal.
- The input strings `a` and `b` remain unchanged after the method is called.