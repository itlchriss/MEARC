Preconditions:
- The input string `str` is not null.
- The length of `str` is between 1 and 2000, inclusive.
- `str` consists of lowercase English letters.

Postconditions:
- The method returns an integer value.
- The returned value is the number of distinct non-empty subsequences of `str`.
- The returned value is modulo 10^9 + 7.