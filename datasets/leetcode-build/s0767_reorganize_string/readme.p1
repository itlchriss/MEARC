Preconditions:
- The input string `s` is not null.
- The length of `s` is between 1 and 500.
- `s` consists only of lowercase English letters.

Postconditions:
- The returned string is a rearrangement of `s`.
- Any two adjacent characters in the returned string are not the same.
- If it is not possible to rearrange `s` such that no two adjacent characters are the same, the method returns an empty string "".