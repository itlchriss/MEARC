Preconditions:
- The values of `jug1Capacity`, `jug2Capacity`, and `targetCapacity` are positive integers.
- The values of `jug1Capacity` and `jug2Capacity` are less than or equal to 10^6.
- The value of `targetCapacity` is less than or equal to the sum of `jug1Capacity` and `jug2Capacity`.

Postconditions:
- The method returns a boolean value indicating whether it is possible to measure exactly `targetCapacity` liters using the given jugs.
- If the method returns true, there is `targetCapacity` liters of water contained within one or both jugs by the end.