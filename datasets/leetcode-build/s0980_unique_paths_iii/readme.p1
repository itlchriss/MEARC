Preconditions:
- The input grid must be a 2D integer array.
- The grid must have at least one row and one column.
- The grid must have exactly one starting square with a value of - The grid must have exactly one ending square with a value of - The grid may contain empty squares with a value of - The grid may contain obstacles with a value of -
Postconditions:
- The method should return an integer representing the number of 4-directional walks from the starting square to the ending square, that walk over every non-obstacle square exactly once.
- If there is no path that walks over every empty square exactly once, the method should return 0.