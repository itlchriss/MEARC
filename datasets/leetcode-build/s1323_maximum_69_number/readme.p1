Preconditions:
- The input `num` is a positive integer.
- The input `num` consists only of digits `6` and `9`.

Postconditions:
- The output is the maximum number that can be obtained by changing at most one digit in `num`.
- The output is a positive integer.
- The output consists only of digits `6` and `9`.
- If no digit in `num` can be changed to obtain a larger number, the output is equal to the input `num`.