Preconditions:
- The input `x` is a positive integer.
- The input `target` is a positive integer.
- The value of `x` is between 2 and 100 (inclusive).
- The value of `target` is between 1 and 200,000,000 (inclusive).

Postconditions:
- The method returns an integer representing the least number of operators used to write an expression that equals the given `target`.