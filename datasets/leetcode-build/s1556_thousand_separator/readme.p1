Preconditions:
- The input integer `n` must be non-negative (0 <= n <= 2^31 - 1).

Postconditions:
- The output string should contain the same digits as the input integer `n`.
- The output string should have a dot (".") inserted as the thousands separator, if applicable.