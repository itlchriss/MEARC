Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is greater than or equal to 2.
- All the integers in `nums` are in the range `[1, n]` inclusive.
- There is exactly one repeated number in `nums`.

Postconditions:
- The method returns the repeated number in `nums`.