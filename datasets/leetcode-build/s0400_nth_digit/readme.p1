Preconditions:
- The input `n` is a positive integer.
- The input `n` is within the range of `1` to `2^31 - 1`.

Postconditions:
- The method returns an integer representing the `n`th digit of the infinite integer sequence.
- The returned digit is within the range of `0` to `9`.