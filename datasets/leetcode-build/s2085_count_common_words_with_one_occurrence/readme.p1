Preconditions:
- The input arrays `words1` and `words2` are not null.
- The lengths of `words1` and `words2` are between 1 and 1000, inclusive.
- The lengths of the strings in `words1` and `words2` are between 1 and 30, inclusive.
- The strings in `words1` and `words2` consist only of lowercase English letters.

Postconditions:
- The method returns an integer representing the number of strings that appear exactly once in both `words1` and `words2`.