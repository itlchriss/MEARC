Preconditions:
- The input `logs` is not null.
- The length of `logs` is greater than or equal to 1.
- Each element in `logs` is a valid operation described in the requirements.
- The folder names in `logs` consist of lowercase English letters and digits.

Postconditions:
- The return value is an integer representing the minimum number of operations needed to go back to the main folder after the change folder operations.