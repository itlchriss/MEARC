The output `answer` is not null.
The length of `answer` is equal to `n`.
The values in `answer` are non-negative integers.
The values in `answer` represent the number of different nodes visited starting from each node.
