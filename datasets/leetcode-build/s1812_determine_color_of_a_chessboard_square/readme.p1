Preconditions:
- The input string `coordinates` must have a length of 2.
- The first character of `coordinates` must be a letter between 'a' and 'h'.
- The second character of `coordinates` must be a digit between '1' and '8'.

Postconditions:
- If the square represented by `coordinates` is white, the method should return `true`.
- If the square represented by `coordinates` is black, the method should return `false`.