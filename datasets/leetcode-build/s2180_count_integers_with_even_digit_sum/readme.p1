Preconditions:
- The input `num` is a positive integer.
- The input `num` is less than or equal to 1000.

Postconditions:
- The output is an integer representing the number of positive integers less than or equal to `num` whose digit sums are even.