Preconditions:
- The input list `words` must not be null.
- The input list `words` must not be empty.
- The input list `words` must contain strings.
- The input character `separator` must not be null.

Postconditions:
- The output list must not be null.
- The output list must not contain empty strings.
- The output list must contain strings formed after splitting each string in `words` by `separator`.
- The output list must maintain the same order as the strings were initially given in `words`.