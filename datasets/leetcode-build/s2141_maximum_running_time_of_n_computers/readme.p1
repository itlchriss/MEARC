Preconditions:
- The input `n` is a positive integer representing the number of computers.
- The input `batteries` is a non-empty array of positive integers representing the battery life of each computer.

Postconditions:
- The method returns a long integer representing the maximum number of minutes that all `n` computers can run simultaneously.

Note: The method does not modify the input array `batteries`.