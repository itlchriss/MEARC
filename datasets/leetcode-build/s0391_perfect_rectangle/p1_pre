The input array `rectangles` is not null.
The length of `rectangles` is greater than or equal to 1.
Each rectangle in `rectangles` is represented by an array of length 4.
The values of `x_i`, `y_i`, `a_i`, and `b_i` for each rectangle are within the range of -10^5 to 10^5.
