Preconditions:
- The input `encodedText` is a valid encoding of some `originalText` that does not have trailing spaces.
- The input `rows` is a positive integer.

Postconditions:
- The method returns a string that is the original text encoded in `encodedText`.
- The returned string does not have trailing spaces.