Preconditions:
- The input integer `n` is non-negative (0 <= n <= 10^9).

Postconditions:
- The returned string represents the binary representation of `n` in base -2.
- The returned string does not have leading zeros, unless the string is "0".