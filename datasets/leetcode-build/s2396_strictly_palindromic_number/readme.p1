Preconditions:
- The input integer `n` must be greater than or equal to 4.
- The input integer `n` must be less than or equal to 10^5.

Postconditions:
- The method should return `true` if `n` is strictly palindromic.
- The method should return `false` if `n` is not strictly palindromic.