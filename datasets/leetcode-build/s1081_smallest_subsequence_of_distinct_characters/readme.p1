Preconditions:
- The input string `s` is not null.
- The length of `s` is between 1 and 1000.
- `s` consists only of lowercase English letters.

Postconditions:
- The output string is not null.
- The output string is a subsequence of `s`.
- The output string contains all the distinct characters of `s` exactly once.
- The output string is lexicographically smallest among all possible subsequences of `s` that satisfy the previous condition.