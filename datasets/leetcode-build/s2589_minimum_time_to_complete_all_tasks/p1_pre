The input `tasks` is a 2D integer array.
The length of `tasks` is between 1 and - Each element in `tasks` is an array of length - The start time `start_i` of each task is between 1 and - The end time `end_i` of each task is between 1 and - The duration `duration_i` of each task is between 1 and `end_i - start_i + 1`.
