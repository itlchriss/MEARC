The output array `ans` is not null.
The length of `ans` is equal to the length of `queries`.
Each element in `ans` is an integer.
The value of each element in `ans` is the number of strings in the range l to r (inclusive) of `words` that start and end with a vowel.
