The returned array `C` is not null.
The length of the returned array `C` is equal to the length of arrays `A` and `B`.
The elements in the returned array `C` are integers.
The elements in the returned array `C` are non-negative.
The elements in the returned array `C` are in non-decreasing order.
For each index `i` in the returned array `C`, `C[i]` is equal to the count of numbers that are present at or before the index `i` in both arrays `A` and `B`.
