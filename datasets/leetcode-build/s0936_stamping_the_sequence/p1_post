The returned array contains the indices of the left-most letter being stamped at each turn.
The returned array is empty if it is not possible to obtain `target` from `s` within `10 * target.length` turns.
