Preconditions:
- The lengths of `word1` and `word2` are equal.
- `word1` and `word2` contain only lowercase English letters.

Postconditions:
- If `word1` and `word2` are close, return `true`.
- If `word1` and `word2` are not close, return `false`.