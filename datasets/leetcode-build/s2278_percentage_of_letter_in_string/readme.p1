Preconditions:
- The input string `s` is not null.
- The input string `s` is not empty.
- The input string `s` consists of lowercase English letters.
- The input character `letter` is a lowercase English letter.

Postconditions:
- The output is an integer representing the percentage of characters in `s` that equal `letter`, rounded down to the nearest whole percent.