Preconditions:
- The input grid is a square matrix with dimensions n x n.
- Each element in the grid is either 0 or 1.

Postconditions:
- The grid is valid, meaning all cells above the main diagonal are zeros.
- The minimum number of steps needed to make the grid valid is returned.
- If the grid cannot be made valid, -1 is returned.