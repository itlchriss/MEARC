Preconditions:
- The input arrays `word1` and `word2` are not null.
- The lengths of `word1` and `word2` are equal.

Postconditions:
- The method returns `true` if the concatenated strings of `word1` and `word2` are equal, and `false` otherwise.