The output is a list of lists representing the skyline.
Each subarray in the output contains two elements: `x` and `y`.
The values of `x` and `y` are integers.
The output list is sorted by the value of `x` in non-decreasing order.
The last subarray in the output has a `y` value of - There are no consecutive subarrays in the output with the same `y` value.
