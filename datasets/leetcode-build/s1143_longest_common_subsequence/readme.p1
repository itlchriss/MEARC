Preconditions:
- The input strings `text1` and `text2` are not null.
- The lengths of `text1` and `text2` are between 1 and 1000, inclusive.
- `text1` and `text2` consist of only lowercase English characters.

Postconditions:
- The method returns an integer representing the length of the longest common subsequence of `text1` and `text2`.
- If there is no common subsequence, the method returns 0.