The input `n` is a positive integer representing the number of buildings.
The input `requests` is a non-empty array of length `m` where each element is an array of length 2 representing a transfer request.
Each transfer request in `requests` is valid, meaning the values of `from_i` and `to_i` are between 0 and `n-1`, inclusive.
