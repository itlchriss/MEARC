Preconditions:
- The input `n` must be a non-negative integer.

Postconditions:
- The method returns a double value representing the probability that soup A will be empty first, plus half the probability that A and B become empty at the same time.
- The returned probability is within 10^-5 of the actual answer.