Preconditions:
- The input `bad` must be a positive integer.
- The input `n` must be a positive integer greater than or equal to `bad`.

Postconditions:
- The method returns an integer representing the first bad version.
- The returned version is a bad version.
- All versions after the returned version are also bad versions.