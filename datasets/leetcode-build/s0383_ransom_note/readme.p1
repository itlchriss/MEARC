Preconditions:
- The lengths of `ransomNote` and `magazine` are both greater than or equal to - `ransomNote` and `magazine` consist only of lowercase English letters.

Postconditions:
- If `ransomNote` can be constructed from `magazine`, the method returns `true`.
- If `ransomNote` cannot be constructed from `magazine`, the method returns `false`.