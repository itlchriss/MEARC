Preconditions:
- The input `n` must be a non-negative integer.
- The input `n` must be less than or equal to 10^4.

Postconditions:
- The method should return an integer representing the number of trailing zeroes in `n!`.
- If `n` is 0, the method should return 0.
- If `n` is greater than 0, the method should return the number of trailing zeroes in `n!`.