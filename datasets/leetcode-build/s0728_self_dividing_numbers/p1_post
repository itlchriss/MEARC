The method returns a list of integers.
The list contains all the self-dividing numbers in the range `[left, right]`.
Each number in the list is divisible by every digit it contains.
Each number in the list does not contain the digit zero.
