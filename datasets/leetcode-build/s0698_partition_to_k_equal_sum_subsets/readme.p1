Preconditions:
- The input array `nums` is not null.
- The input array `nums` has at least `k` elements.
- The input integer `k` is greater than or equal to - The input integer `k` is less than or equal to the length of the input array `nums`.
- Each element in the input array `nums` is a positive integer.
- The frequency of each element in the input array `nums` is between 1 and 4 (inclusive).

Postconditions:
- The method returns `true` if it is possible to divide the input array `nums` into `k` non-empty subsets whose sums are all equal.
- The method returns `false` if it is not possible to divide the input array `nums` into `k` non-empty subsets whose sums are all equal.