1. The input string `columnTitle` is not null.
2. The input string `columnTitle` is not empty.
3. The input string `columnTitle` consists only of uppercase English letters.
4. The length of the input string `columnTitle` is between 1 and 7 (inclusive).
5. The input string `columnTitle` is within the range of "A" to "FXSHRXW".
