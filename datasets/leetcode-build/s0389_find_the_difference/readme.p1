Preconditions:
- The input strings `s` and `t` must not be null.
- The length of string `t` must be one more than the length of string `s`.
- The strings `s` and `t` must consist of lowercase English letters.

Postconditions:
- The output must be a single character.
- The output character must be the letter that was added to string `t` after shuffling string `s`.