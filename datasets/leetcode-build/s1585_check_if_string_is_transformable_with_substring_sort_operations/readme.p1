Preconditions:
- The lengths of strings `s` and `t` are equal.
- The lengths of strings `s` and `t` are between 1 and 10^5.
- Strings `s` and `t` consist of only digits.

Postconditions:
- The method returns `true` if it is possible to transform `s` into `t` using the given operation, otherwise it returns `false`.