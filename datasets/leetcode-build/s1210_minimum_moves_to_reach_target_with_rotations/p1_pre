The input grid must be a square grid with dimensions `n*n`.
The snake must start at the top left corner at `(0, 0)` and `(0, 1)`.
The snake must end at the lower right corner at `(n-1, n-2)` and `(n-1, n-1)`.
The grid must have empty cells represented by zeros and blocked cells represented by ones.
It is guaranteed that the snake starts at empty cells.
