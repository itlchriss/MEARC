Preconditions:
- The length of string `p` is greater than or equal to - The length of string `s` is greater than or equal to the length of string `p`.
- The length of array `removable` is less than the length of string `s`.
- The elements in array `removable` are distinct.
- String `p` is a subsequence of string `s`.
- String `s` and string `p` consist of lowercase English letters.

Postconditions:
- The returned value is an integer representing the maximum number of characters that can be removed from string `s` such that string `p` is still a subsequence of `s`.