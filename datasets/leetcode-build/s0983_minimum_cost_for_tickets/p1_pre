The input arrays `days` and `costs` are not null.
The length of `days` is greater than or equal to 1 and less than or equal to 365.
Each element in `days` is an integer between 1 and 365.
The elements in `days` are in strictly increasing order.
The length of `costs` is 3.
Each element in `costs` is an integer between 1 and 1000.
