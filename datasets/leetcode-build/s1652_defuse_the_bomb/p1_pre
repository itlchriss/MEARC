The input array `code` is not null.
The length of the input array `code` is greater than 0.
The input integer `k` is within the range `-(n - 1) <= k <= n - 1`, where `n` is the length of the input array `code`.
Each element in the input array `code` is within the range `1 <= code[i] <= 100`.
