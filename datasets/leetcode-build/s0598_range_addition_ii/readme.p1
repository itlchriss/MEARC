Preconditions:
- The input matrix `M` is initialized with all `0`'s.
- The input array `ops` is not null.

Postconditions:
- The maximum integer in the matrix `M` is found.
- The number of maximum integers in the matrix `M` after performing all the operations is returned.