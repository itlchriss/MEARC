The method returns `true` if for every index `i` in the range `0 <= i < n`, the digit `i` occurs `num[i]` times in `num`.
The method returns `false` if there exists an index `i` in the range `0 <= i < n` such that the digit `i` does not occur `num[i]` times in `num`.
