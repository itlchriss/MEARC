Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is greater than or equal to 2.
- The input integer `k` is greater than or equal to 1.

Postconditions:
- The output is an integer representing the number of pairs `(i, j)` that meet the requirements.
- The output is non-negative.