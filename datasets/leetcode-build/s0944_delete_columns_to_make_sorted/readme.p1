Preconditions:
- The input array `strs` is not null.
- The length of `strs` is greater than - All strings in `strs` have the same length.
- Each string in `strs` consists of lowercase English letters.

Postconditions:
- The method returns an integer representing the number of columns that will be deleted.
- The original array `strs` remains unchanged.