Preconditions:
- The input string `s` is not null.
- The input string `s` consists only of letters 'a' and 'b'.

Postconditions:
- The method returns an integer representing the minimum number of steps to make the given string empty.