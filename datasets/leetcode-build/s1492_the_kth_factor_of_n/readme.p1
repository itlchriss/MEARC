Preconditions:
- The input integers `n` and `k` are positive.
- `n` is greater than or equal to `k`.

Postconditions:
- The method returns an integer.
- If `n` has less than `k` factors, the method returns -1.
- If `n` has at least `k` factors, the method returns the kth factor of `n`.