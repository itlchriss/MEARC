Preconditions:
- The maximum choosable integer (`maxChoosableInteger`) must be between 1 and 20 (inclusive).
- The desired total (`desiredTotal`) must be between 0 and 300 (inclusive).

Postconditions:
- The method should return `true` if the first player can force a win, and `false` otherwise.