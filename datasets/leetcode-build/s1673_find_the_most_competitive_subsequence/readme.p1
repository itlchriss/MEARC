Preconditions:
- The input array `nums` is not null.
- The input array `nums` has at least `k` elements.
- The input array `nums` contains only non-negative integers.
- The input integer `k` is a positive integer.

Postconditions:
- The returned array `result` is not null.
- The length of the returned array `result` is equal to `k`.
- The elements in the returned array `result` are selected from the input array `nums`.
- The elements in the returned array `result` are in the same order as they appear in the input array `nums`.
- The returned array `result` is the most competitive subsequence of `nums` of size `k`.