The input `questions` is a 2D integer array.
The length of `questions` is greater than or equal to 1 and less than or equal to 10^5.
Each element in `questions` is an array of length 2.
The first element of each array in `questions` represents the number of points that can be earned by solving the question.
The second element of each array in `questions` represents the number of questions that cannot be solved after solving the current question.
