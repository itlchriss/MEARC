Preconditions:
- The input string `s` is not null.
- The length of `s` is between 1 and - Each character in `s` is either 'L' or 'R'.
- `s` is a balanced string.

Postconditions:
- The method returns an integer representing the maximum amount of split balanced strings.
- Each split substring contains the same number of 'L' and 'R' characters.