Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.
- The elements in the input array `nums` are unique.

Postconditions:
- The output is a list of lists, representing all possible subsets of the input array `nums`.
- The output does not contain any duplicate subsets.
- The order of the subsets in the output can be arbitrary.