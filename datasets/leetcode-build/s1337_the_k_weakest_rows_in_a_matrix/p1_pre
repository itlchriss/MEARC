The input matrix `mat` is not null.
The input matrix `mat` has at least one row.
The input matrix `mat` has at least one column.
The input matrix `mat` contains only 0s and 1s.
The input integer `k` is greater than - The input integer `k` is less than or equal to the number of rows in `mat`.
