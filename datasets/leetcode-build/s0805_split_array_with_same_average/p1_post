The method returns a boolean value indicating whether it is possible to split the array into two non-empty arrays with equal average.
If it is possible to split the array into two non-empty arrays with equal average, the method returns `true`.
If it is not possible to split the array into two non-empty arrays with equal average, the method returns `false`.
