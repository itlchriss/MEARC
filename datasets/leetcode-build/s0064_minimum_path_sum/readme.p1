Preconditions:
- The input grid must not be null.
- The input grid must have at least one row and one column.
- The input grid must have non-negative numbers.

Postconditions:
- The method returns an integer representing the minimum sum of all numbers along the path from the top left to the bottom right of the grid.