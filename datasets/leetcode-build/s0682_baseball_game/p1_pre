The input `ops` is not null.
The length of `ops` is greater than or equal to 1.
Each element in `ops` is either an integer, "+", "D", or "C".
For each "+" operation, there are at least two previous scores on the record.
For each "D" and "C" operation, there is at least one previous score on the record.
