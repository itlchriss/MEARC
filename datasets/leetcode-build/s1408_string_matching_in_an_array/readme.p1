Preconditions:
- The input array `words` is not null.
- The length of the input array `words` is greater than or equal to - Each string in the input array `words` is not null.
- Each string in the input array `words` contains only lowercase English letters.
- Each string in the input array `words` has a length greater than or equal to 1 and less than or equal to - Each string in the input array `words` is unique.

Postconditions:
- The method returns a list of strings.
- The list contains all strings in `words` that are substrings of another word in `words`.
- The order of the strings in the output list can be in any order.