Preconditions:
- The input string `word` is not null.
- The length of `word` is greater than or equal to 1 and less than or equal to 100.
- `word` consists only of lowercase English letters.

Postconditions:
- The method returns an integer representing the minimum number of operations needed to remove all adjacent almost-equal characters from `word`.