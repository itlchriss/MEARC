Preconditions:
1. The input string `digits` must not be null.
2. The input string `digits` must only contain digits from 2 to 9 inclusive.
3. The length of the input string `digits` must be between 0 and 4 inclusive.

Postconditions:
1. The method should return a list of strings representing all possible letter combinations that the input number could represent.
2. The order of the letter combinations in the output list does not matter.
3. If the input string `digits` is empty, the method should return an empty list.