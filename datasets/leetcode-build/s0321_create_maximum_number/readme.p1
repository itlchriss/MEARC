Preconditions:
- The input arrays `nums1` and `nums2` must not be null.
- The length of `nums1` and `nums2` must be greater than or equal to `k`.
- The values in `nums1` and `nums2` must be between 0 and 9.

Postconditions:
- The returned array must have a length of `k`.
- The relative order of the digits from the same array in the returned array must be preserved.
- The returned array must contain the maximum possible number of length `k` that can be created from the digits of `nums1` and `nums2`.