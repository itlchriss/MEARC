Preconditions:
- The input integers `n` and `k` are positive.
- The value of `k` is valid for the given value of `n`.

Postconditions:
- The method returns a character representing the `k`th bit in the binary string `S_n`.