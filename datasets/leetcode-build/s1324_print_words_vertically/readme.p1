Preconditions:
- The input string `s` is not null.
- The length of `s` is between 1 and - `s` contains only uppercase English letters.
- There is only one space between two words in `s`.

Postconditions:
- The method returns a list of strings.
- The words in the output list are in the same order as they appear in `s`.
- Each word is printed vertically, with each character in a separate column.
- Trailing spaces are not allowed in the output list.