Preconditions:
- The input `num` must be a positive integer.
- The input `num` must be within the range of `1` to `2^31 - 1`.

Postconditions:
- The method should return `true` if `num` is a perfect square.
- The method should return `false` if `num` is not a perfect square.