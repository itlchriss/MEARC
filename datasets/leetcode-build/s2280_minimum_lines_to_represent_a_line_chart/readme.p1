Preconditions:
- The input `stockPrices` is a 2D integer array.
- Each element `stockPrices[i]` is an array of length 2, representing the day and price of the stock.
- The values of `day_i` and `price_i` are integers between 1 and 10^9.
- All `day_i` values are distinct.

Postconditions:
- The method returns an integer representing the minimum number of lines needed to represent the line chart.