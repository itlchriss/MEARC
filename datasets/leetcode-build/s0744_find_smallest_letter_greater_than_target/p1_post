1. The returned character is larger than the `target` character.
2. The returned character is the smallest character in the `letters` array that is larger than the `target` character.
3. If there is no character in the `letters` array that is larger than the `target` character, the returned character is the smallest character in the `letters` array.
