Preconditions:
- The input string `date` must be in the format "YYYY-MM-DD".
- The length of the input string `date` must be 10.
- The characters at indices 4 and 7 of the input string `date` must be '-'.
- All other characters in the input string `date` must be digits.
- The input string `date` must represent a calendar date between Jan 1st, 1900 and Dec 31st, 2019.

Postconditions:
- The method should return an integer representing the day number of the year for the given input date.