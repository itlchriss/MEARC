The `commands` array is not null.
The `obstacles` array is not null.
The length of the `commands` array is between 1 and 10,000.
Each element in the `commands` array is either -2, -1, or an integer between 1 and 9.
The length of the `obstacles` array is between 0 and 10,000.
Each element in the `obstacles` array is a pair of integers between -30,000 and 30,000.
