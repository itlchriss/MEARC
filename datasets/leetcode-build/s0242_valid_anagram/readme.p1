Preconditions:
1. The method should take two string parameters, `s` and `t`.
2. The length of `s` and `t` should be between 1 and 5 * 10^4.
3. `s` and `t` should consist of lowercase English letters.

Postconditions:
1. The method should return a boolean value indicating whether `t` is an anagram of `s`.
2. If `t` is an anagram of `s`, the method should return `true`.
3. If `t` is not an anagram of `s`, the method should return `false`.