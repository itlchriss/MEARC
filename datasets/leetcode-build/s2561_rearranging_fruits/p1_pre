The lengths of `basket1` and `basket2` are equal.
The lengths of `basket1` and `basket2` are both greater than or equal to 1.
The elements of `basket1` and `basket2` are integers.
The elements of `basket1` and `basket2` are greater than or equal to 1.
The elements of `basket1` and `basket2` are less than or equal to 10^9.
