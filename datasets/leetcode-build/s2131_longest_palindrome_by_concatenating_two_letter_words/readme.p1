Preconditions:
- The input array `words` is not null.
- The length of each element in `words` is exactly - Each element in `words` consists of lowercase English letters.

Postconditions:
- The method returns an integer representing the length of the longest palindrome that can be created.
- If it is impossible to create any palindrome, the method returns 0.