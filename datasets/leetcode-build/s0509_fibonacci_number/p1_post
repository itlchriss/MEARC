The method should return an integer value representing the Fibonacci number `F(n)`.
The returned value should be the sum of the two preceding Fibonacci numbers, `F(n-1)` and `F(n-2)`, if `n` is greater than 1.
If `n` is 0, the method should return 0.
If `n` is 1, the method should return 1.
