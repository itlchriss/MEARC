Preconditions:
- The input strings `s` and `t` are not null.
- The lengths of `s` and `t` are between 1 and 10^5.

Postconditions:
- The method returns an integer representing the minimum number of characters that need to be appended to `s` so that `t` becomes a subsequence of `s`.