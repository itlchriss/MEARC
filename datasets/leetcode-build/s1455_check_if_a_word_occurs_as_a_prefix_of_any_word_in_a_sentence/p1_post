If `searchWord` is a prefix of any word in `sentence`, return the index of the first occurrence of the word where `searchWord` is a prefix (1-indexed).
If `searchWord` is not a prefix of any word in `sentence`, return -1.
