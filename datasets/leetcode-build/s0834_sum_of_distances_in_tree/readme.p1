Preconditions:
- The input `n` is a positive integer.
- The input `edges` is a 2D array of size `n-1` where each element is an array of size - The elements in `edges` are valid node labels in the range from 0 to `n-1`.
- The elements in `edges` represent valid edges in the tree.
- The given input represents a valid tree.

Postconditions:
- The output `answer` is an array of length `n`.
- Each element in `answer` represents the sum of distances between the corresponding node and all other nodes in the tree.