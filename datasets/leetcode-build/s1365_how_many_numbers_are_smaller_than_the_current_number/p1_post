The output array must not be null.
The length of the output array must be the same as the input array.
Each element in the output array must be an integer.
For each element `nums[i]` in the input array, the corresponding element in the output array must be the count of valid `j's` such that `j != i` and `nums[j] < nums[i]`.
