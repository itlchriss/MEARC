Preconditions:
- The input list `nums` must not be null.
- The input list `nums` must have a length greater than or equal to 2.
- The input integer `target` must be within the range of -50 to 50.

Postconditions:
- The method returns an integer representing the number of pairs `(i, j)` where `0 <= i < j < n` and `nums[i] + nums[j] < target`.