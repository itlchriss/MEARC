Preconditions:
- The length of `s1` and `s2` must be - `s1` and `s2` consist only of lowercase English letters.

Postconditions:
- The method returns `true` if it is possible to make `s1` and `s2` equal using the given operation.
- The method returns `false` if it is not possible to make `s1` and `s2` equal using the given operation.