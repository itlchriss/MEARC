Preconditions:
- The input `n` is a positive integer.
- The input `n` is less than or equal to 10^9.

Postconditions:
- The method returns an integer representing the number of ways `n` can be written as the sum of consecutive positive integers.