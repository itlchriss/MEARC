The output `count` is an integer array of length `points.length`.
Each element in `count` represents the number of rectangles that contain the corresponding point in `points`.
The elements in `count` are in the same order as the points in `points`.
