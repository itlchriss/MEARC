Preconditions:
- The input `trees` is a non-empty array.
- Each element in `trees` is an array of length 2 representing the coordinates of a tree.
- The coordinates of the trees are within the range of 0 to 
Postconditions:
- The output is a 2D array representing the coordinates of trees that are located on the fence perimeter.
- The output array is sorted in clockwise order.
- The output array contains all the trees in `trees` that are located on the fence perimeter.
- The output array does not contain any duplicate trees.