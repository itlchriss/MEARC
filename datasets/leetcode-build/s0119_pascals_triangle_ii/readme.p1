Preconditions:
- The input `rowIndex` is a non-negative integer.
- The input `rowIndex` is less than or equal to 33.

Postconditions:
- The method returns a List of Integers.
- The List contains the `rowIndex`-th row of Pascal's triangle.
- The List is in the correct order, with the first element being the leftmost number in the row and the last element being the rightmost number in the row.
- The List contains the correct values, where each number is the sum of the two numbers directly above it in Pascal's triangle.