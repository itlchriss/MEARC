The output is an integer representing the minimum number of substrings needed to partition the input string.
Each substring in the partition has unique characters.
No letter appears in a single substring more than once.
Each character in the input string belongs to exactly one substring in the partition.
