The input `n` is a positive integer representing the number of functions.
The input `logs` is a list of strings representing the log messages.
Each log message in `logs` is formatted as "{function_id}:{start|end}:{timestamp}".
The function IDs in `logs` range from 0 to n-- The timestamps in `logs` range from 0 to 10^- There are no two start events happening at the same timestamp.
There are no two end events happening at the same timestamp.
Each function has an "end" log for each "start" log.
