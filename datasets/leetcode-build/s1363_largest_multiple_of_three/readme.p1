Preconditions:
- The input array `digits` must not be null.
- The length of the input array `digits` must be between 1 and 10,000 (inclusive).
- Each element in the input array `digits` must be an integer between 0 and 9 (inclusive).

Postconditions:
- The method should return a string representing the largest multiple of three that can be formed by concatenating some of the given digits in any order.
- If there is no answer, the method should return an empty string.
- The returned string must not contain unnecessary leading zeros.