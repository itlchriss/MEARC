Preconditions:
- The input integers `n` and `k` must be positive integers.
- `k` must be less than or equal to `n`.

Postconditions:
- The method returns an integer, which is the `k`th lexicographically smallest number in the range `[1, n]`.
- The returned integer is between 1 and `n`, inclusive.