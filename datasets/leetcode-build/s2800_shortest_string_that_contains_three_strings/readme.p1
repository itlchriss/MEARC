Preconditions:
- The input strings `a`, `b`, and `c` are not null.
- The lengths of `a`, `b`, and `c` are between 1 and 100 (inclusive).
- `a`, `b`, and `c` consist only of lowercase English letters.

Postconditions:
- The returned string is the lexicographically smallest string that contains `a`, `b`, and `c` as substrings.
- The returned string has a length that is at least the sum of the lengths of `a`, `b`, and `c`.