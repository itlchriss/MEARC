Preconditions:
- The input array `chars` is not null.
- The input array `chars` is not empty.
- The input array `chars` contains only lowercase English letters, uppercase English letters, digits, or symbols.

Postconditions:
- The input array `chars` is modified to store the compressed string.
- The length of the modified input array `chars` is returned as the new length of the array.