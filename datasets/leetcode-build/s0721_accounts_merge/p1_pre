The input list `accounts` is not null.
Each element `accounts[i]` in the `accounts` list is not null.
The first element `accounts[i][0]` in each `accounts[i]` list is a non-empty string representing a name.
The rest of the elements in each `accounts[i]` list are non-empty strings representing emails.
The length of the `accounts` list is between 1 and 1000.
The length of each `accounts[i]` list is between 2 and 10.
The length of each email `accounts[i][j]` (for j > 0) is between 1 and 30.
The characters in `accounts[i][0]` are all English letters.
