1. The method should return `true` if it is possible to partition the array into three non-empty parts with equal sums.
2. The method should return `false` if it is not possible to partition the array into three non-empty parts with equal sums.
