The method returns an integer representing the total cost to insert all elements from `instructions` into `nums`.
The returned total cost is non-negative.
The returned total cost is less than or equal to 10^9 + 7.
