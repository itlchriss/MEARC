Preconditions:
- The input arrays `nums1` and `nums2` are not null.
- The lengths of `nums1` and `nums2` are greater than or equal to - The elements in `nums1` and `nums2` are integers between 1 and 
Postconditions:
- The returned array `answer` is not null.
- The length of `answer` is - The first element of `answer` is the number of indices `i` such that `0 <= i < n` and `nums1[i]` occurs at least once in `nums2`.
- The second element of `answer` is the number of indices `i` such that `0 <= i < m` and `nums2[i]` occurs at least once in `nums1`.