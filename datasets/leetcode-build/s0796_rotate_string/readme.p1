Preconditions:
- The input strings `s` and `goal` are not null.
- The lengths of `s` and `goal` are between 1 and 100 (inclusive).
- `s` and `goal` consist only of lowercase English letters.

Postconditions:
- The method returns `true` if and only if `s` can become `goal` after some number of shifts.
- The method returns `false` if `s` cannot become `goal` after any number of shifts.