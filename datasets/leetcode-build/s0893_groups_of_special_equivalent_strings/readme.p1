Preconditions:
- The input array `words` is not null.
- The length of `words` is greater than or equal to - The length of each string in `words` is greater than or equal to 1 and less than or equal to - Each string in `words` consists of lowercase English letters.
- All the strings in `words` have the same length.

Postconditions:
- The method returns an integer representing the number of groups of special-equivalent strings from `words`.