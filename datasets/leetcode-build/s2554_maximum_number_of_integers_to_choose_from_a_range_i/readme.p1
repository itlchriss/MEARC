Preconditions:
- The input array `banned` is not null.
- The input array `banned` contains integers within the range `[1, n]`.
- The input integer `n` is greater than or equal to 1.
- The input integer `maxSum` is greater than or equal to 1.

Postconditions:
- The output integer represents the maximum number of integers that can be chosen.
- The output integer is within the range `[0, n]`.
- The sum of the chosen integers does not exceed `maxSum`.
- The chosen integers are within the range `[1, n]`.
- Each chosen integer does not appear in the array `banned`.