Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is at least 2.

Postconditions:
- The returned value is an integer.
- If there is a square streak in the input array `nums`, the returned value is the length of the longest square streak.
- If there is no square streak in the input array `nums`, the returned value is -1.