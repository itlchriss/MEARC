Preconditions:
- The lengths of `s` and `goal` are both greater than or equal to - The lengths of `s` and `goal` are both less than or equal to 2 * 10^- `s` and `goal` consist of lowercase letters.

Postconditions:
- If it is possible to swap two letters in `s` such that the result is equal to `goal`, return `true`.
- If it is not possible to swap two letters in `s` such that the result is equal to `goal`, return `false`.