Preconditions:
- The input array `nums` is not null.
- The length of `nums` is greater than or equal to 2.

Postconditions:
- The method returns an integer representing the maximum length of all alternating subarrays present in `nums`.
- If no alternating subarray exists in `nums`, the method returns -1.