The input `accounts` is a 2D integer array.
The length of `accounts` is equal to `m`.
The length of each subarray in `accounts` is equal to `n`.
The values in `accounts` are positive integers.
The values in `accounts` are less than or equal to 100.
