The input array `nums` is not null.
The input array `requests` is not null.
The length of `nums` is equal to `n`.
The length of `requests` is equal to `m`.
Each element in `nums` is an integer between 0 and 10^- Each element in `requests` is an array of length - The first element of each array in `requests` is a valid index in `nums`.
The second element of each array in `requests` is a valid index in `nums`.
The start index of each request is less than or equal to the end index.
