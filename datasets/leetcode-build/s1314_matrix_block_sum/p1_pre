The input matrix `mat` is not null.
The input matrix `mat` has at least one row and one column.
The input integer `k` is greater than or equal to 1.
The input integer `k` is less than or equal to the minimum of the number of rows and columns in `mat`.
