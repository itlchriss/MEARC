1. If there is a non-empty intersection between `event1` and `event2`, the method should return `true`.
2. If there is no intersection between `event1` and `event2`, the method should return `false`.
