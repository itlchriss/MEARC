Preconditions:
- The input integers `n` and `k` are both positive.
- `n` is less than or equal to `k`.
- `k` is less than or equal to `26 * n`.

Postconditions:
- The returned string has a length equal to `n`.
- The returned string has a numeric value equal to `k`.
- The returned string is lexicographically smallest among all strings with length `n` and numeric value `k`.