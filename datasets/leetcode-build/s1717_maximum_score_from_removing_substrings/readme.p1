Preconditions:
- The input string `s` is not null.
- The input string `s` consists of lowercase English letters.
- The length of the input string `s` is between 1 and 10^5.
- The values of `x` and `y` are between 1 and 10^4.

Postconditions:
- The method returns an integer representing the maximum points that can be gained after applying the operations on `s`.