The returned list `words` contains the same elements as the input array `words`.
The returned list `words` does not contain any adjacent elements that are anagrams of each other.
The returned list `words` is obtained by deleting anagrams from the input array `words` in any arbitrary order.
