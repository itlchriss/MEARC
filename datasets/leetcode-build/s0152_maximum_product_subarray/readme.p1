Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is greater than 0.

Postconditions:
- The method returns an integer value representing the maximum product of a contiguous non-empty subarray within the input array `nums`.
- The returned product value fits in a 32-bit integer.