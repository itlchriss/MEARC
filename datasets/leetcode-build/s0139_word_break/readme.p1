Preconditions:
- The input string `s` is not null.
- The input list `wordDict` is not null.
- The input list `wordDict` does not contain any null elements.
- The input list `wordDict` does not contain any empty strings.
- The input list `wordDict` does not contain any duplicate strings.

Postconditions:
- The method returns `true` if `s` can be segmented into a space-separated sequence of one or more dictionary words.
- The method returns `false` if `s` cannot be segmented into a space-separated sequence of one or more dictionary words.