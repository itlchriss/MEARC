Preconditions:
- The input strings `s` and `t` are not null.
- The lengths of `s` and `t` are equal.
- `s` and `t` consist of lowercase English letters only.

Postconditions:
- The method returns an integer representing the minimum number of steps required to make `t` an anagram of `s`.