Preconditions:
- The length of `baseCosts` array, `n`, must be greater than - The length of `toppingCosts` array, `m`, must be greater than or equal to - Each element in `baseCosts` array must be a positive integer.
- Each element in `toppingCosts` array must be a positive integer.
- The value of `target` must be a positive integer.

Postconditions:
- The method should return an integer representing the closest possible cost of the dessert to the target price.
- If there are multiple possible costs, the method should return the lower one.