Preconditions:
1. The method `multiply` takes two non-null string parameters `num1` and `num2`.
2. The lengths of `num1` and `num2` are between 1 and 200 (inclusive).
3. `num1` and `num2` consist only of digits.
4. `num1` and `num2` do not contain any leading zeros, except if they are both equal to "0".

Postconditions:
1. The method returns a non-null string representing the product of `num1` and `num2`.
2. The returned string consists only of digits.
3. The returned string does not contain any leading zeros, except if the product is equal to 0.