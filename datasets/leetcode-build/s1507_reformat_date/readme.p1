Preconditions:
- The input `date` string is not null.
- The input `date` string is in the format "Day Month Year".
- The `Day` part of the input `date` string is in the set {"1st", "2nd", "3rd", "4th", ..., "30th", "31st"}.
- The `Month` part of the input `date` string is in the set {"Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"}.
- The `Year` part of the input `date` string is in the range [1900, 2100].

Postconditions:
- The output string is in the format "YYYY-MM-DD".
- The `YYYY` part of the output string represents the 4-digit year.
- The `MM` part of the output string represents the 2-digit month.
- The `DD` part of the output string represents the 2-digit day.