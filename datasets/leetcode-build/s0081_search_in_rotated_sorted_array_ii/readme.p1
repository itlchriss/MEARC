Preconditions:
- The input array `nums` is not null.
- The input array `nums` is sorted in non-decreasing order.
- The input array `nums` is rotated at some pivot index `k`.
- The input array `nums` may contain duplicates.
- The input integer `target` is within the range of -10^4 to 10^4.

Postconditions:
- The method returns a boolean value indicating whether the `target` is present in the `nums` array.
- If the `target` is present in the `nums` array, the method returns `true`.
- If the `target` is not present in the `nums` array, the method returns `false`.
- The overall operation steps are minimized.