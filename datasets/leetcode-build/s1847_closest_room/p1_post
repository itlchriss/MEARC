The returned array `answer` is not null.
The length of `answer` is equal to `k`.
Each element in `answer` is an integer.
The values in `answer` are the room numbers that satisfy the conditions specified in the requirements.
If there is no room that satisfies the conditions for a query, the corresponding element in `answer` is -1.
