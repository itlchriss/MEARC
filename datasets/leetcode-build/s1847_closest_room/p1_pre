The input arrays `rooms` and `queries` are not null.
The length of `rooms` is equal to `n`.
The length of `queries` is equal to `k`.
Each element in `rooms` is an array of length 2.
Each element in `queries` is an array of length 2.
The values of `roomId_i` in `rooms` are unique.
The values of `roomId_j` in `queries` are within the range of 1 to 10^7.
The values of `size_i` in `rooms` and `minSize_j` in `queries` are within the range of 1 to 10^7.
