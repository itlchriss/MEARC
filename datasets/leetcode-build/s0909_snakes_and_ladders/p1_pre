The input matrix `board` is not null.
The input matrix `board` has a length of `n`.
Each row of the input matrix `board` has a length of `n`.
The input matrix `board` contains integers in the range `[1, n^2]` and `-1`.
The input matrix `board` does not contain any ladders or snakes on squares `1` and `n^2`.
