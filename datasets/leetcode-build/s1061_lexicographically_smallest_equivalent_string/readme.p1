Preconditions:
The lengths of `s1`, `s2`, and `baseStr` are greater than or equal to 1 and less than or equal to The lengths of `s1` and `s2` are equal.
`s1`, `s2`, and `baseStr` consist of lowercase English letters.

Postconditions:
The returned string is the lexicographically smallest equivalent string of `baseStr`.
The returned string has the same length as `baseStr`.
The characters in the returned string are equivalent according to the equivalency information from `s1` and `s2`.
The characters in the returned string are sorted in lexicographical order within each equivalence group.