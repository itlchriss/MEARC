Preconditions:
- The input array `nums` is not null.
- The input array `nums` has at least one element.
- The input array `nums` contains only binary values (0 or 1).
- The input integer `k` is non-negative.
- The input integer `k` is less than or equal to the length of the input array `nums`.

Postconditions:
- The method returns an integer representing the maximum number of consecutive 1's in the array after flipping at most `k` 0's.
- The input array `nums` remains unchanged after the method is executed.