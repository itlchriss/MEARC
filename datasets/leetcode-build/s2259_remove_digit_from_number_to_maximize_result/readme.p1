Preconditions:
- The input string `number` must be a positive integer.
- The character `digit` must be a digit from `'1'` to `'9'`.
- The character `digit` must occur at least once in the string `number`.

Postconditions:
- The resulting string after removing exactly one occurrence of `digit` from `number` must be a valid positive integer.
- The resulting string must have the maximum possible value in decimal form.