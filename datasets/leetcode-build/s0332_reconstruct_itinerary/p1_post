The output is a list of strings.
The output list contains all the airports in the itinerary in order.
The output list starts with "JFK".
The output list is the smallest lexical order when read as a single string.
Each ticket in `tickets` is used exactly once in the itinerary.
