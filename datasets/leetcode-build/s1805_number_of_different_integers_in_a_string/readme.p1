Preconditions:
- The input string `word` is not null.
- The length of `word` is between 1 and - `word` consists of digits and lowercase English letters.

Postconditions:
- The method returns an integer representing the number of different integers in the string `word`.
- The non-digit characters in `word` are replaced with spaces.
- The integers in `word` are separated by at least one space.
- Leading zeros in the integers are ignored when comparing their decimal values.