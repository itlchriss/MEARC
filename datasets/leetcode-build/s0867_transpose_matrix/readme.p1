Preconditions:
- The input matrix is a 2D integer array.
- The input matrix is not null.
- The input matrix has at least one row and one column.
- The input matrix has a maximum size of 1000 rows and 1000 columns.
- The values in the input matrix are within the range of -10^9 to 10^9.

Postconditions:
- The method returns a 2D integer array representing the transpose of the input matrix.
- The returned matrix has the same number of columns as the input matrix's number of rows.
- The returned matrix has the same number of rows as the input matrix's number of columns.
- The values in the returned matrix are obtained by flipping the row and column indices of the input matrix.