Preconditions:
- The input matrix `values` is not null.
- The input matrix `values` has at least one row and one column.
- The input matrix `values` has `m` rows and `n` columns.
- Each row of the input matrix `values` is sorted in non-increasing order.

Postconditions:
- The method returns an integer representing the maximum amount of money that can be spent buying all `m * n` products.