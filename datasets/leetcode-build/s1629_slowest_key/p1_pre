The length of `releaseTimes` and `keysPressed` arrays should be the same.
The length of `releaseTimes` and `keysPressed` arrays should be at least - The elements in `releaseTimes` array should be in ascending order.
The elements in `keysPressed` array should be lowercase English letters.
