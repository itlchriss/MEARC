Preconditions:
- The input strings `s` and `p` are not null.
- The length of `s` is greater than or equal to the length of `p`.

Postconditions:
- The method returns a list of integers representing the start indices of `p`'s anagrams in `s`.
- The order of the indices in the returned list can be in any order.