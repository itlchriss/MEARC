The input strings `num1` and `num2` must be valid complex numbers.
The real part of `num1` and `num2` must be integers in the range [-100, 100].
The imaginary part of `num1` and `num2` must be integers in the range [-100, 100].
