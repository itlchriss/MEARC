Preconditions:
- The input array `nums` is not null.
- The length of `nums` is at least 2.
- The elements in `nums` are non-negative integers.

Postconditions:
- The returned value is an integer.
- The returned value is the length of the `left` subarray after partitioning.
- The `left` subarray is non-empty.
- Every element in the `left` subarray is less than or equal to every element in the `right` subarray.
- The `left` subarray has the smallest possible size.