The input strings `s` and `t` are non-null.
The input strings `s` and `t` represent non-negative rational numbers.
The input strings `s` and `t` follow the specified format for representing rational numbers.
The `<IntegerPart>` of `s` and `t` does not have leading zeros (except for the zero itself).
The length of the `<IntegerPart>` of `s` and `t` is between 1 and 4 (inclusive).
The length of the `<NonRepeatingPart>` of `s` and `t` is between 0 and 4 (inclusive).
The length of the `<RepeatingPart>` of `s` and `t` is between 1 and 4 (inclusive).
