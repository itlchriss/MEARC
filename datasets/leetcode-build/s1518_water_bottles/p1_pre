The method should take two integer parameters `numBottles` and `numExchange`.
The value of `numBottles` should be between 1 and 100 (inclusive).
The value of `numExchange` should be between 2 and 100 (inclusive).
