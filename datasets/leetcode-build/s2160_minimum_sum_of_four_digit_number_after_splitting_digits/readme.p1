Preconditions:
- The input `num` must be a positive integer.
- The input `num` must consist of exactly four digits.

Postconditions:
- The output must be an integer representing the minimum possible sum of `new1` and `new2`.
- The digits in `num` must be split into two new integers `new1` and `new2`.
- Leading zeros are allowed in `new1` and `new2`.
- All the digits found in `num` must be used in the split.
- The sum of `new1` and `new2` must be minimized.