Preconditions:
- The input `n` is a positive integer.
- The input `n` is less than or equal to 10^4.

Postconditions:
- The method returns an integer representing the number of "good" integers in the range [1, n].
- The returned integer is non-negative.
- The returned integer is less than or equal to n.