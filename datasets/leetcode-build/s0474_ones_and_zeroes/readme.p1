Preconditions:
- The input array `strs` is not null.
- The integers `m` and `n` are non-negative.

Postconditions:
- The returned value is an integer.
- The returned value is the size of the largest subset of `strs` such that there are at most `m` 0's and `n` 1's in the subset.