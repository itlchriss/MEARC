The input array `intervals` is not null.
The input array `queries` is not null.
The length of `intervals` is greater than or equal to 1.
The length of `queries` is greater than or equal to 1.
Each interval in `intervals` is a 2-element array.
The first element of each interval in `intervals` is less than or equal to the second element.
The elements in `intervals` are within the range of 1 to 10^7.
The elements in `queries` are within the range of 1 to 10^7.
