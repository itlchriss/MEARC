Preconditions:
- The input integer `n` is non-negative.
- The input integer `n` is less than or equal to 10^9.

Postconditions:
- The output integer is less than or equal to the input integer `n`.
- The output integer has monotone increasing digits, meaning that each pair of adjacent digits satisfy `x <= y`.