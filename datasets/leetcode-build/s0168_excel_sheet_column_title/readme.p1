Preconditions:
- The input `columnNumber` must be a positive integer greater than or equal to 1.
- The input `columnNumber` must be less than or equal to 2^31 - 1.

Postconditions:
- The method should return a string representing the corresponding column title for the given `columnNumber`.