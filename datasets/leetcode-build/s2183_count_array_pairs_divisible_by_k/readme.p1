Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is greater than or equal to 2.
- The value of `k` is greater than or equal to 1.

Postconditions:
- The method returns an integer representing the number of pairs `(i, j)` such that `0 <= i < j <= n - 1` and `nums[i] * nums[j]` is divisible by `k`.
- The returned value is non-negative.