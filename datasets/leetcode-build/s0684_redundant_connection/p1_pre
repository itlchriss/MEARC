The input `edges` is a non-empty array.
The length of `edges` is equal to `n`.
Each element in `edges` is an array of length - The values in each element of `edges` are integers.
The integers in each element of `edges` are between 1 and `n`.
The integers in each element of `edges` are different.
The graph represented by `edges` is connected.
