Preconditions:
- The input integer `n` is a positive integer.
- `n` is greater than or equal to 2.

Postconditions:
- The output is a list of two integers `[A, B]`.
- `A` and `B` are positive integers.
- `A` and `B` do not contain any `0` in their decimal representation.
- The sum of `A` and `B` is equal to `n`.