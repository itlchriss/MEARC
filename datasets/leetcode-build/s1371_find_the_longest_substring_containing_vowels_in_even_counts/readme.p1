Preconditions:
- The input string `s` is not null.
- The input string `s` contains only lowercase English letters.

Postconditions:
- The output is an integer representing the size of the longest substring containing each vowel an even number of times.