Preconditions:
- The input string `pressedKeys` is not null.
- The input string `pressedKeys` contains only digits from '2' to '9'.
- The length of the input string `pressedKeys` is between 1 and 10^5.

Postconditions:
- The method returns an integer representing the total number of possible text messages Alice could have sent.
- The returned value is modulo 10^9 + 7.