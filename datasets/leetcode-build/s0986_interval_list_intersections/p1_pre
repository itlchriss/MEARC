The input arrays `firstList` and `secondList` are not null.
The input arrays `firstList` and `secondList` are not empty.
The intervals in `firstList` and `secondList` are sorted in ascending order.
The intervals in `firstList` and `secondList` are pairwise disjoint.
