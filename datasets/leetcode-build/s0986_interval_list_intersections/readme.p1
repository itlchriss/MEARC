Preconditions:
- The input arrays `firstList` and `secondList` are not null.
- The input arrays `firstList` and `secondList` are not empty.
- The intervals in `firstList` and `secondList` are sorted in ascending order.
- The intervals in `firstList` and `secondList` are pairwise disjoint.

Postconditions:
- The output is a 2D array representing the intersection of the intervals in `firstList` and `secondList`.
- The intervals in the output array are sorted in ascending order.
- The intervals in the output array are pairwise disjoint.