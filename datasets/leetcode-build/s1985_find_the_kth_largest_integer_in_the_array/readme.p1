Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is greater than or equal to `k`.
- Each string in `nums` represents an integer without leading zeros.

Postconditions:
- The output string represents the `k`th largest integer in `nums`.
- Duplicate numbers are counted distinctly.