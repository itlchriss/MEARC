1. The length of `sub` is at least 1 and at most the length of `s`.
2. The length of `mappings` is at most 1000.
3. Each element in `mappings` has a length of 2.
4. The characters in `sub` cannot be replaced more than once.
5. The characters in `s`, `sub`, `mappings`, `old_i`, and `new_i` are all valid English letters or digits.
