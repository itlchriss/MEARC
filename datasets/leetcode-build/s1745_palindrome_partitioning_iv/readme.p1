Preconditions:
- The input string `s` is not null.
- The length of the input string `s` is at least 3 and at most 2000.
- The input string `s` consists only of lowercase English letters.

Postconditions:
- The method returns `true` if it is possible to split the string `s` into three non-empty palindromic substrings.
- The method returns `false` if it is not possible to split the string `s` into three non-empty palindromic substrings.