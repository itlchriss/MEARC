Preconditions:
- The input `n` is a positive integer.
- The input `target` is a positive integer.
- The input `n` is always possible to make beautiful.

Postconditions:
- The output `x` is a non-negative integer.
- The sum of the digits of `n + x` is less than or equal to `target`.
- The output `x` is the minimum non-negative integer such that `n + x` is beautiful.