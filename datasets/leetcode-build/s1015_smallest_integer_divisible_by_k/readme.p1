Preconditions:
- The input `k` is a positive integer.
- `k` is less than or equal to 10^5.

Postconditions:
- The method returns an integer representing the length of the smallest positive integer `n` that is divisible by `k` and only contains the digit `1`.
- If there is no such `n`, the method returns -1.