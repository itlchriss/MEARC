1. The input strings `num1` and `num2` must not be null.
2. The input strings `num1` and `num2` must consist of only digits.
3. The input strings `num1` and `num2` must not have any leading zeros except for the zero itself.
