Preconditions:
- The input integers `left` and `right` must be within the range of 1 to 10^6.
- The value of `left` must be less than or equal to the value of `right`.

Postconditions:
- The method should return an integer representing the count of numbers in the inclusive range `[left, right]` that have a prime number of set bits in their binary representation.