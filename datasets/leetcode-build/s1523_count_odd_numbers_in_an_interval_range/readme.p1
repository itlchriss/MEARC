Preconditions:
- The method takes two non-negative integers `low` and `high` as input.

Postconditions:
- The method returns an integer representing the count of odd numbers between `low` and `high` (inclusive).