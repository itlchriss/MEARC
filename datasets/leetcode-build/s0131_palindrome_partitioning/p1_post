The method returns a list of lists of strings, representing all possible palindrome partitions of `s`.
Each sublist in the returned list represents a valid palindrome partition of `s`.
Each string in the sublists is a palindrome.
The order of the sublists in the returned list does not matter.
The order of the strings within each sublist does not matter.
