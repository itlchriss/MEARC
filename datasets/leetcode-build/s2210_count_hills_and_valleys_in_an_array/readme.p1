Preconditions:
- The input array `nums` is not null.
- The length of `nums` is at least 3.
- Each element in `nums` is an integer between 1 and 100 (inclusive).

Postconditions:
- The method returns an integer representing the number of hills and valleys in `nums`.