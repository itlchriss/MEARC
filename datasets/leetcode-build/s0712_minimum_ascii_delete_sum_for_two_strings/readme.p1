Preconditions:
1. The input strings `s1` and `s2` are not null.
2. The lengths of `s1` and `s2` are between 1 and 1000 (inclusive).
3. `s1` and `s2` consist only of lowercase English letters.

Postconditions:
1. The method returns an integer representing the lowest ASCII sum of deleted characters to make `s1` and `s2` equal.
2. The method does not modify the input strings `s1` and `s2`.