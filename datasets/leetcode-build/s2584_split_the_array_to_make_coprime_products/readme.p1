Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is greater than or equal to 2.

Postconditions:
- The returned index `i` is the smallest index at which the array can be split validly.
- The returned index `i` is -1 if there is no valid split.