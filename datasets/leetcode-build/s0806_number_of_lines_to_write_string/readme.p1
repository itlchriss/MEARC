Preconditions:
- The `widths` array must have a length of 26.
- Each element in the `widths` array must be between 2 and 10 (inclusive).
- The `s` string must have a length between 1 and 1000 (inclusive).
- The `s` string must only contain lowercase English letters.

Postconditions:
- The returned array `result` must have a length of 2.
- The first element of the `result` array (`result[0]`) represents the total number of lines used to write the string `s`.
- The second element of the `result` array (`result[1]`) represents the width of the last line in pixels.