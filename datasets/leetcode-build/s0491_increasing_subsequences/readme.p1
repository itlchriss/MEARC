Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is at least 2.
- Each element in the input array `nums` is an integer.
- The input array `nums` may contain duplicates.

Postconditions:
- The output is a list of lists of integers.
- Each list in the output represents an increasing subsequence of the input array `nums`.
- The output contains all possible increasing subsequences of the input array `nums` with at least two elements.
- The order of the lists in the output does not matter.