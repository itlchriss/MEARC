1. The input string `s` is not null.
2. The input string `s` is a valid expression.
3. The input string `s` consists of integers and operators `('+', '-', '*', '/')` separated by some number of spaces.
4. All the integers in the expression are non-negative integers in the range `[0, 2^31 - 1]`.
