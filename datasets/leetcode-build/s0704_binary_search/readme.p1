Preconditions:
- The input array `nums` is not null.
- The input array `nums` is sorted in ascending order.
- The input array `nums` contains unique integers.
- The input array `nums` has a length greater than or equal to 1.
- The input integer `target` is within the range of integers in `nums`.

Postconditions:
- If the `target` exists in `nums`, the method returns the index of the `target` in `nums`.
- If the `target` does not exist in `nums`, the method returns -1.