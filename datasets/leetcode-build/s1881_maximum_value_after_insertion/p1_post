The output string must represent the maximum value of `n` after inserting `x`.
The output string must have the same sign as the input string `n`.
The output string must have a length greater than or equal to the length of the input string `n`.
The output string must have the digit `x` inserted at any valid position in the decimal representation of `n`, except to the left of the negative sign if `n` is negative.
