Preconditions:
- The input `n` is a positive integer greater than 1.

Postconditions:
- The method returns `true` if Alice wins the game, and `false` otherwise.
- The method modifies the value of `n` on the chalkboard according to the rules of the game.
- The method assumes that both players play optimally.