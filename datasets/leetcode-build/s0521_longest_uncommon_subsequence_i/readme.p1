Preconditions:
- The input strings `a` and `b` are not null.
- The lengths of `a` and `b` are between 1 and 100 (inclusive).
- The strings `a` and `b` consist of lowercase English letters.

Postconditions:
- The method returns an integer representing the length of the longest uncommon subsequence between `a` and `b`.
- If there is no uncommon subsequence, the method returns -1.