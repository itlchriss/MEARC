The input `segments` is a 2D integer array.
Each element `segments[i]` is an array of length 3.
The start value `start_i` of each segment is less than the end value `end_i`.
The color value `color_i` of each segment is distinct.
