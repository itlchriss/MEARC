The returned list `result` is not null.
The size of the returned list `result` is valid.
Each element in the returned list `result` is a valid grid coordinate [r, c].
Each grid coordinate [r, c] in the returned list `result` represents a cell where rain water can flow to both the Pacific and Atlantic oceans.
