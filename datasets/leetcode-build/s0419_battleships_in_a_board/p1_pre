1. The input `board` is a non-null `m x n` matrix.
2. The elements of `board` are either `'.'` or `'X'`.
3. The dimensions `m` and `n` of `board` are both greater than or equal to 1 and less than or equal to 200.
