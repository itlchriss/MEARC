Preconditions:
- The input integer `n` must be greater than or equal to 1.
- The input integer `n` must be less than or equal to 10^4.

Postconditions:
- The method should return `true` if `n` has exactly three positive divisors.
- The method should return `false` if `n` does not have exactly three positive divisors.