Preconditions:
- The input strings `num1` and `num2` must be valid numeric strings.
- The integers `min_sum` and `max_sum` must be within the valid range of 1 to 400.
- The value of `num1` must be less than or equal to the value of `num2`.

Postconditions:
- The method should return the number of integers that satisfy the conditions.
- The returned value should be modulo `10^9 + 7`.