Preconditions:
- The input array `strs` is not null.
- The length of the input array `strs` is greater than or equal to 1.
- Each string in the input array `strs` is not null.
- Each string in the input array `strs` consists of only lower-case English letters.

Postconditions:
- The returned string is the longest common prefix among the input strings.
- If there is no common prefix, the returned string is an empty string `""`.