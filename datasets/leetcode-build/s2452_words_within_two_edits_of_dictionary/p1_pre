The length of each word in `queries` and `dictionary` is the same.
The length of `queries` and `dictionary` is between 1 and - Each word in `queries` and `dictionary` is composed of lowercase English letters.
