The method returns a list of words from `queries` that match with some word from `dictionary` after a maximum of two edits.
The order of the words in the returned list is the same as their order in `queries`.
