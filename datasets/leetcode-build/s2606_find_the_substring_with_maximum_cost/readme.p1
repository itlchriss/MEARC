Preconditions:
- The length of string `s` is at least 1 and at most 10^- String `s` consists of lowercase English letters.
- The length of string `chars` is at least 1 and at most - String `chars` consists of distinct lowercase English letters.
- The length of array `vals` is the same as the length of string `chars`.
- Each element in array `vals` is an integer between -1000 and 
Postconditions:
- The method returns an integer representing the maximum cost among all substrings of string `s`.