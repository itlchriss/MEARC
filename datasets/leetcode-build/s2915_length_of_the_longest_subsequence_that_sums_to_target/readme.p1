Preconditions:
- The input list `nums` is not null.
- The input list `nums` is not empty.
- The input list `nums` contains only integers.
- The input integer `target` is not null.

Postconditions:
- The returned value is an integer.
- The returned value is the length of the longest subsequence of `nums` that sums up to `target`.
- If no such subsequence exists, the returned value is -1.