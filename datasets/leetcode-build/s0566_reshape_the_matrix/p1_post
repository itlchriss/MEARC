1. If the reshape operation is possible and legal, the method should return a new reshaped matrix with dimensions `r x c`.
2. The elements in the reshaped matrix should be filled with all the elements of the original matrix `mat` in the same row-traversing order as they were.
3. If the reshape operation is not possible or legal, the method should return the original matrix `mat`.
