The input strings `a` and `b` must not be null.
The lengths of `a` and `b` must be between 1 and 10^4 (inclusive).
The characters in `a` and `b` must be either '0' or '1'.
Each string should not contain leading zeros except for the zero itself.
