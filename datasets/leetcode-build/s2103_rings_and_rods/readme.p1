Preconditions:
- The input string `rings` must have a length of `2n`.
- The value of `n` must be between 1 and 100 (inclusive).
- The characters at even indices in `rings` (0, 2, 4, ...) must be either 'R', 'G', or 'B'.
- The characters at odd indices in `rings` (1, 3, 5, ...) must be digits from '0' to '9'.

Postconditions:
- The method should return an integer representing the number of rods that have all three colors of rings on them.