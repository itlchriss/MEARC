The input integers `rows` and `cols` must be greater than or equal to 1.
The input integers `rCenter` and `cCenter` must be within the range of the matrix dimensions (`rows` and `cols` respectively).
