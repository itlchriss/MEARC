Preconditions:
- The input array `nums` is not null.
- The input array `nums` contains only positive integers.
- The input integer `k` is positive.
- The length of the input array `nums` is between 1 and 20 (inclusive).
- The values in the input array `nums` are between 1 and 1000 (inclusive).
- The value of `k` is between 1 and 1000 (inclusive).

Postconditions:
- The method returns an integer representing the number of non-empty beautiful subsets of the array `nums`.