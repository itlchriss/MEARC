The input string `licensePlate` is not null.
The input array `words` is not null.
The length of `licensePlate` is between 1 and 7 (inclusive).
The length of each word in `words` is between 1 and 15 (inclusive).
Each word in `words` consists of lowercase English letters.
