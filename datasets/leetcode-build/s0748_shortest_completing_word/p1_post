The method returns a string, which is the shortest completing word in `words`.
The returned word contains all the letters in `licensePlate`.
The returned word is case insensitive.
If a letter appears more than once in `licensePlate`, it must appear in the returned word the same number of times or more.
If there are multiple shortest completing words, the method returns the first one that occurs in `words`.
