Preconditions:
- The input array `words` is not null.
- The length of `words` is at least 1 and at most - Each string in `words` is not null and consists of at least 1 and at most 50 English lowercase letters.

Postconditions:
- The method returns an integer representing the minimum possible length of `str[n - 1]`.
- The value returned is greater than or equal to 0.