Preconditions:
- The lengths of the `scores` and `ages` arrays must be the same.
- The length of the `scores` and `ages` arrays must be between 1 and - Each element in the `scores` array must be between 1 and 10^- Each element in the `ages` array must be between 1 and 
Postconditions:
- The method should return an integer representing the highest overall score of all possible basketball teams.