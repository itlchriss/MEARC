Preconditions:
- The input `text` is a non-empty string.
- The length of `text` is between 1 and 2000.
- `text` consists only of lowercase English letters.

Postconditions:
- The method returns an integer representing the number of distinct non-empty substrings of `text` that can be written as the concatenation of some string with itself.
- The returned value is greater than or equal to 0.