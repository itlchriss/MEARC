Preconditions:
- The input array `nums` is not null.
- The length of `nums` is at least 3.

Postconditions:
- The method returns an integer representing the sum of beauty of all `nums[i]` where `1 <= i <= nums.length - 2`.
- The returned sum is non-negative.