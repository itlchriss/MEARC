Preconditions:
- The input arrays `nums1` and `nums2` must not be empty.
- The lengths of `nums1` and `nums2` must be the same.
- The elements in `nums1` and `nums2` must be integers.
- The lengths of `nums1` and `nums2` must be between 1 and 500 (inclusive).
- The elements in `nums1` and `nums2` must be between -1000 and 1000 (inclusive).

Postconditions:
- The method returns an integer value.
- The returned value is the maximum dot product between non-empty subsequences of `nums1` and `nums2` with the same length.