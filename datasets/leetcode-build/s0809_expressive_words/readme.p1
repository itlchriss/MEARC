Preconditions:
- The input string `s` is not null.
- The input array `words` is not null.
- The length of `s` is between 1 and 100 (inclusive).
- The length of each word in `words` is between 1 and 100 (inclusive).
- `s` and each word in `words` consist of lowercase letters.

Postconditions:
- The method returns an integer representing the number of query strings that are stretchy.

Note: The given requirements already provide the necessary preconditions and postconditions for the method.