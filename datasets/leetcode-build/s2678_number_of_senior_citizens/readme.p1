Preconditions:
- The input array `details` is not null.
- The length of each string in `details` is - Each string in `details` consists of digits from '0' to '9'.
- The character at index 10 in each string in `details` is either 'M' or 'F' or 'O'.
- The phone numbers and seat numbers of the passengers in `details` are distinct.

Postconditions:
- The method returns an integer representing the number of passengers who are strictly more than 60 years old.