The method takes two integer parameters `queryRow` and `queryGlass` which represent the row and glass indices respectively.
The method takes an integer parameter `poured` which represents the number of cups of champagne poured.
The value of `poured` is non-negative.
The value of `queryRow` is between 0 and 99 (inclusive).
The value of `queryGlass` is between 0 and `queryRow` (inclusive).
