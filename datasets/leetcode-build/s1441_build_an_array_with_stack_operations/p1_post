The returned list is not null.
The returned list contains the operations needed to build the `target` array.
If the `target` array is already built, the returned list is empty.
The length of the returned list is equal to the number of operations needed to build the `target` array.
