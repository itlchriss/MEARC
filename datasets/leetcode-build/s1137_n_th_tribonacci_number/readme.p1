Preconditions:
- The input `n` must be a non-negative integer.
- The input `n` must be less than or equal to 37.

Postconditions:
- The method should return an integer value.
- The returned value should be the `n`-th Tribonacci number.
- The returned value should be less than or equal to 2^31 - 1.