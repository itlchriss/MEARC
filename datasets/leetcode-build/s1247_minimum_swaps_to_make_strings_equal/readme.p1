Preconditions:
The lengths of `s1` and `s2` are equal.
`s1` and `s2` only contain the letters "x" and "y".

Postconditions:
The minimum number of swaps required to make `s1` and `s2` equal is returned.
If it is impossible to make `s1` and `s2` equal, -1 is returned.