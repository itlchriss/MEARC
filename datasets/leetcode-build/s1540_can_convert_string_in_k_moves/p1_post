The method returns `true` if it is possible to convert string `s` into string `t` in no more than `k` moves.
The method returns `false` if it is not possible to convert string `s` into string `t` in no more than `k` moves.
