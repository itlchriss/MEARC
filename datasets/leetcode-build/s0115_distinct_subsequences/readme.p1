Preconditions:
- The input strings `s` and `t` are not null.
- The lengths of `s` and `t` are between 1 and 1000, inclusive.
- `s` and `t` consist of English letters.

Postconditions:
- The method returns an integer representing the number of distinct subsequences of `s` that equal `t`.