Preconditions:
- The input string `binary` is not null.
- The length of `binary` is at least 1 and at most 10^5.
- `binary` consists of only '0's and '1's.

Postconditions:
- The method returns an integer representing the number of unique good subsequences of `binary`.
- The returned value is modulo 10^9 + 7.