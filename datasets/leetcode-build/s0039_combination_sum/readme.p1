Preconditions:
- The input array `candidates` must not be null.
- The input array `candidates` must not be empty.
- The input array `candidates` must contain distinct integers.
- The input integer `target` must be greater than or equal to 1.
- The input integer `target` must be less than or equal to 500.

Postconditions:
- The method returns a list of lists of integers, representing unique combinations of `candidates` that sum up to `target`.
- The returned list must not be null.
- The returned list must not be empty.
- Each combination in the returned list must be unique.
- The frequency of at least one chosen number in each combination must be different.
- The sum of the numbers in each combination must be equal to `target`.
- The number of combinations in the returned list must be less than 150.