The method returns an array `answer` of length `n`.
Each element in the array `answer` is a non-negative integer between 0 and 2^maximumBit - 1 (inclusive).
The first element in the array `answer` is the answer to the first query.
The last element in the array `answer` is the answer to the last query.
After each query, the last element in the current array `nums` is removed.
