Preconditions:
- The input string `s` is not null.
- The length of the input string `s` is between 1 and 1000.
- The input string `s` consists of lowercase English letters.

Postconditions:
- The method returns `true` if all characters in the input string `s` have the same number of occurrences.
- The method returns `false` if there are characters in the input string `s` that have different numbers of occurrences.