The method should return an integer value representing the maximum number of consecutive floors without a special floor.
The returned value should be non-negative.
If all the floors between `bottom` and `top` (inclusive) are special floors, the method should return 0.
