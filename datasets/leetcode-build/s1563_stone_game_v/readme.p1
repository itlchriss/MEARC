Preconditions:
- The input array `stoneValue` is not null.
- The length of `stoneValue` is greater than or equal to - Each element in `stoneValue` is an integer.
- Each element in `stoneValue` is greater than or equal to - Each element in `stoneValue` is less than or equal to 10^
Postconditions:
- The return value is an integer.
- The return value is the maximum score that Alice can obtain.