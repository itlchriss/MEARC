Preconditions:
- The `haystack` and `needle` strings are not null.

Postconditions:
- If `needle` is an empty string, return 0.
- If `needle` is not part of `haystack`, return -1.
- If `needle` is part of `haystack`, return the index of the first occurrence of `needle` in `haystack`.