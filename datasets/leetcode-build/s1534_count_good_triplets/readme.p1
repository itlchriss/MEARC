Preconditions:
- The input array `arr` must have a length greater than or equal to 3.
- The integers `a`, `b`, and `c` must be non-negative.
- The integers `a`, `b`, and `c` must be less than or equal to 1000.

Postconditions:
- The method should return an integer representing the number of good triplets.
- The returned integer should be non-negative.