Preconditions:
- The lengths of `rowSum` and `colSum` arrays are equal.
- The lengths of `rowSum` and `colSum` arrays are not greater than 500.
- The elements of `rowSum` and `colSum` arrays are non-negative integers.
- The sum of all elements in `rowSum` array is equal to the sum of all elements in `colSum` array.

Postconditions:
- The returned matrix is a 2D array.
- The dimensions of the returned matrix are `rowSum.length x colSum.length`.
- The sum of each row in the returned matrix is equal to the corresponding element in `rowSum` array.
- The sum of each column in the returned matrix is equal to the corresponding element in `colSum` array.
- All elements in the returned matrix are non-negative integers.