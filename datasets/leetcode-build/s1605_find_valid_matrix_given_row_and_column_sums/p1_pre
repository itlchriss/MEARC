The lengths of `rowSum` and `colSum` arrays are equal.
The lengths of `rowSum` and `colSum` arrays are not greater than 500.
The elements of `rowSum` and `colSum` arrays are non-negative integers.
The sum of all elements in `rowSum` array is equal to the sum of all elements in `colSum` array.
