Preconditions:
- The `skills` array is not null.
- The `people` list is not null.
- The `skills` array and `people` list are not empty.
- The length of `req_skills` is greater than or equal to 1.
- The length of each string in `req_skills` is greater than or equal to 1.
- Each string in `req_skills` consists of lowercase English letters.
- The length of `people` is greater than or equal to 1.
- The length of each list in `people` is greater than or equal to 0.
- The length of each string in each list in `people` is greater than or equal to 1.
- Each string in each list in `people` consists of lowercase English letters.
- Each skill in each list in `people` is a skill in `req_skills`.

Postconditions:
- The returned array is not null.
- The returned array is not empty.
- The length of the returned array is less than or equal to the length of `people`.
- Each element in the returned array is a valid index of a person in the `people` list.
- For every required skill in `req_skills`, there is at least one person in the returned array who has that skill.