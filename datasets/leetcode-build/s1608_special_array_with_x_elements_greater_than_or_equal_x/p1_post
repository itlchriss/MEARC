If there exists a number `x` such that there are exactly `x` numbers in `nums` that are greater than or equal to `x`, return `x`.
If there is no such number `x`, return -- The value of `x` returned is unique.
