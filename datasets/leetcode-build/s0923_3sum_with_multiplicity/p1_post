The method should return an integer value.
The returned value should be the number of tuples `i, j, k` such that `i < j < k` and `arr[i] + arr[j] + arr[k] == target`.
The returned value should be modulo `10^9 + 7`.
