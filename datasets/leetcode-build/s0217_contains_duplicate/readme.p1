Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is greater than or equal to 1.
- The elements in the input array `nums` are integers.

Postconditions:
- The method returns `true` if any value appears at least twice in the input array `nums`.
- The method returns `false` if every element in the input array `nums` is distinct.