Preconditions:
- The input strings `s1` and `s2` are not null.
- The lengths of `s1` and `s2` are between 1 and 10^4 (inclusive).
- `s1` and `s2` consist of lowercase English letters.

Postconditions:
- The method returns a boolean value indicating whether `s2` contains a permutation of `s1`.
- If `s2` contains a permutation of `s1`, the method returns `true`.
- If `s2` does not contain a permutation of `s1`, the method returns `false`.