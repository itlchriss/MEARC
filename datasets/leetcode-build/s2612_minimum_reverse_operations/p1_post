The returned array `ans` has a length of `n`.
The values in the `ans` array are between -1 and the maximum number of reverse operations needed to bring the 1 to any position in `arr`, inclusive.
The value at position `i` in the `ans` array is the minimum number of reverse operations needed to bring the 1 to position `i` in `arr`, or -1 if it is impossible.
