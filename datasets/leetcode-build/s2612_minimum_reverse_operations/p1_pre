The input integer `n` is greater than or equal to - The input integer `p` is between 0 and `n - 1`, inclusive.
The input integer array `banned` is not null.
The values in the `banned` array are between 0 and `n - 1`, inclusive.
The input integer `k` is between 1 and `n`, inclusive.
The value at position `p` in the `arr` array is - The value at position `banned[i]` in the `arr` array is 0 for all `i` in the `banned` array.
