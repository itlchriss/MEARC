The input array `words` must have a length between 2 and - Each element in the `words` array must be a string containing only uppercase English letters.
The length of each element in the `words` array and the `result` string must be between 1 and - The number of different characters used in the `words` array and the `result` string must be at most
