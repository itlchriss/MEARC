Preconditions:
- The input strings `left` and `right` must represent positive integers.
- The length of `left` and `right` must be between 1 and 18 (inclusive).
- `left` and `right` cannot have leading zeros.
- `left` must be less than or equal to `right`.

Postconditions:
- The method returns an integer representing the number of super-palindromes in the inclusive range `[left, right]`.