The returned array `answer` is not null.
The length of `answer` is equal to `m`.
Each element in `answer` is a non-negative integer.
The maximum value of each element in `answer` is 10^9.
After each query, the array `nums` is reset to its original state.
