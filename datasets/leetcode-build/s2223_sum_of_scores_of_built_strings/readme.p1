Preconditions:
- The input string `s` is not null.
- The length of `s` is between 1 and 10^5.

Postconditions:
- The method returns a long value representing the sum of the scores of every `s_i`.
- The sum of the scores is calculated correctly according to the given rules.
- The method terminates and returns the correct result within a reasonable amount of time.