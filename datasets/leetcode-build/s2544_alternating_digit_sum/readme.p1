Preconditions:
1. The input integer `n` must be a positive integer.
2. The input integer `n` must be less than or equal to 10^9.

Postconditions:
1. The method should return an integer value.
2. The returned integer value should be the sum of all digits of `n` with their corresponding sign.