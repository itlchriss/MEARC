The returned array `answer` is not null.
The length of `answer` is equal to the length of `queries`.
Each element in `answer` is the XOR of the elements in `arr` from the corresponding range specified in `queries`.
