Preconditions:
- The input string `s` is not null.
- The length of the input string `s` is between 1 and 300.
- The input string `s` contains only lowercase English letters.

Postconditions:
- The method returns an integer value representing the length of the longest substring between two equal characters, excluding the two characters.
- If there is no such substring, the method returns -1.