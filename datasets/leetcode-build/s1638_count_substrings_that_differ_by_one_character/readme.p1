Preconditions:
- The input strings `s` and `t` are not null.
- The lengths of `s` and `t` are between 1 and 100, inclusive.
- `s` and `t` consist of lowercase English letters only.

Postconditions:
- The method returns an integer representing the number of substrings in `s` that differ from some substring in `t` by exactly one character.