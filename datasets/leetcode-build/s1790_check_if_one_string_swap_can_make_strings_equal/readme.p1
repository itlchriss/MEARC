Preconditions:
- The lengths of `s1` and `s2` are equal.
- `s1` and `s2` consist of only lowercase English letters.

Postconditions:
- If it is possible to make both strings equal by performing at most one string swap on exactly one of the strings, return `true`.
- If it is not possible to make both strings equal with one string swap, return `false`.