Preconditions:
- The length of both strings `s1` and `s2` must be the same.
- The length of `s1` and `s2` must be greater than or equal to 1.
- Both `s1` and `s2` must consist of lowercase English letters.

Postconditions:
- The method returns `true` if `s2` is a scrambled string of `s1`.
- The method returns `false` if `s2` is not a scrambled string of `s1`.