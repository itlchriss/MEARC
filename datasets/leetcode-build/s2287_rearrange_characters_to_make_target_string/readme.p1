Preconditions:
- The input strings `s` and `target` are not null.
- The length of `s` is greater than or equal to the length of `target`.
- The strings `s` and `target` consist of lowercase English letters.

Postconditions:
- The method returns an integer representing the maximum number of copies of `target` that can be formed by rearranging letters from `s`.