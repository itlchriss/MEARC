The input `rec` is a 2D integer array representing `n` rectangles.
Each rectangle `rec[i]` has a width `width_i` and a height `height_i`.
The width and height of each rectangle are positive integers.
