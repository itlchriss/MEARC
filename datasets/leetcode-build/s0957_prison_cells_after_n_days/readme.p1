Preconditions:
- The input array `cells` must have a length of - Each element in the `cells` array must be either 0 or - The input integer `n` must be between 1 and 10^
Postconditions:
- The method should return an integer array representing the state of the prison after `n` days.
- The returned array should have a length of - Each element in the returned array should be either 0 or 1.