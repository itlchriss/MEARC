1. The method returns `true` if it is possible to reach cell `(fx, fy)` after exactly `t` seconds.
2. The method returns `false` if it is not possible to reach cell `(fx, fy)` after exactly `t` seconds.
