The method should return a list of cells `(x, y)` such that `r1 <= x <= r2` and `c1 <= y <= c2`.
The cells in the list should be represented as strings in the format `"<col><row>"`.
The cells in the list should be sorted in non-decreasing order first by columns and then by rows.
