Preconditions:
- The input string `s` has a length greater than or equal to 2.
- The input integer `k` is greater than or equal to 1 and less than or equal to half the length of `s`.

Postconditions:
- The output is an integer representing the minimum number of letter changes required to partition `s` into `k` substrings such that each substring is a semi-palindrome.