Preconditions:
- The input string `s` is not null.
- The length of the input string `s` is greater than or equal to 1.
- The input string `s` contains only lowercase English letters.

Postconditions:
- The method returns a string that is a non-empty prefix of the input string `s`.
- The returned string is also a suffix of the input string `s`.
- If there is no such prefix that satisfies the above conditions, the method returns an empty string "".