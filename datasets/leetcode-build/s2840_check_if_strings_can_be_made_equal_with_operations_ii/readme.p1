Preconditions:
- The lengths of `s1` and `s2` are equal.
- `s1` and `s2` consist only of lowercase English letters.

Postconditions:
- The method returns `true` if it is possible to make `s1` and `s2` equal using the given operation, and `false` otherwise.