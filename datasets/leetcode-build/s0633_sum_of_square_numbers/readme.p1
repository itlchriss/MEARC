Preconditions:
- The input `c` is a non-negative integer.
- The input `c` is within the range of `0` to `2^31 - 1`.

Postconditions:
- The method returns `true` if there exist two integers `a` and `b` such that `a^2 + b^2 = c`.
- The method returns `false` if there does not exist two integers `a` and `b` such that `a^2 + b^2 = c`.