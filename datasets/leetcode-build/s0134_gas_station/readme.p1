Preconditions:
- The length of the `gas` array is equal to the length of the `cost` array.
- The length of the `gas` and `cost` arrays is greater than or equal to - The values in the `gas` and `cost` arrays are non-negative integers.
- The values in the `gas` and `cost` arrays are less than or equal to 10^
Postconditions:
- If there exists a solution, the method returns the starting gas station's index.
- If there is no solution, the method returns -1.