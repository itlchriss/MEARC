Preconditions:
- The input arrays `rec1` and `rec2` must have a length of 4.
- The values in `rec1` and `rec2` must be integers.
- The values in `rec1` and `rec2` must be within the range of -10^9 to 10^9.
- The values in `rec1` and `rec2` must represent valid rectangles with non-zero area.

Postconditions:
- The method should return a boolean value indicating whether the two rectangles overlap or not.
- If the rectangles overlap, the method should return `true`.
- If the rectangles do not overlap, the method should return `false`.