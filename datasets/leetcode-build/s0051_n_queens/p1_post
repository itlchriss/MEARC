The method returns a list of lists of strings, representing distinct board configurations of the n-queens' placement.
Each inner list represents a row on the chessboard.
Each string in the inner list represents a cell on the chessboard, where `'Q'` indicates a queen and `'.'` indicates an empty space.
The number of inner lists in the returned list is equal to `n`.
The length of each inner list is equal to `n`.
No two queens in any board configuration attack each other.
