Preconditions:
- The input grid must be a 2-D array of size m x n.
- Each cell in the grid must contain either a 1 or a -1.

Postconditions:
- The output array must be of size n.
- Each element in the output array must be the column that the ball falls out of at the bottom after dropping the ball from the corresponding column at the top.
- If a ball gets stuck in the box, the corresponding element in the output array must be -1.