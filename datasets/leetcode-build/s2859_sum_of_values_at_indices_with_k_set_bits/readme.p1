Preconditions:
- The input list `nums` is not null.
- The input list `nums` is not empty.
- The input integer `k` is non-negative.
- The input integer `k` is less than or equal to 10.

Postconditions:
- The return value is an integer.
- The return value is the sum of elements in `nums` whose corresponding indices have exactly `k` set bits in their binary representation.