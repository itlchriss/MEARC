ensures:
- The type_integer_ type_array_ param_nums_ is not null.
- The type_integer_ result is the smallest missing positive type_integer_.
- The type_integer_ result is greater_than to 0.
- The algorithm must run in O(n) time.
- The algorithm must use constant extra space.
requires: []
