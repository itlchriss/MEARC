- If the integer parameter `version1` is less than the integer parameter `version2`, the integer result is equal to -1.
- If the integer parameter `version1` is greater than the integer parameter `version2`, the integer result is equal to 1.
- If the integer parameter `version1` is equal to the integer parameter `version2`, the integer result is equal to 0.
- The integer parameter `version1` and the integer parameter `version2` are valid version numbers.
- The integer parameter `version1` and the integer parameter `version2` consist of one or more revisions joined by a dot.
- Each revision in the integer parameter `version1` and the integer parameter `version2` consists of digits and may contain leading zeros.
- Every revision in the integer parameter `version1` and the integer parameter `version2` contains at least one character.
- Revisions in the integer parameter `version1` and the integer parameter `version2` are 0-indexed from left to right.
- Revisions in the integer parameter `version1` and the integer parameter `version2` are compared in left-to-right order using their integer value ignoring any leading zeros.
- If a version number does not specify a revision at an index, then treat the revision as 0.
- The integer result is less than or equal to 1 and is greater than or equal to -1.