ensures:
- The type_integer_ result is the area of the largest rectangle containing only str_as in the binary type_matrix_.
- The binary type_matrix_ param_matrix_ is not null.
- The binary type_matrix_ param_matrix_ is a expr_a type_matrix_.
- The binary type_matrix_ param_matrix_ must only contain str_as and str_bs.
- The type_integer_ result is greater_than_or_equal to 0.
- The type_integer_ result is less_than_or_equal to 200.
requires: []
