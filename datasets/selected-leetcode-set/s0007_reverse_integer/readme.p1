- The integer parameter `x` is within the range of -2^31 to 2^31 - 1.
- The integer result is the reverse of the digits of the integer parameter `x`.
- If the reverse of the digits of the integer parameter `x` causes the value to go outside the signed 32-bit integer range, the result is equal to 0.