- The integer list result contains the starting indices of substrings in the string parameter `s` that are a concatenation of each word in the string array parameter `words` exactly once, in any order, and without any intervening characters.
- The order of the starting indices in the integer list result does not matter.