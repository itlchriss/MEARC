- The integer parameter `columnNumber` is greater than or equal to 1 and is less than or equal to 2^31 - 1.
- The string result is the corresponding column title of the integer parameter `columnNumber` as it appears in an Excel sheet.
- The string result consists of uppercase letters only.
- The string result is not null.
- The string result length is less than or equal to 7.