- The string parameter `s` must not be null.
- The length of the integer array result is equal to the length of the string parameter `s`.
- The integer array result contains a permutation of `n + 1` integers of all the integers in the range `[0, n]`.
- The integer array result can be represented as a string `s` of length `n` where:
    - If `s[i] == 'I'`, then `perm[i] < perm[i + 1]`.
    - If `s[i] == 'D'`, then `perm[i] > perm[i + 1]`.
- If there are multiple valid permutations of `perm`, any of them can be returned.