- The integer array parameter `dominoes` must not be null.
- The length of the integer array parameter `dominoes` is greater than or equal to 1 and is less than or equal to 40000.
- Each element in the integer array parameter `dominoes` is an array of length 2.
- The integer values in each element of the integer array parameter `dominoes` are greater than or equal to 1 and are less than or equal to 9.
- The integer result is the number of pairs (i, j) where 0 <= i < j < length of `dominoes`, and `dominoes[i]` is equivalent to `dominoes[j]`.