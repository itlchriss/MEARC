- If the string parameter `s` follows the same pattern as the string parameter `pattern`, the boolean result is equal to the true literal.
- If the string parameter `s` does not follow the same pattern as the string parameter `pattern`, the boolean result is equal to the false literal.
- The length of the string parameter `pattern` is greater than or equal to 1 and is less than or equal to 300.
- The string parameter `pattern` contains only lower-case English letters.
- The length of the string parameter `s` is greater than or equal to 1 and is less than or equal to 3000.
- The string parameter `s` contains only lower-case English letters and spaces.
- The string parameter `s` does not contain any leading or trailing spaces.
- All the words in the string parameter `s` are separated by a single space.