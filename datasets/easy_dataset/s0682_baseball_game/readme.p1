- The integer array parameter `ops` must not be null.
- The integer array parameter `ops` must have a length greater than or equal to 1 and less than or equal to 1000.
- The elements in the integer array parameter `ops` must be either `"C"`, `"D"`, `"+"`, or a string representing an integer in the range [-3 * 10^4, 3 * 10^4].
- For the operation `"+"`, there must be at least two previous scores on the record.
- For the operations `"C"` and `"D"`, there must be at least one previous score on the record.
- The integer result is the sum of all the scores on the record after applying the operations in the `ops` array.