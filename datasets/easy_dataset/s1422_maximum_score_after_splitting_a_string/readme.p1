- The string parameter `s` must have a length greater than or equal to 2 and less than or equal to 500.
- The string parameter `s` consists of only the characters '0' and '1'.
- The integer result is the maximum score after splitting the string `s` into two non-empty substrings.
- The score after splitting a string is calculated as the number of '0's in the left substring plus the number of '1's in the right substring.