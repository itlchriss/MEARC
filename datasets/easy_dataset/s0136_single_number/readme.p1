- The integer array parameter `nums` is non-empty.
- Every element in the integer array parameter `nums` appears twice except for one element.
- The integer result is the single element that appears only once in the integer array parameter `nums`.
- The runtime complexity of the solution is linear.
- The solution uses only constant extra space.