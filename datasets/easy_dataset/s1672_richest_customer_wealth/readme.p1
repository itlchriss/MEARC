- The integer 2D array parameter `accounts` must not be null.
- The integer 2D array parameter `accounts` must have a length `m` and each sub-array within `accounts` must have a length `n`.
- The integer result is the wealth of the richest customer.
- The wealth of a customer is the sum of the amounts in their bank accounts.
- The richest customer is the one with the maximum wealth among all customers.