- The integer matrix parameter `grid` must not be null.
- The integer matrix parameter `grid` must be a square matrix (n x n) where n is greater than or equal to 3 and less than or equal to 100.
- The integer matrix result `maxLocal` must be of size (n - 2) x (n - 2).
- Each element in the integer matrix result `maxLocal` is equal to the largest value of the 3 x 3 matrix in `grid` centered around the corresponding row and column.
- The values in the integer matrix result `maxLocal` correspond to the largest value of a contiguous 3 x 3 matrix in `grid`.