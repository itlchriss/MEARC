- The string parameter `s` must not contain leading zeros.
- If the string parameter `s` contains at most one contiguous segment of ones, the boolean result is equal to the true literal.
- If the string parameter `s` does not contain at most one contiguous segment of ones, the boolean result is equal to the false literal.