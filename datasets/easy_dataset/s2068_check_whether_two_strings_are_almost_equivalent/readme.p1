- If the differences between the frequencies of each letter from 'a' to 'z' between the two string parameters `word1` and `word2` is at most 3, the boolean result is equal to the true literal.
- If the differences between the frequencies of each letter from 'a' to 'z' between the two string parameters `word1` and `word2` is more than 3, the boolean result is equal to the false literal.
- The length of the string parameter `word1` is equal to the length of the string parameter `word2`.
- The string parameters `word1` and `word2` consist only of lowercase English letters.