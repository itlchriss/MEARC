- The length of the string array result is less than or equal to the sum of the lengths of the string parameters `s1` and `s2`.
- Each element in the string array result is an uncommon word that appears exactly once in one of the string parameters `s1` or `s2`, and does not appear in the other string parameter.
- All words in the string parameters `s1` and `s2` are separated by a single space.
- The string parameters `s1` and `s2` consist of lowercase English letters and spaces.
- The string parameters `s1` and `s2` do not have leading or trailing spaces.