- If the strings `s` and `t` are isomorphic, the boolean result is equal to the true literal.
- If the strings `s` and `t` are not isomorphic, the boolean result is equal to the false literal.
- The length of string `s` is greater than or equal to 1 and is less than or equal to 50000.
- The length of string `t` is equal to the length of string `s`.
- All characters in strings `s` and `t` are valid ASCII characters.