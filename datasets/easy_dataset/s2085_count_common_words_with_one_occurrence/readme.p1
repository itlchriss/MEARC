- If a string appears exactly once in both `words1` and `words2` arrays, it is counted towards the final result.
- If a string appears more than once in either `words1` or `words2` arrays, it is not counted towards the final result.
- The final result is the count of strings that appear exactly once in both `words1` and `words2` arrays.