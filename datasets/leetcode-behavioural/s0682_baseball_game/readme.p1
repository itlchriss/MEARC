- If the string array parameter `ops` contains an integer `x`, add `x` to the record.
- If the string array parameter `ops` contains "+", add the sum of the previous two scores to the record.
- If the string array parameter `ops` contains "D", add double the previous score to the record.
- If the string array parameter `ops` contains "C", invalidate and remove the previous score from the record.
- The integer result is the sum of all the scores on the record.