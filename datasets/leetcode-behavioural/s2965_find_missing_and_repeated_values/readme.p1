- The integer matrix parameter `grid` is a 2D integer matrix of size `n * n`.
- All values in the integer matrix parameter `grid` are in the range `[1, n^2]`.
- Each integer in the integer matrix parameter `grid` appears exactly once, except for the repeated value `a` and the missing value `b`.
- The integer result is a 0-indexed integer array `ans` of size 2.
- The integer result `ans[0]` is equal to the repeated value `a`.
- The integer result `ans[1]` is equal to the missing value `b`.
- The integer result `a` is repeated in the integer matrix parameter `grid`.
- The integer result `b` is missing from the integer matrix parameter `grid`.
- The integer result `a` and `b` are unique values.