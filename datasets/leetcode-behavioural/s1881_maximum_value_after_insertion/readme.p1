- The string parameter `n` represents a very large integer and is in the range of [1, 9].
- The integer parameter `x` is in the range of [1, 9].
- The string result is the maximum value of `n` after inserting `x` anywhere in the decimal representation of `n`.
- The digit `x` cannot be inserted to the left of the negative sign in case `n` represents a negative number.