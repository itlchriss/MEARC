Method behavioural requirements:
- The integer matrix parameter `mat` must not be null.
- The integer parameter `k` must be greater than 0.
- The integer result is the k<sup>th</sup> smallest array sum among all possible arrays formed by choosing one element from each row of the matrix `mat`.