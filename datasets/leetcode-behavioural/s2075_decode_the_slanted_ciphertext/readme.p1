- The string parameter `encodedText` consists of lowercase English letters and spaces only.
- The integer parameter `rows` is greater than or equal to 1 and is less than or equal to 1000.
- The string result `originalText` does not have any trailing spaces.
- The test cases are generated such that there is only one possible `originalText`.