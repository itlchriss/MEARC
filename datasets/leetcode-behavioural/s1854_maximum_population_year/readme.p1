- The integer array parameter `logs` must not be null.
- Each element in the integer array parameter `logs` must be an array of length 2.
- The integer array parameter `logs` must have a length greater than or equal to 1 and less than or equal to 100.
- The integer values in each element of the integer array parameter `logs` must satisfy the condition `1950 <= birth_i < death_i <= 2050`.
- The integer result is the earliest year with the maximum population.