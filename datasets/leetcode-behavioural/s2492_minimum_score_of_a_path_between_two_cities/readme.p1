- The integer parameter `n` is greater than or equal to 2 and is less than or equal to 100000.
- The length of the 2D integer array parameter `roads` is greater than or equal to 1 and is less than or equal to 100000.
- Each element in the 2D integer array parameter `roads` has a length of 3.
- The integer values `a_i` and `b_i` in the 2D integer array parameter `roads` are greater than or equal to 1 and are less than or equal to `n`.
- The integer values `a_i` and `b_i` in the 2D integer array parameter `roads` are not equal.
- The integer value `distance_i` in the 2D integer array parameter `roads` is greater than or equal to 1 and is less than or equal to 10000.
- The integer result is the minimum possible score of a path between cities 1 and `n`.