- The integer parameter `bad` is greater than or equal to 1 and is less than or equal to 2^31 - 1.
- The integer result is the first bad version found using the `isBadVersion(version)` API.
- The number of calls to the `isBadVersion(version)` API is minimized.