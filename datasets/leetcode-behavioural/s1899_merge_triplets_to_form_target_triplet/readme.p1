Method behavioural requirements:
- The 2D integer array parameter `triplets` must not be null.
- The integer array parameter `target` must not be null.
- The integer array parameter `target` must have a length of 3.
- If it is possible to obtain the target triplet `[x, y, z]` as an element of the 2D integer array parameter `triplets`, the boolean result is true.
- If it is not possible to obtain the target triplet `[x, y, z]` as an element of the 2D integer array parameter `triplets`, the boolean result is false.