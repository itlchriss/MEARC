- The string parameter `s` consists of lowercase English letters.
- The integer array parameter `shifts` has the same length as the string parameter `s`.
- Each element in the integer array parameter `shifts` is greater than or equal to 0 and is less than or equal to 10^9.
- The final string after applying all shifts to the string `s` is returned as the result.