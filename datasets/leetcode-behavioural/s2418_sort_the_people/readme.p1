- The length of the string array parameter `names` is equal to the length of the integer array parameter `heights`.
- The integer array parameter `heights` consists of distinct positive integers.
- The string array result `names` is sorted in descending order by the integer array parameter `heights`.
- The order of names with the same height remains the same after sorting.