- The integer array parameter `nums` must be 0-indexed.
- A pair of indices `(i, j)` is a bad pair if `i < j` and `j - i != nums[j] - nums[i]`.
- The integer result is the total number of bad pairs in the integer array `nums`.