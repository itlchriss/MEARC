- The integer array parameter `positions` must not be null.
- The integer array parameter `positions` must have a length equal to the length of the string array parameter `pieces`.
- The integer array parameter `positions` must contain distinct coordinates within the range of 1 to 8.
- The string array parameter `pieces` must only contain the strings "rook", "queen", or "bishop".
- The integer result is the number of valid move combinations for the given pieces and positions.