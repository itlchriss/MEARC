- The integer array parameter `words` must not be null.
- The length of each string in the string array parameter `words` is equal.
- The string array parameter `words` contains lowercase English letters.
- The integer result is the number of groups of special-equivalent strings from the string array `words`.
- Two strings `words[i]` and `words[j]` are special-equivalent if after any number of moves, `words[i] == words[j]`.
- A group of special-equivalent strings from `words` is a non-empty subset of words such that every pair of strings in the group are special equivalent, and the group is the largest size possible.