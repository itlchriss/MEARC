Method behavioural requirements:
- The integer parameter `n` must be greater than or equal to 2 and less than or equal to 28.
- The integer parameter `firstPlayer` must be greater than or equal to 1 and less than `secondPlayer`.
- The integer parameter `secondPlayer` must be less than or equal to `n`.
- The integer array result contains two values representing the earliest and latest possible round numbers in which the players `firstPlayer` and `secondPlayer` will compete against each other.