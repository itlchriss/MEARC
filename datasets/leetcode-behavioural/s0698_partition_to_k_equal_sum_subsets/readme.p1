Method behavioural requirements:
- The integer array parameter `nums` must not be null.
- The integer parameter `k` must be greater than 0 and less than or equal to the length of the integer array parameter `nums`.
- The boolean result is true if it is possible to divide the integer array parameter `nums` into `k` non-empty subsets whose sums are all equal.