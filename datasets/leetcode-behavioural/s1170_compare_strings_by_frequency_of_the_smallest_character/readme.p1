- The string array parameter `queries` and `words` must not be null.
- The integer array result `answer` must have the same length as the string array parameter `queries`.
- For each query string `queries[i]`, the integer result `answer[i]` is the number of words in the string array `words` such that `f(queries[i])` < `f(W)` for each word `W` in the string array `words`.