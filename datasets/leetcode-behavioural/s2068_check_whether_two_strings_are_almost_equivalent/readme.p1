- If the differences between the frequencies of each letter from 'a' to 'z' between the two string parameters `word1` and `word2` is at most 3, the boolean result is true.
- If the differences between the frequencies of each letter from 'a' to 'z' between the two string parameters `word1` and `word2` is more than 3, the boolean result is false.
- The length of the string parameters `word1` and `word2` is equal.
- The string parameters `word1` and `word2` consist only of lowercase English letters.