- The string parameter `word1` and `word2` consist of lowercase English letters.
- The integer result is the length of the longest palindrome that can be constructed by choosing non-empty subsequences from `word1` and `word2` and concatenating them.
- If no palindromes can be constructed, the integer result is 0.