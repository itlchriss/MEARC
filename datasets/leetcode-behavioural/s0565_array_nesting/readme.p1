- The integer array parameter `nums` is not null.
- The integer array parameter `nums` is a permutation of the numbers in the range `[0, n - 1]`.
- The integer result is the longest length of a set `s[k]`.
- The set `s[k]` is built according to the rule specified.
- The set `s[k]` stops adding elements right before a duplicate element occurs.