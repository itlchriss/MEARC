- If the integer parameter `x` is positive, the integer result is `x` with its digits reversed.
- If the integer parameter `x` is negative, the integer result is `-x` with its digits reversed.
- If the reversed integer result is outside the range of [-2^31, 2^31 - 1], the integer result is 0.