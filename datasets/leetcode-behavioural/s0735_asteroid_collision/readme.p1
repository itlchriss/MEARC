- The integer array parameter `asteroids` must not be null.
- The length of the integer array parameter `asteroids` is greater than or equal to 2 and is less than or equal to 10000.
- All values in the integer array parameter `asteroids` are greater than or equal to -1000 and are less than or equal to 1000.
- The integer array result contains the state of the asteroids after all collisions.
- If two asteroids meet, the smaller one will explode.
- If both asteroids are the same size, both will explode.
- Two asteroids moving in the same direction will never meet.