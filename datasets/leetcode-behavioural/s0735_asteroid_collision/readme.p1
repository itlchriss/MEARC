- The integer array parameter `asteroids` must not be null.
- The integer array parameter `asteroids` must have a length greater than or equal to 2 and less than or equal to 10000.
- All values in the integer array parameter `asteroids` must be greater than or equal to -1000 and less than or equal to 1000.
- All values in the integer array parameter `asteroids` must not be equal to 0.
- The integer array result represents the state of the asteroids after all collisions.
- If two asteroids meet, the smaller one will explode.
- If both asteroids are the same size, both will explode.
- Two asteroids moving in the same direction will never meet.