- The long result `x` is a non-negative integer.
- The sum of the digits of the long result `n + x` is less than or equal to the integer parameter `target`.
- The long result `x` is the minimum non-negative integer that makes `n + x` beautiful.