- The long result `x` must be non-negative.
- The long result `x` must be the minimum integer such that `n + x` is considered beautiful.
- An integer is considered beautiful if the sum of its digits is less than or equal to the target.
- The input will always be generated in a way that it is possible to make `n` beautiful.