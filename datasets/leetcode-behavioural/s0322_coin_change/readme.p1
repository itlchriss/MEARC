- If the integer array parameter `coins` is empty and the integer parameter `amount` is 0, the integer result is 0.
- If the integer array parameter `coins` is not empty and the integer parameter `amount` is 0, the integer result is 0.
- If the integer array parameter `coins` is not empty and the integer parameter `amount` cannot be made up by any combination of the coins, the integer result is -1.
- If the integer array parameter `coins` is not empty and the integer parameter `amount` can be made up by a combination of the coins, the integer result is the fewest number of coins needed to make up that amount.