- The integer matrix parameter `mat` and the integer matrix parameter `target` must have the same dimensions, `n x n`.
- The integer matrix parameter `mat` and the integer matrix parameter `target` must only contain binary values, either `0` or `1`.
- The boolean result is equal to the true literal if it is possible to make the integer matrix `mat` equal to the integer matrix `target` by rotating `mat` in 90-degree increments.
- The boolean result is equal to the false literal if it is not possible to make the integer matrix `mat` equal to the integer matrix `target` by rotating `mat` in 90-degree increments.