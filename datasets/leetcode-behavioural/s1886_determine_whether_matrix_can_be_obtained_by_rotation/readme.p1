Method behavioural specifications:
- The integer matrix parameter `mat` and the integer matrix parameter `target` must have the same dimensions.
- The integer matrix parameter `mat` and the integer matrix parameter `target` must consist of only 0s and 1s.
- The boolean result is true if it is possible to make the integer matrix parameter `mat` equal to the integer matrix parameter `target` by rotating `mat` in 90-degree increments.
- The boolean result is false if it is impossible to make the integer matrix parameter `mat` equal to the integer matrix parameter `target` by rotating `mat` in 90-degree increments.