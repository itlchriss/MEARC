- The integer parameter `n` must be within the inclusive range [1, 16].
- The integer list result must be a valid n-bit gray code sequence.
- The first element of the integer list result must be 0.
- Each integer in the integer list result must be unique.
- The binary representation of every pair of adjacent integers in the integer list result must differ by exactly one bit.
- The binary representation of the first and last integers in the integer list result must differ by exactly one bit.