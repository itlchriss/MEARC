- The string parameter `s` contains at least one word.
- The string parameter `s` may contain leading or trailing spaces.
- The string parameter `s` may contain multiple spaces between two words.
- The string result is the words in reverse order concatenated by a single space.
- The string result should not contain leading or trailing spaces.
- The string result should only have a single space separating the words.
- The string result should reduce multiple spaces between two words to a single space.