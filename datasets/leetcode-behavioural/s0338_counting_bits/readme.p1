- The integer parameter `num` must be non-negative.
- The length of the integer array result `ans` is equal to `num + 1`.
- For each integer `i` in the range from `0` to `num`, the integer at index `i` in the integer array result `ans` is the number of '1's in the binary representation of `i`.