Method behavioural requirements:
- The integer parameter `n` must be greater than or equal to 2 and less than or equal to 100.
- The integer array parameter `roads` must not be null.
- Each element in the integer array parameter `roads` must be an array of length 2.
- The integers in each array element of the integer array parameter `roads` must be between 0 and `n-1`.
- The integers in each array element of the integer array parameter `roads` must be different from each other.
- There can be at most one road connecting each pair of cities.
- The integer result is the maximal network rank of the entire infrastructure.