- The integer array parameter `apples` and `days` must have the same length `n`.
- The integer array parameter `apples` and `days` must not be null.
- The integer array parameter `apples` and `days` must have values greater than or equal to 0 and less than or equal to 20000.
- The integer array parameter `days` is equal to 0 if and only if the corresponding element in the integer array parameter `apples` is equal to 0.
- The integer result is the maximum number of apples that can be eaten.
- The maximum number of apples that can be eaten is calculated based on the growth of apples and their rotting days as described in the example scenarios.