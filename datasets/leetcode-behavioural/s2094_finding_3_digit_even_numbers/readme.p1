- The length of the integer array parameter `digits` is greater than or equal to 3 and is less than or equal to 100.
- All values in the integer array parameter `digits` are greater than or equal to 0 and are less than or equal to 9.
- The integer array result contains unique integers that are formed by concatenating three elements from the `digits` array in any arbitrary order.
- The integers in the result array do not have leading zeros.
- The integers in the result array are even.
- The integers in the result array are sorted in ascending order.