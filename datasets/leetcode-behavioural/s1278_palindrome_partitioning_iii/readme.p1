- The string parameter `s` must not be null.
- The integer parameter `k` must be greater than or equal to 1 and less than or equal to the length of the string parameter `s`.
- The integer result is the minimal number of characters that need to be changed to divide the string parameter `s` into `k` non-empty disjoint substrings such that each substring is a palindrome.