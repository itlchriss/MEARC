- The integer parameters `rows` and `cols` are greater than or equal to 1 and are less than or equal to 100.
- The integer parameters `rStart` and `cStart` are greater than or equal to 0 and are less than `rows` and `cols` respectively.
- The length of the integer array result is equal to `rows * cols`.
- The integer array result contains coordinates representing the positions of the grid in the order they were visited in a clockwise spiral shape.
- The coordinates in the integer array result are unique.