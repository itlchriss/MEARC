- The string parameter `s` must not be null.
- The integer parameter `k` must be greater than 0.
- The string result is the reformatted license key where each group contains exactly `k` characters, except for the first group which could be shorter but must contain at least one character.
- All lowercase letters in the reformatted license key must be converted to uppercase.
- There must be a dash inserted between two groups in the reformatted license key.
- Any extra dashes in the reformatted license key must be removed.