- The string parameter `loginTime` and `logoutTime` are in the format `hh:mm`.
- The integer result is the number of full chess rounds played between the `loginTime` and `logoutTime`.
- If the `logoutTime` is earlier than the `loginTime`, it means the player has played from `loginTime` to midnight and from midnight to `logoutTime`.