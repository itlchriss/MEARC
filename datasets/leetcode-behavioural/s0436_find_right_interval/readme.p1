- The integer array parameter `intervals` must not be null.
- The length of each sub-array in the integer array parameter `intervals` is 2.
- The integer parameters `start` and `end` in each sub-array are unique.
- The integer array result must have the same length as the integer array parameter `intervals`.
- For each interval `i` in the integer array parameter `intervals`, the integer result at index `i` is the index of the right interval for interval `i`.
- If no right interval exists for interval `i`, the integer result at index `i` is -1.