- The integer array parameter `arr` must not be null.
- The boolean result is true if it is possible to reorder the integer array parameter `arr` such that `arr[2 * i + 1] = 2 * arr[2 * i]` for every `0 <= i < len(arr) / 2`.
- The boolean result is false if it is not possible to reorder the integer array parameter `arr` as specified above.