- The string parameter `s` must not be null.
- The boolean result is true if it is possible to split the string parameter `s` into three non-empty palindromic substrings.
- The boolean result is false if it is not possible to split the string parameter `s` into three non-empty palindromic substrings.