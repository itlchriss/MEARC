Method behavioural requirements:
- The integer parameter `x` and the integer parameter `y` must be non-negative integers.
- The integer result is the number of positions at which the corresponding bits of the integer parameter `x` and the integer parameter `y` are different.