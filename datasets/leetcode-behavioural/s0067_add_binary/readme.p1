- The string parameters `a` and `b` consist only of '0' or '1' characters.
- The length of the string parameters `a` and `b` is greater than or equal to 1 and is less than or equal to 10000.
- The string parameters `a` and `b` do not contain leading zeros except for the zero itself.
- The string result is the sum of the binary strings `a` and `b`.