- The string parameter `a` and `b` must consist only of '0' or '1' characters.
- The string parameter `a` and `b` must not contain leading zeros except for the zero itself.
- The string result is the sum of the binary strings `a` and `b` as a binary string.