- The integer array parameter `citations` must not be null.
- The integer array parameter `citations` length is greater than or equal to 1 and is less than or equal to 5000.
- All values in the integer array parameter `citations` are greater than or equal to 0 and are less than or equal to 1000.
- The integer result is the researcher's `h-index`.
- The `h-index` is the maximum value where `h` of the researcher's papers have at least `h` citations each, and the remaining papers have no more than `h` citations each.