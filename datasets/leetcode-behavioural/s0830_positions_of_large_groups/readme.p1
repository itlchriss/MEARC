- The string parameter `s` must not be null.
- The list of lists result contains intervals of every large group sorted in increasing order by start index.
- A large group is identified by an interval [start, end] where start and end denote the start and end indices (inclusive) of the group.
- A large group is considered to have 3 or more characters.
- Each sublist in the result list represents a large group with the start index at index 0 and the end index at index 1.