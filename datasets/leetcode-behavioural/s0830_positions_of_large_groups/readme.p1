- The list of lists result contains intervals of every large group sorted in increasing order by start index.
- Each sublist in the result list represents a large group interval with the start index at index 0 and the end index at index 1.
- A large group is identified by having 3 or more consecutive characters in the string.
- The input string `s` contains only lowercase English letters.
- The length of the input string `s` is between 1 and 1000.