- The integer result is the length of the longest special substring of the string `s` which occurs at least thrice.
- If no special substring occurs at least thrice, the integer result is -1.
- The string parameter `s` consists of only lowercase English letters.
- The length of the string parameter `s` is greater than or equal to 3 and is less than or equal to 500000.