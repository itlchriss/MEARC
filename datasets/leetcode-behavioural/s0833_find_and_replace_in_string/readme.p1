- If the substring parameter `sources[i]` occurs at the index parameter `indices[i]` in the original string parameter `s`, the substring parameter `sources[i]` is replaced with the substring parameter `targets[i]`.
- If the substring parameter `sources[i]` does not occur at the index parameter `indices[i]` in the original string parameter `s`, no replacement is done.