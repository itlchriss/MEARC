- The integer 2D array parameter `dungeon` must not be null.
- The integer 2D array parameter `dungeon` must have dimensions `m x n` where `m` and `n` are greater than or equal to 1 and less than or equal to 200.
- The integer result is the knight's minimum initial health so that he can rescue the princess.
- The knight must move only rightward or downward in each step.
- If the knight's health drops to 0 or below at any point, he dies immediately.