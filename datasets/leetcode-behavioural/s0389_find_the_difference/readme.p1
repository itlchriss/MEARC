- The character result is the letter that was added to string `t` after random shuffling string `s`.
- The length of string `t` is one more than the length of string `s`.
- The characters in string `s` and string `t` are lowercase English letters.