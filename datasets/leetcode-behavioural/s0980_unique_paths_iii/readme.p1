- The integer array parameter `grid` must not be null.
- The integer array parameter `grid` must have a length `m` and each sub-array within `grid` must have a length `n`.
- The integer array parameter `grid` must contain values between -1 and 2.
- The integer result is the number of 4-directional walks from the starting square to the ending square, that walk over every non-obstacle square exactly once.