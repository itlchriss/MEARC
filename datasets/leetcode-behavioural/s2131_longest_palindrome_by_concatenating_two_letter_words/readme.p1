- The integer array parameter `words` must not be null.
- Each element in the string array parameter `words` consists of two lowercase English letters.
- The integer result is the length of the longest palindrome that can be created by selecting some elements from `words` and concatenating them in any order.
- Each element in `words` can be selected at most once.
- If it is impossible to create any palindrome, the integer result is equal to 0.