- The integer 2D array parameter `points` must not be null.
- The integer parameter `k` must be greater than or equal to 0.
- The integer result is the maximum value of the equation `y_i + y_j + |x_i - x_j|` where `|x_i - x_j| <= k` and `1 <= i < j <= points.length`.