- The string parameter `s` is not null.
- The string parameter `s` length is greater than or equal to 1 and is less than or equal to 200.
- The string parameter `s` contains only uppercase English letters and spaces.
- The list of strings result contains each word vertically in the same order as they appear in the string `s`.
- Each word in the list of strings result is complete with spaces when necessary.
- Trailing spaces are not allowed in the list of strings result.
- Each word in the list of strings result is put on only one column.
- Each column in the list of strings result contains only one word.