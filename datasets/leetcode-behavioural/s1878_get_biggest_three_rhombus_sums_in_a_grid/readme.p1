Method behavioural requirements:
- The integer matrix parameter `grid` must not be null.
- The integer array result contains the three biggest distinct rhombus sums in descending order.
- If there are less than three distinct values, all distinct values are returned.