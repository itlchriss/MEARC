Method behavioural requirements:
- If the integer array parameter `arr` can be converted to the integer array parameter `target` by reversing any non-empty sub-array, the boolean result is true.
- If the integer array parameter `arr` cannot be converted to the integer array parameter `target` by reversing any non-empty sub-array, the boolean result is false.