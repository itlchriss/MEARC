- The integer array parameter `nums` in the `Solution` constructor must not be null.
- The `shuffle` method returns a random shuffling of the array `nums`.
- The `reset` method resets the array back to its original configuration and returns it.
- All permutations of the array `nums` should be equally likely as a result of shuffling.