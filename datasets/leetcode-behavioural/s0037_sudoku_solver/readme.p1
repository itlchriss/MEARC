- Every cell in the 9x9 char array parameter `board` must contain a digit from 1-9 or a '.' character.
- After executing the method with the given method signature, the char array parameter `board` must satisfy the following rules:
    - Each digit from 1-9 must occur exactly once in each row.
    - Each digit from 1-9 must occur exactly once in each column.
    - Each digit from 1-9 must occur exactly once in each of the 9 3x3 sub-boxes of the grid.