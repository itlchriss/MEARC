- The integer array parameter `arr` contains all integers in the range `[1, m * n]`.
- The integer matrix parameter `mat` contains all integers in the range `[1, m * n]`.
- The integer result is the smallest index `i` at which either a row or a column will be completely painted in the matrix `mat`.