Method behavioural specifications:
- The integer matrix parameter `values` must not be null.
- The integer matrix parameter `values` has `m` rows and `n` columns where `1 <= m <= 10` and `1 <= n <= 10000`.
- All values in the integer matrix parameter `values` are greater than or equal to 1 and are less than or equal to 1000000.
- The items in each shop are sorted in non-increasing order of value.
- On each day, the rightmost available item `j` for the price of `values[i][j] * d` is bought, where `d` is the day number.
- The total spending is the maximum amount of money that can be spent on buying all `m * n` products.