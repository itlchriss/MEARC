- The integer parameter `n` must be between 2 and 9, inclusive.
- The integer parameter `k` must be between 0 and 9, inclusive.
- The integer array result must contain all non-negative integers of length `n` such that the absolute difference between every two consecutive digits is `k`.
- Every number in the integer array result must not have leading zeros.