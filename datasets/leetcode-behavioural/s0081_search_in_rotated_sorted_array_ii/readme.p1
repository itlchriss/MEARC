- The integer array parameter `nums` is sorted in non-decreasing order.
- The integer array parameter `nums` is rotated at an unknown pivot index `k` such that the resulting array is `[nums[k], nums[k+1], ..., nums[n-1], nums[0], nums[1], ..., nums[k-1]]`.
- The integer array parameter `nums` may contain duplicates.
- The integer result is true if the integer parameter `target` is in the integer array parameter `nums`, otherwise false.
- The integer result must be determined with the least number of operation steps possible.
- The integer result is true if the integer parameter `target` is in the integer array parameter `nums`, otherwise false.