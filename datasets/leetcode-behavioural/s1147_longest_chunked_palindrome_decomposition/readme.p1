- The integer result is the largest possible value of `k` that satisfies the given conditions.
- The string parameter `text` is a non-empty string.
- The concatenation of all substrings is equal to the original string `text`.
- Each pair of substrings `subtext_i` and `subtext_k-i+1` are equal for all valid values of `i`.
- The length of the string parameter `text` is greater than or equal to 1 and is less than or equal to 1000.
- The string parameter `text` consists only of lowercase English characters.