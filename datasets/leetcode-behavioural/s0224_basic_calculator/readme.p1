- The integer result is the evaluation of the valid expression represented by the string parameter `s`.
- The string parameter `s` consists of digits, '+', '-', '(', ')', and ' '.
- The string parameter `s` represents a valid expression.
- The '+' operator is not used as a unary operation.
- The '-' operator could be used as a unary operation.
- There are no two consecutive operators in the input.
- Every number and running calculation will fit in a signed 32-bit integer.