- For each word in the input `sentence`, if the word has a **successor** that can be replaced by more than one **root** from the `dictionary`, the word should be replaced with the **root** that has the shortest length.
- The output `sentence` should have all the **successors** replaced with their corresponding **roots** from the `dictionary`.
- The output `sentence` should maintain the same word order as the input `sentence`.
- The output `sentence` should only contain lower-case letters and spaces.
- The output `sentence` should not have leading or trailing spaces.