- The string list parameter `dictionary` contains the roots.
- The string parameter `sentence` contains words separated by spaces.
- The string result is the `sentence` after replacing all successors with the root forming it.
- If a successor can be replaced by more than one root, it is replaced with the root that has the shortest length.