- The integer array parameter `servers` must not be null.
- The integer array parameter `tasks` must not be null.
- The length of the integer array parameter `servers` is equal to `n`.
- The length of the integer array parameter `tasks` is equal to `m`.
- All values in the integer array parameter `servers` are greater than or equal to 1 and are less than or equal to 200000.
- All values in the integer array parameter `tasks` are greater than or equal to 1 and are less than or equal to 200000.
- The length of the integer array result `ans` is equal to the length of the integer array parameter `tasks`.
- All values in the integer array result `ans` are unique and are greater than or equal to 0 and are less than the length of the integer array parameter `servers`.