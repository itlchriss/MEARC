- The integer matrix parameter `score` must not be null.
- The integer matrix parameter `score` must have a length `m` and each row must have a length `n`.
- The integer parameter `k` is greater than or equal to 0 and is less than the number of exams `n`.
- The integer matrix result is the matrix `score` sorted by the scores in the `k`th exam from highest to lowest.