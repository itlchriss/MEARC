Method behavioural requirements:
- The integer array parameter `groupSizes` must not be null.
- Each person in the output list must be in a group of size specified by `groupSizes[i]`.
- Each person should appear in exactly one group.
- Every person must be in a group.