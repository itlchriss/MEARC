- The integer array parameter `groupSizes` must not be null.
- The size of the resulting list of groups is equal to the length of the integer array parameter `groupSizes`.
- Each group in the resulting list contains people whose group size matches the corresponding value in the integer array parameter `groupSizes`.
- Each person appears in exactly one group.
- Every person is in a group.
- If there are multiple valid solutions, any of them can be returned.