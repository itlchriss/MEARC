- If the integer array parameter `rods` contains rods that can be welded together to create two disjoint subsets with equal sum, the integer result is the largest possible height of the billboard installation.
- If the integer array parameter `rods` does not contain rods that can be welded together to create two disjoint subsets with equal sum, the integer result is 0.