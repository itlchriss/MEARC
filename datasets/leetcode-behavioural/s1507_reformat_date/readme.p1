- The string parameter `date` is in the format `Day Month Year`.
- The string parameter `date` contains a day in the set `{"1st", "2nd", "3rd", ..., "30th", "31st"}`.
- The string parameter `date` contains a month in the set `{"Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"}`.
- The string parameter `date` contains a year in the range `[1900, 2100]`.
- The string result is in the format `YYYY-MM-DD`.
- The string result's year `YYYY` is a 4-digit number.
- The string result's month `MM` is a 2-digit number.
- The string result's day `DD` is a 2-digit number.