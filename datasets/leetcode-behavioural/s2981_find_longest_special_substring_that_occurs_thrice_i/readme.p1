- The integer parameter `s` must have a length greater than or equal to 3 and less than or equal to 50.
- The string `s` consists of only lowercase English letters.
- The integer result is the length of the longest special substring of `s` which occurs at least thrice.
- If no special substring occurs at least thrice, the integer result is -1.