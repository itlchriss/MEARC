- The integer array parameter `arr` contains distinct integers.
- The integer parameter `k` is greater than or equal to 1 and is less than or equal to 10^9.
- The integer result is the integer which will win the game after winning `k` consecutive rounds.
- The game will be played between the first two elements of the array in each round.
- In each round, the larger integer wins and remains at position 0, while the smaller integer moves to the end of the array.
- The game ends when an integer wins `k` consecutive rounds.
- It is guaranteed that there will be a winner of the game.