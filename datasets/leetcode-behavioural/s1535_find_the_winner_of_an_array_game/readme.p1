- The integer array parameter `arr` contains distinct integers.
- The integer parameter `k` is greater than or equal to 1 and is less than or equal to 10^9.
- The integer result is the integer which will win the game.
- The game ends when an integer wins `k` consecutive rounds.
- The winner is the integer that wins `k` consecutive rounds.
- The winner is the integer that remains at position 0 after winning `k` consecutive rounds.