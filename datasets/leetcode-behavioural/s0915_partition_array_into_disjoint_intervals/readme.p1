Method behavioural requirements:
- The integer array parameter `nums` is partitioned into two non-empty subarrays `left` and `right`.
- Every element in the `left` subarray is less than or equal to every element in the `right` subarray.
- The `left` subarray has the smallest possible size.
- The integer result is the length of the `left` subarray after partitioning.