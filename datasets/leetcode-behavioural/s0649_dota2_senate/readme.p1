Method behavioural requirements:
- If the input string parameter `senate` is empty, the string result is empty.
- If the input string parameter `senate` contains only one party, the string result is the same party.
- If the input string parameter `senate` contains both parties, the string result is the party that will announce the victory and change the Dota2 game.