- The integer parameter `n` must be between 1 and 10, inclusive.
- The integer parameter `k` must be between 1 and 100, inclusive.
- If there are less than `k` happy strings of length `n`, the string result is an empty string.
- The string result is the k-th lexicographical string of all happy strings of length `n`.