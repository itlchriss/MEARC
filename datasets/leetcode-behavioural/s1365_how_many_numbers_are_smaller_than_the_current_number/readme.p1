- The integer array parameter `nums` must not be null.
- The integer array result has the same length as the integer array parameter `nums`.
- For each element `nums[i]` in the integer array parameter `nums`, the integer array result at index `i` contains the count of valid indices `j` such that `j != i` and `nums[j] < nums[i]`.