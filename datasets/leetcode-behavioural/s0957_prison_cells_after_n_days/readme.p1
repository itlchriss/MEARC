- The integer array parameter `cells` must not be null.
- The integer array parameter `cells` must have a length of 8.
- The integer array parameter `cells` must contain only 0s and 1s.
- The integer parameter `n` must be between 1 and 10^9 (inclusive).
- The integer array result is the state of the prison after `n` days, following the rules described above.