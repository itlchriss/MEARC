- The integer array parameter `positions` must not be null.
- The integer array parameter `healths` must not be null.
- The string parameter `directions` must not be null.
- The length of the integer array parameter `positions` must be equal to the length of the integer array parameter `healths` and the length of the string parameter `directions`.
- All values in the integer array parameter `positions` must be unique.
- The integer array result contains the health of the remaining robots in the order they were given in the input.
- If two robots collide, the robot with lower health is removed from the line, and the health of the other robot decreases by one.
- If both robots have the same health, they are both removed from the line.
- If there are no survivors, an empty array is returned.