- The length of the string parameter `message` is greater than or equal to 1 and is less than or equal to 10000.
- The string parameter `message` consists only of lowercase English letters and space character.
- The integer parameter `limit` is greater than or equal to 1 and is less than or equal to 10000.
- The length of each string in the string array result is equal to the integer parameter `limit`, except for the last string whose length can be at most `limit`.
- Each string in the string array result has the suffix `"<a/b>"`, where `"b"` is the total number of parts and `"a"` is the index of the part, starting from 1 to `b`.
- The concatenated string of all parts in order is equal to the string parameter `message`.
- The result contains as few parts as possible.
- If it is impossible to split the string parameter `message` as required, the result is an empty array.