- The integer array parameter `arr` must not be null.
- The integer result is the majority element in the integer array `arr`.
- The majority element is the element that appears more than ⌊n / 2⌋ times in the integer array `arr`, where `n` is the length of the array.
- The integer result is within the range of -2^31 to 2^31 - 1.