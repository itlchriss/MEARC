- The integer array parameter `rating` contains unique values.
- The integer result is the number of valid teams that can be formed.
- A team is considered valid if the soldiers' ratings satisfy the condition (`rating[i] < rating[j] < rating[k]`) or (`rating[i] > rating[j] > rating[k]`) where (`0 <= i < j < k < n`).