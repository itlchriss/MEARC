Method behavioural specifications:
- The 2D integer arrays `items1` and `items2` must not be null.
- Each item in the 2D integer arrays `items1` and `items2` must have a length of 2.
- The value of each item in `items1` and `items2` must be unique.
- The returned 2D integer array `ret` must be in ascending order by value.
- Each item in the returned 2D integer array `ret` must have a length of 2.
- The weight of each item in the returned 2D integer array `ret` must be the sum of weights of all items with the same value.