- The string parameter `licensePlate` must not be null.
- The string array parameter `words` must not be null.
- The string result is a word from the string array parameter `words` that is the shortest completing word for the string parameter `licensePlate`.
- The completing word must contain all the letters in the `licensePlate` string, ignoring numbers and spaces, and treating letters as case insensitive.
- If there are multiple shortest completing words, the method must return the first one that occurs in the `words` array.