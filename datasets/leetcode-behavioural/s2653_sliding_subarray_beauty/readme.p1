- The integer array parameter `nums` must not be null.
- The integer parameter `k` is greater than or equal to 1 and is less than or equal to the length of the integer array `nums`.
- The integer parameter `x` is greater than or equal to 1 and is less than or equal to `k`.
- The length of the integer array result is equal to `n - k + 1`.
- Each element in the integer array result is the `x`<sup>th</sup> smallest negative integer in the corresponding subarray of size `k`, or `0` if there are fewer than `x` negative integers in the subarray.