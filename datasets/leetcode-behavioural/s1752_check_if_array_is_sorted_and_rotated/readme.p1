Method behavioural requirements:
- If the integer array parameter `nums` was originally sorted in non-decreasing order and then rotated some number of positions (including zero), the boolean result is true.
- If the integer array parameter `nums` was not originally sorted in non-decreasing order and then rotated some number of positions (including zero), the boolean result is false.