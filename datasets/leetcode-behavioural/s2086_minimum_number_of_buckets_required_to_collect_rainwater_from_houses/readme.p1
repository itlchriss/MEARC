- The integer result is the minimum number of buckets needed so that for every house, there is at least one bucket collecting rainwater from it.
- If it is impossible to collect the rainwater from all the houses, the integer result is -1.
- The string parameter `street` has a length greater than or equal to 1 and less than or equal to 100000.
- Each character in the string parameter `street` is either 'H' representing a house or '.' representing an empty space.