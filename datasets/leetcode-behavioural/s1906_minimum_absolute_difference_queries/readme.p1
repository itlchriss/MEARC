- The integer array parameter `nums` must not be null.
- The integer array parameter `nums` must have a length between 2 and 100,000.
- Each element in the integer array parameter `nums` must be between 1 and 100.
- The integer array parameter `queries` must not be null.
- The integer array parameter `queries` must have a length between 1 and 20,000.
- Each subarray in the integer array parameter `queries` must have two elements where the first element is less than the second element and both elements are within the bounds of the integer array parameter `nums`.
- The integer array result `ans` must have the same length as the integer array parameter `queries`.
- The integer array result `ans` must contain the minimum absolute difference for each subarray specified in the integer array parameter `queries`.
- If all elements in a subarray are the same, the minimum absolute difference is -1.