- The integer 2D array parameter `grid` must not be null.
- The integer 2D array parameter `grid` must have a size of `m x n` where `m` and `n` are greater than or equal to 1 and less than or equal to 100.
- The integer result is an array of size `n`.
- Each element in the integer result array is the column that the ball falls out of at the bottom after dropping the ball from the corresponding column at the top, or -1 if the ball gets stuck in the box.