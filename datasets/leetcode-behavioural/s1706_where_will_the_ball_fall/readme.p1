- The integer array parameter `grid` must not be null.
- The integer array parameter `grid` must have a size of `m x n` where `m` and `n` are greater than or equal to 1 and less than or equal to 100.
- The integer array result `answer` must have a size of `n`.
- Each element in the integer array result `answer` is the column that the ball falls out of at the bottom after dropping the ball from the `i-th` column at the top, or `-1` if the ball gets stuck in the box.