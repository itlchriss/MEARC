- The string parameter `str1` and `str2` must not be null.
- The string result is a subsequence of both string parameter `str1` and `str2`.
- The string result is the shortest string that has both string parameter `str1` and `str2` as subsequences.