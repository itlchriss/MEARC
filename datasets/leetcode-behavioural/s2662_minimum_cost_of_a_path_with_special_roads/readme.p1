- The integer array parameter `start` and `target` must have a length of 2.
- The integer array parameter `start` and `target` must contain values within the range of 1 to 100000.
- The integer array parameter `specialRoads` must have a length between 1 and 200.
- Each element in the integer array parameter `specialRoads` must have a length of 5.
- The values in the `specialRoads` array must satisfy the constraints: `startX <= x1i, x2i <= targetX` and `startY <= y1i, y2i <= targetY`.
- The cost values in the `specialRoads` array must be within the range of 1 to 100000.
- The integer result is the minimum cost required to go from the position specified in the `start` array to the position specified in the `target` array using the special roads provided in the `specialRoads` array.