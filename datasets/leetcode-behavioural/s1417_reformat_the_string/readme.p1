- If the input string `s` is empty, the result is an empty string.
- If the input string `s` contains only letters or only digits, the result is an empty string.
- The reformatted string contains no two adjacent characters of the same type (letter or digit).
- The reformatted string is a valid permutation of the input string `s` where no letter is followed by another letter and no digit is followed by another digit.