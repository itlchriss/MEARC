- The integer result is the minimum number of characters that need to be appended to the end of string parameter `s` so that string parameter `t` becomes a subsequence of string parameter `s`.
- The length of string parameter `s` is greater than or equal to the length of string parameter `t`.
- The string parameter `s` consists only of lowercase English letters.
- The string parameter `t` consists only of lowercase English letters.