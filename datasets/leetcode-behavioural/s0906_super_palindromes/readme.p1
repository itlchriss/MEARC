- The string parameters `left` and `right` must consist of only digits.
- The string parameters `left` and `right` cannot have leading zeros.
- The string parameters `left` and `right` represent integers in the range [1, 10^18 - 1].
- The string parameter `left` is less than or equal to the string parameter `right`.
- The integer result is the number of super-palindromes integers in the inclusive range [left, right].