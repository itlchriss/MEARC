- The integer parameter `s` must be a string consisting of digits from 0 to 9.
- The integer result is the length of the longest semi-repetitive substring inside the string `s`.
- A semi-repetitive substring is defined as a substring where there is at most one consecutive pair of the same digits.
- The substring must be contiguous and non-empty.
- The length of the string `s` is greater than or equal to 1 and is less than or equal to 50.