Method behavioural requirements:
- The integer 2D array parameter `grid` must not be null.
- The integer result is the size `k` of the largest magic square that can be found within the integer 2D array parameter `grid`.
- The largest magic square has a side length of `k`.
- Every row sum, column sum, and diagonal sum of the largest magic square is equal.