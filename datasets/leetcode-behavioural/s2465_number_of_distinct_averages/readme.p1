Method behavioural specifications:
- The integer array parameter `nums` has an even length.
- The integer array parameter `nums` is not empty.
- The integer result is the number of distinct averages calculated using the process described.
- The average of two numbers `a` and `b` is calculated as `(a + b) / 2`.
- When there is a tie for a minimum or maximum number, any can be removed.