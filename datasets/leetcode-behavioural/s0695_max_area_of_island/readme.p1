- The integer matrix parameter `grid` must not be null.
- The integer matrix parameter `grid` must have a length `m` and each row in the matrix must have a length `n`.
- The integer matrix parameter `grid` must only contain values of 0 or 1.
- The integer result is the maximum area of an island in the integer matrix `grid`.
- If there is no island in the integer matrix `grid`, the integer result is equal to 0.