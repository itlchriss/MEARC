- The string parameter `s` must not be null.
- The string parameter `t` must not be null.
- The string result is the minimum window substring of string `s` that includes every character in string `t`.
- If there is no such substring, the string result is equal to the empty string `""`.
- The minimum window substring must be unique.
- The algorithm must run in O(m + n) time complexity, where m is the length of string `s` and n is the length of string `t`.