- The string parameter `s3` is formed by an interleaving of the string parameters `s1` and `s2`.
- The length of the string parameter `s1` is less than or equal to 100.
- The length of the string parameter `s2` is less than or equal to 100.
- The length of the string parameter `s3` is less than or equal to 200.
- The string parameters `s1`, `s2`, and `s3` consist of lowercase English letters.