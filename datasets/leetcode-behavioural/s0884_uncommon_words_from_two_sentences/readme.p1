- The string array result contains all the words that appear exactly once in one of the sentences and do not appear in the other sentence.
- The length of the string array result is not fixed and can vary based on the input sentences.
- The words in the string array result are in any order.
- The string parameters `s1` and `s2` consist of lowercase English letters and spaces.
- The string parameters `s1` and `s2` do not have leading or trailing spaces.
- All the words in `s1` and `s2` are separated by a single space.