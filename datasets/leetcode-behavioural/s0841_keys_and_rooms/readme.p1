Method behavioural requirements:
- If the integer collection parameter `rooms` contains a set of keys that allows visiting all rooms, the boolean result is true.
- If the integer collection parameter `rooms` does not contain a set of keys that allows visiting all rooms, the boolean result is false.