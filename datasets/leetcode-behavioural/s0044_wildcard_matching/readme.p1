- If the input string parameter `inputString` matches the pattern parameter `pattern` entirely, the boolean result is true.
- If the input string parameter `inputString` does not match the pattern parameter `pattern` entirely, the boolean result is false.
- The wildcard character '?' in the pattern parameter `pattern` matches any single character in the input string parameter `inputString`.
- The wildcard character '*' in the pattern parameter `pattern` matches any sequence of characters (including the empty sequence) in the input string parameter `inputString`.