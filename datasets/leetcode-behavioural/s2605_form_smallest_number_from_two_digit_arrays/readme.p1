- The integer array parameter `nums1` must contain unique digits.
- The integer array parameter `nums2` must contain unique digits.
- The integer result is the smallest number that contains at least one digit from both `nums1` and `nums2`.