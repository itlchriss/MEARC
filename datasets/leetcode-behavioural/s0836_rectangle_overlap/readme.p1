- If the integer array parameter `rec1` and the integer array parameter `rec2` overlap, the boolean result is true.
- If the integer array parameter `rec1` and the integer array parameter `rec2` do not overlap, the boolean result is false.