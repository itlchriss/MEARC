- The integer parameter `n` is greater than or equal to 1 and is less than or equal to 10000.
- The integer result is the clumsy factorial of the integer parameter `n`.
- The integer result is calculated using the following operations in order: multiply `'*'`, divide `'/'`, add `'+'`, and subtract `'-'`.
- The division used is floor division.
- All multiplication and division steps are performed before any addition or subtraction steps.
- Multiplication and division steps are processed from left to right.