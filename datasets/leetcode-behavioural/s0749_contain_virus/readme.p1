- The integer matrix parameter `grid` must not be null.
- The integer matrix parameter `grid` must have dimensions `m x n` where `1 <= m, n <= 50`.
- The integer matrix parameter `grid` must contain only 0s and 1s, where 0 represents uninfected cells and 1 represents infected cells.
- The integer result is the number of walls used to quarantine all the infected regions.
- If the world will become fully infected, the integer result is the number of walls used.