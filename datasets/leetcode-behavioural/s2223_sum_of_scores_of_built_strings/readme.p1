- The string parameter `s` must not be null.
- The string parameter `s` consists of lowercase English letters.
- The integer result is the sum of the scores of every `s_i` where `1 <= i <= n`.
- The score of `s_i` is the length of the longest common prefix between `s_i` and `s_n`.
- If there is no common prefix between `s_i` and `s_n`, the score is 0.