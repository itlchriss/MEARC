- The merged string result is formed by adding letters from `word1` and `word2` in alternating order.
- If one string is longer than the other, the additional letters are appended to the end of the merged string.
- The length of the merged string is equal to the sum of the lengths of `word1` and `word2`.
- The merged string contains all the letters from `word1` and `word2` in alternating order.
- The merged string consists of lowercase English letters.
- The length of `word1` and `word2` is greater than or equal to 1 and is less than or equal to 100.