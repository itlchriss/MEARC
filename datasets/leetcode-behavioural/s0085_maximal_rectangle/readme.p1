- The integer result is the area of the largest rectangle containing only '1's in the binary matrix.
- The binary matrix parameter `matrix` must not be null.
- The binary matrix parameter `matrix` must be a `rows x cols` matrix.
- The binary matrix parameter `matrix` must only contain '0's and '1's.
- The integer result is greater than or equal to 0.
- The integer result is less than or equal to 200.