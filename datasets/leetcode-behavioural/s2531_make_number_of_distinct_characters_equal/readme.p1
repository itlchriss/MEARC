- The string parameters `word1` and `word2` must consist of only lowercase English letters.
- The length of the string parameters `word1` and `word2` is greater than or equal to 1 and is less than or equal to 100000.
- The boolean result is equal to the true literal if it is possible to get the number of distinct characters in `word1` and `word2` to be equal with exactly one move.
- The boolean result is equal to the false literal if it is not possible to get the number of distinct characters in `word1` and `word2` to be equal with exactly one move.