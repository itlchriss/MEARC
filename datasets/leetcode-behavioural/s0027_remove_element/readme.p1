- Every occurrence of the integer parameter `val` in the integer array parameter `nums` is removed in-place.
- The relative order of the elements in the integer array parameter `nums` may be changed.
- The first `k` elements of the integer array parameter `nums` hold the final result after removing the occurrences of the integer parameter `val`.
- The integer result `k` is returned after placing the final result in the first `k` slots of the integer array parameter `nums`.
- No extra space is allocated for another array, and the operation is done by modifying the input array in-place with O(1) extra memory.