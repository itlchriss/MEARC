- The string parameter `s` must not be null.
- The string result is the lexicographically smallest string that can be written on the paper.
- The robot can only perform the specified operations until both `s` and `t` are empty.
- The robot can only remove the first character of `s` or the last character of `t` in each operation.
- The robot appends the removed character to the string `t` or writes it on paper to form the written string `p`.
- The written string `p` is the lexicographically smallest string that can be formed through the specified operations.