- The boolean result is equal to the true literal if there exists a valid path in the grid from the upper-left cell to the bottom-right cell following the streets as described.
- The boolean result is equal to the false literal if there is no valid path in the grid from the upper-left cell to the bottom-right cell following the streets as described.
- The integer 2D array parameter `grid` must have a length `m` and each row in the array must have a length `n`.
- The integer values in the `grid` array must be between 1 and 6 inclusive.
- The starting point is the upper-left cell `(0, 0)` and the ending point is the bottom-right cell `(m - 1, n - 1)`.
- The path should only follow the streets as described in the grid.
- It is not allowed to change any street in the grid.