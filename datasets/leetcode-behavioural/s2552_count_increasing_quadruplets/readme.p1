- The integer array parameter `nums` must be of size `n` and contain all numbers from `1` to `n`.
- The long result is the number of increasing quadruplets `(i, j, k, l)` where `0 <= i < j < k < l < n` and `nums[i] < nums[k] < nums[j] < nums[l]`.