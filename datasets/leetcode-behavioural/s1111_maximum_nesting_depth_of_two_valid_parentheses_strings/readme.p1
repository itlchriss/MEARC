- The string parameter `seq` must not be null.
- The integer array result `answer` must have a length equal to the length of the string parameter `seq`.
- The integer array result `answer` must contain only 0s and 1s.
- The integer array result `answer` must encode a choice of subsequences `A` and `B` such that `max(depth(A), depth(B))` is minimized.
- The integer array result `answer` must follow the encoding rule: `answer[i] = 0` if `seq[i]` is part of subsequence `A`, else `answer[i] = 1`.