- The string parameter `seq` must consist of only "(" and ")" characters.
- The integer array result must have a length equal to the length of the string parameter `seq`.
- Each element in the integer array result must be either 0 or 1.
- The integer array result must encode a choice of subsequences `A` and `B` such that `max(depth(A), depth(B))` is minimized.
- The integer array result must follow the encoding rule: `answer[i] = 0` if `seq[i]` is part of `A`, else `answer[i] = 1`.