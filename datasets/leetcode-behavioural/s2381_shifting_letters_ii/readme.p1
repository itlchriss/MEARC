- The string parameter `s` must consist of lowercase English letters.
- The 2D integer array parameter `shifts` must have a length greater than or equal to 1 and less than or equal to 50000.
- Each element in the 2D integer array parameter `shifts` must have a length of 3.
- The integer values in each element of the 2D integer array parameter `shifts` must satisfy the constraints: 0 <= start_i <= end_i < s.length and 0 <= direction_i <= 1.
- The final string result after applying all shifts to `s` must consist of lowercase English letters.