- The integer list result contains the start indices of `p`'s anagrams in `s`.
- The start indices in the integer list result are in any order.
- The length of the integer list result is less than or equal to the length of string `s`.
- The length of string `s` is greater than or equal to the length of string `p`.
- The strings `s` and `p` consist of lowercase English letters.