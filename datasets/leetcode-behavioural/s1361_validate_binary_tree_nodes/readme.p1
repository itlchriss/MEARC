- The integer parameter `n` must be greater than or equal to 1.
- The integer array parameter `leftChild` must have the same length as the integer array parameter `rightChild` and both arrays must have a length equal to the integer parameter `n`.
- The integer array parameter `leftChild` and `rightChild` must contain values between -1 and `n - 1`.
- The boolean result is true if and only if all the given nodes form exactly one valid binary tree.