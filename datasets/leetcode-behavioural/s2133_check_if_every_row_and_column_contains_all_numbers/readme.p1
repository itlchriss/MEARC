Method behavioural specifications:
- The integer matrix parameter `matrix` is valid if every row and every column contains all integers from 1 to n (inclusive).
- The boolean result is true if the matrix is valid.
- The boolean result is false if the matrix is not valid.
- The integer matrix `matrix` is a square matrix (n x n) where n is the length of the matrix.
- The integer values in the matrix `matrix` are between 1 and n (inclusive).
- The length of the integer matrix `matrix` is between 1 and 100 (inclusive).