- For each string `a` in the string array parameter `words1`, if every letter in each string `b` in the string array parameter `words2` occurs in `a` including multiplicity, the string `a` is added to the result list.
- The result list contains all the universal strings in the string array parameter `words1`.