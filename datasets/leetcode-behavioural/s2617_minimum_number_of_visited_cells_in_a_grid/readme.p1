- The integer matrix parameter `grid` must be a 0-indexed `m x n` matrix.
- The integer result is the minimum number of cells needed to visit to reach the bottom-right cell `(m - 1, n - 1)`.
- If there is no valid path to reach the bottom-right cell, the integer result is -1.