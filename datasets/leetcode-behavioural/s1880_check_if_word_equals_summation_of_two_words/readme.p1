- The numerical value of the string `firstWord` is the concatenation of the letter values of each letter in `firstWord`, converted into an integer.
- The numerical value of the string `secondWord` is the concatenation of the letter values of each letter in `secondWord`, converted into an integer.
- The numerical value of the string `targetWord` is the concatenation of the letter values of each letter in `targetWord`, converted into an integer.
- If the summation of the numerical values of `firstWord` and `secondWord` is equal to the numerical value of `targetWord`, the boolean result is equal to the true literal.
- If the summation of the numerical values of `firstWord` and `secondWord` is not equal to the numerical value of `targetWord`, the boolean result is equal to the false literal.
- The length of the strings `firstWord`, `secondWord`, and `targetWord` is greater than or equal to 1 and is less than or equal to 8.
- The strings `firstWord`, `secondWord`, and `targetWord` consist of lowercase English letters from 'a' to 'j' inclusive.