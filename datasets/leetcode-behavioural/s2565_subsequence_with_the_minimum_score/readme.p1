- The integer result is the minimum possible score to make string `t` a subsequence of string `s`.
- The integer result is equal to 0 if no characters are removed from string `t`.
- If characters are removed from string `t`, the integer result is equal to `right - left + 1`, where `left` is the minimum index among all removed characters and `right` is the maximum index among all removed characters.
- The strings `s` and `t` consist of only lowercase English letters.
- The length of strings `s` and `t` is greater than or equal to 1 and is less than or equal to 100000.