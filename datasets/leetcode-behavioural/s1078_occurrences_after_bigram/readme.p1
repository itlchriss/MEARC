- If the string parameter `text` contains occurrences of the form "first second third", where `second` comes immediately after `first` and `third` comes immediately after `second`, the string array result contains all the words `third` for each occurrence of "first second third".
- The string array result contains the words `third` in the order they appear in the text after the corresponding "first second" occurrence.