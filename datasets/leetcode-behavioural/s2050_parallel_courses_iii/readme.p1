- The integer parameter `n` is greater than or equal to 1 and is less than or equal to 50000.
- The length of the 2D integer array parameter `relations` is less than or equal to the minimum of `n * (n - 1) / 2` and 50000.
- Each sub-array in the 2D integer array parameter `relations` has a length of 2.
- The values in each sub-array in the 2D integer array parameter `relations` are unique.
- The values in each sub-array in the 2D integer array parameter `relations` are greater than or equal to 1 and are less than or equal to `n`.
- The first value in each sub-array in the 2D integer array parameter `relations` is not equal to the second value.
- The length of the integer array parameter `time` is equal to `n`.
- The values in the integer array parameter `time` are greater than or equal to 1 and are less than or equal to 10000.
- The graph represented by the 2D integer array parameter `relations` is a directed acyclic graph.
- The integer result is the minimum number of months needed to complete all the courses.