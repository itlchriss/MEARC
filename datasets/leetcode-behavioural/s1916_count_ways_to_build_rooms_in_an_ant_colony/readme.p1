- The integer array parameter `prevRoom` must not be null.
- The integer array parameter `prevRoom` must be 0-indexed.
- The integer array parameter `prevRoom` must have a length `n` where `n` is between 2 and 10^5.
- The integer array parameter `prevRoom` must have `prevRoom[0]` equal to -1.
- The integer array parameter `prevRoom` must have elements such that `0 <= prevRoom[i] < n` for all `1 <= i < n`.
- The integer result is the number of different orders to build all the rooms in, modulo 10^9 + 7.