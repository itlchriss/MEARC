Method behavioural requirements:
- The integer collection parameter `cost` must not be null.
- The integer collection parameter `cost` must have a size of `size1`.
- Each element in the integer collection parameter `cost` must have a size of `size2`.
- The integer result is the minimum cost to connect the two groups.