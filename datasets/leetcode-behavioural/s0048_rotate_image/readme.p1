- The 2D integer array parameter `matrix` must have a length equal to `n`.
- Each row of the 2D integer array parameter `matrix` must have a length equal to `n`.
- The integer values in the 2D integer array parameter `matrix` are between -1000 and 1000.
- The 2D integer array `matrix` is rotated by 90 degrees clockwise.
- The rotation of the 2D integer array `matrix` is done in-place, meaning the input matrix is modified directly without allocating another matrix.