- The 2D integer array parameter `matrix` must not be null.
- The 2D integer array parameter `matrix` must have a length equal to `n`.
- Each row of the 2D integer array parameter `matrix` must have a length equal to `n`.
- The integer values in the 2D integer array parameter `matrix` must be within the range -1000 to 1000.
- The 2D integer array `matrix` is rotated by 90 degrees clockwise in-place.
- The rotated 2D integer array `matrix` is modified directly without allocating another 2D matrix.