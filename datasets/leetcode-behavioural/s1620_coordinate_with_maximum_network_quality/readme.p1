Method behavioural requirements:
- The integer array parameter `towers` must not be null.
- The integer parameter `radius` must be greater than or equal to 1.
- The integer array result must have a length of 2.
- The integer array result must represent the integral coordinate with the maximum network quality.
- If there are multiple coordinates with the same network quality, the result must be the lexicographically minimum non-negative coordinate.