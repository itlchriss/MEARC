- The first row of the output list must be the header row with the first column as "Table" followed by the names of the food items in alphabetical order.
- The subsequent rows must represent each table's orders, with the first column being the table number and the remaining columns denoting the quantity of each food item ordered by that table.
- The rows must be sorted in numerically increasing order based on the table number.
- Each table's row must contain the count of each food item ordered by that table, with zeros for items not ordered.
- The output list must not contain customer names and must only display table numbers and food items.
- The output list must follow the specified format and order as described in the examples provided.