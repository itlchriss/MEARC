Method behavioural requirements:
- The integer parameter `n` must be greater than or equal to 1.
- The integer 2D array parameter `edges` must have a length of `n - 1`.
- The integer 2D array parameter `edges` must contain distinct pairs of integers within the range of 0 to `n`.
- The integer collection result contains the root labels of all minimum height trees.