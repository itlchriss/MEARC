- The integer parameter `n` is greater than or equal to 1 and is less than or equal to 20000.
- The length of the integer array parameter `edges` is equal to `n - 1`.
- Each element in the integer array parameter `edges` is an array of two integers `[a, b]` where `0 <= a, b < n` and `a != b`.
- All pairs `(a, b)` in the integer array parameter `edges` are distinct.
- The given input is guaranteed to represent a tree with no repeated edges.
- The integer list result contains the root labels of all minimum height trees.
- The result list can be returned in any order.