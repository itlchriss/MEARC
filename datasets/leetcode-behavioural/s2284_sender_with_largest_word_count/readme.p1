- The string array parameter `messages` and `senders` must not be null.
- The length of the string array parameter `messages` and `senders` must be equal.
- Each element in the string array parameter `messages` must consist of uppercase and lowercase English letters and spaces.
- Each element in the string array parameter `senders` must consist of uppercase and lowercase English letters only.
- All words in each element of the string array parameter `messages` must be separated by a single space.
- Each element in the string array parameter `messages` must not have leading or trailing spaces.
- The string result is the sender with the largest word count.
- If there is more than one sender with the largest word count, the sender with the lexicographically largest name is returned.