- The integer array parameter `a` and `b` must be of the same length `n`.
- The integer array result `C` must be of the same length as the input arrays `a` and `b`.
- Each element in the integer array result `C` must be equal to the count of numbers that are present at or before the corresponding index in both arrays `a` and `b`.
- The values in the integer array result `C` must be in ascending order.
- The values in the integer array result `C` must be unique.