- The boolean result is equal to the true literal if changing the cell at position `(rMove, cMove)` to the color `color` is a legal move.
- The boolean result is equal to the false literal if changing the cell at position `(rMove, cMove)` to the color `color` is not a legal move.