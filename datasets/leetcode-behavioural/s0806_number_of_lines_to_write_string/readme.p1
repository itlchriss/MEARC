- The integer array parameter `widths` must contain 26 elements, each element representing the width of a lowercase English letter.
- The string parameter `s` contains only lowercase English letters.
- The integer result array has a length of 2.
- The integer result array's first element represents the total number of lines required to write the string `s` across, where each line is no longer than 100 pixels wide.
- The integer result array's second element represents the width of the last line in pixels after writing the string `s` across multiple lines.