- If there exist adjacent strings in the string array parameter `words` that are anagrams of each other, delete the string at the higher index.
- Repeat the above step until no more adjacent strings are anagrams of each other.
- The resulting list should contain the remaining strings after performing all operations.