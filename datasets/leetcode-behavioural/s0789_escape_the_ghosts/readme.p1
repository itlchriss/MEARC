Method behavioural requirements:
- The integer array parameter `ghosts` must not be null.
- The integer array parameter `target` must not be null.
- The boolean result is true if it is possible to reach the integer array parameter `target` before any ghost reaches the player.
- The boolean result is false if any ghost can reach the integer array parameter `target` at the same time as the player.