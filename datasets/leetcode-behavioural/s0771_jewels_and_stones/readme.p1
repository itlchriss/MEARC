- The integer result is the number of stones in the `stones` string that are also jewels in the `jewels` string.
- The strings `jewels` and `stones` consist of only English letters.
- The characters in the `jewels` string are unique.
- The length of the `jewels` and `stones` strings is between 1 and 50.