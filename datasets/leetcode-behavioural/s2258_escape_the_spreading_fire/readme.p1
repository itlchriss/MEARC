- The integer 2D array parameter `grid` must not be null.
- The integer 2D array parameter `grid` must have a size of `m x n` where `m` and `n` are greater than or equal to 2 and less than or equal to 300.
- The integer 2D array parameter `grid` must contain values of 0, 1, or 2.
- The top-left cell of the integer 2D array parameter `grid` must be 0.
- The bottom-right cell of the integer 2D array parameter `grid` must be 0.
- The integer result is the maximum number of minutes that can be stayed in the initial position before moving while still safely reaching the safehouse.
- If it is impossible to reach the safehouse, the integer result is -1.
- If it is always possible to reach the safehouse regardless of the minutes stayed, the integer result is 10^9.