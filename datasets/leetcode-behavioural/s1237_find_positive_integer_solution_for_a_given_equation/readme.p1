Method behavioural requirements:
- The integer parameter `z` must be a positive integer.
- The method must return a list of positive integer pairs `x` and `y` where `f(x, y) == z`.
- The function `f(x, y)` must be monotonically increasing, i.e., `f(x, y) < f(x + 1, y)` and `f(x, y) < f(x, y + 1)`.