- The string parameter `s` consists of digits and the `'#'` letter.
- The string parameter `s` will be a valid string such that mapping is always possible.
- The string result is formed after mapping the string parameter `s` to English lowercase characters as follows:
    - Characters ('a' to 'i') are represented by ('1' to '9') respectively.
    - Characters ('j' to 'z') are represented by ('10#' to '26#') respectively.