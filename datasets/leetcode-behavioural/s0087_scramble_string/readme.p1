- The string parameters `s1` and `s2` must be of the same length.
- The length of the string parameters `s1` and `s2` is greater than or equal to 1 and is less than or equal to 30.
- The string parameters `s1` and `s2` consist of lower-case English letters.
- The boolean result is equal to the true literal if `s2` is a scrambled string of `s1`, otherwise, the boolean result is equal to the false literal.