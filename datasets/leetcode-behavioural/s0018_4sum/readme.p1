- The integer array parameter `nums` must not be null.
- The integer result is a list of lists of integers representing unique quadruplets `[nums[a], nums[b], nums[c], nums[d]]` such that `nums[a] + nums[b] + nums[c] + nums[d] == target`.
- The integers `a`, `b`, `c`, and `d` must be distinct.
- The order of the quadruplets in the output list can be in any order.