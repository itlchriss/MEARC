- The integer result is the area of the largest square containing only '1's in the binary matrix.
- The binary matrix parameter `matrix` must not be null.
- The binary matrix parameter `matrix` must have a length `m` and each row must have a length `n`.
- The length of the binary matrix parameter `matrix` is less than or equal to 300.
- All elements in the binary matrix parameter `matrix` are either '0' or '1'.