- If the input string `s` is empty, the resulting string is also empty.
- The resulting string `t` contains all consonants in their original positions.
- The vowels in the resulting string `t` are sorted in non-decreasing order of their ASCII values.
- The vowels considered are 'a', 'e', 'i', 'o', and 'u', in both lowercase and uppercase.
- The resulting string `t` has the same length as the input string `s`.
- The resulting string `t` is a permutation of the input string `s`, with vowels sorted and consonants remaining in their original positions.