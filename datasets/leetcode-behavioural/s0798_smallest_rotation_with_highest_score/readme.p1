- The integer array parameter `nums` must not be null.
- The integer result is the rotation index `k` that corresponds to the highest score achieved after rotating the integer array parameter `nums`.
- If there are multiple rotation indexes `k` that correspond to the highest score achieved after rotating the integer array parameter `nums`, the smallest such index `k` is returned.