- The integer array parameter `firstList` must not be null.
- The integer array parameter `secondList` must not be null.
- The integer array result contains closed intervals that represent the intersection of the intervals in `firstList` and `secondList`.
- The closed intervals in the integer array result are pairwise disjoint and in sorted order.
- The closed intervals in the integer array result are represented as `[start, end]` where `start <= end`.