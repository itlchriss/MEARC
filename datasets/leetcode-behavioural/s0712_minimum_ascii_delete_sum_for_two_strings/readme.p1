- The integer result is the lowest ASCII sum of deleted characters to make the two strings `s1` and `s2` equal.
- The length of the strings `s1` and `s2` is greater than or equal to 1 and is less than or equal to 1000.
- The strings `s1` and `s2` consist of lowercase English letters.