- The string parameter `s1` and `s2` must not be null.
- The integer result is the lowest ASCII sum of deleted characters to make the two strings equal.
- The ASCII value of each deleted character must be added to the sum.
- The characters in the two strings must be deleted in such a way that both strings become equal.