- The integer parameter `k` is greater than or equal to 2 and is less than or equal to 9.
- The integer parameter `n` is greater than or equal to 1 and is less than or equal to 60.
- The integer collection result contains lists of integers representing valid combinations of `k` numbers that sum up to `n`.
- Each list in the integer collection result contains `k` unique numbers from 1 to 9.
- Each number in the list is used at most once.
- The list must not contain the same combination twice.
- The order of combinations in the list is not specified.