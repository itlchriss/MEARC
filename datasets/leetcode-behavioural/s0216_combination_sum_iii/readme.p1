- The integer parameter `k` must be between 2 and 9, inclusive.
- The integer parameter `n` must be between 1 and 60, inclusive.
- The integer collection result must contain lists of integers representing valid combinations of `k` numbers that sum up to `n`.
- Each list in the integer collection result must not contain the same combination twice.
- Each list in the integer collection result must contain `k` numbers from 1 to 9, with each number used at most once.