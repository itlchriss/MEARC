- The integer array result `answer` has the same length as the input string `s`.
- Each element in the integer array result `answer` represents the distance from the corresponding index in the input string `s` to the closest occurrence of character `c`.
- The distance between two indices `i` and `j` is calculated as the absolute difference `abs(i - j)`.
- The character `c` occurs at least once in the input string `s`.
- The input string `s` has a length greater than or equal to 1 and less than or equal to 10000.
- All characters in the input string `s` and the character `c` are lowercase English letters.