- If any value in the integer array parameter `nums` appears at least twice, the boolean result is true.
- If every element in the integer array parameter `nums` is distinct, the boolean result is false.