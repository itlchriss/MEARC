- The integer 2D array parameter `accounts` must not be null.
- The integer 2D array parameter `accounts` must have a length `m` and each sub-array within `accounts` must have a length `n`.
- The integer result is the wealth of the richest customer in the `accounts` array.
- The wealth of a customer is the sum of all the amounts in their bank accounts.
- If multiple customers have the same maximum wealth, any one of them can be considered the richest.