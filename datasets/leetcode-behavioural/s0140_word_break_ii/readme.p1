- If the string parameter `s` can be segmented into valid dictionary words using the strings in the list parameter `wordDict`, the result is a list of strings where each string represents a valid segmentation of `s` using the words in `wordDict`.
- If the string parameter `s` cannot be segmented into valid dictionary words using the strings in the list parameter `wordDict`, the result is an empty list.