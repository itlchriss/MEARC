- The integer array parameter `costs` must have a length that is even.
- The integer array parameter `costs` must contain arrays of length 2, where the first element represents the cost of flying the person to city A and the second element represents the cost of flying the person to city B.
- The integer result is the minimum cost to fly every person to a city such that exactly `n` people arrive in each city.