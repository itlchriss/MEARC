- The integer array parameter `nums` must not be null.
- The integer array parameter `nums` may contain duplicates.
- The integer parameter `target` must exist in the integer array parameter `nums`.
- The integer result of the `pick` method is an index `i` from the integer array parameter `nums` where `nums[i] == target`.
- If there are multiple valid indices `i` where `nums[i] == target`, each index should have an equal probability of returning.