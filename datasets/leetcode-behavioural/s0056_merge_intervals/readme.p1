- The integer array parameter `intervals` must not be null.
- The integer array parameter `intervals` must have a length greater than or equal to 1 and less than or equal to 10000.
- Each element in the integer array parameter `intervals` must be an array of length 2.
- The integer values in each element of the integer array parameter `intervals` must satisfy the condition 0 <= start_i <= end_i <= 10000.
- The integer array result must contain non-overlapping intervals that cover all the intervals in the input.
- The integer array result must merge overlapping intervals into a single interval.