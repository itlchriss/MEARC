Method behavioural specifications:
- The integer parameter `n` is greater than or equal to 2 and is less than or equal to 100000.
- The length of the 2D integer array parameter `meetings` is greater than or equal to 1 and is less than or equal to 100000.
- Each element in the 2D integer array parameter `meetings` has a length of 3.
- The integer values `x_i` and `y_i` in each element of the 2D integer array parameter `meetings` are greater than or equal to 0 and are less than `n`.
- The integer values `x_i` and `y_i` in each element of the 2D integer array parameter `meetings` are not equal.
- The integer values `time_i` in each element of the 2D integer array parameter `meetings` are greater than or equal to 1 and are less than or equal to 100000.
- The integer parameter `firstPerson` is greater than or equal to 1 and is less than `n`.
- The integer result is a list of all the people that have the secret after all the meetings have taken place.
- The list may be returned in any order.