- For every index `i` in the range `0 <= i < n`, the digit at index `i` in the string `num` occurs `num[i]` times in the string `num`.
- If the condition holds true for every index `i`, the boolean result is true.
- If the condition does not hold true for any index `i`, the boolean result is false.