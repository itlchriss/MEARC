- The string parameter `s` consists only of lowercase English letters.
- The integer result is the minimum number of moves needed to make the string `s` a palindrome.
- The input will be generated such that `s` can always be converted to a palindrome.