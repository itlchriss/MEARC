Method behavioural requirements:
- The integer parameter `lo` must be greater than or equal to 1.
- The integer parameter `hi` must be greater than or equal to the integer parameter `lo`.
- The integer parameter `k` must be greater than or equal to 1 and less than or equal to the difference between the integer parameters `hi` and `lo` plus 1.
- The integer result is the k<sup>th</sup> integer in the range `[lo, hi]` sorted by the power value.