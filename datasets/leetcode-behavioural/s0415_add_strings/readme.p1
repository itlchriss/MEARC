- The string parameters `num1` and `num2` consist of only digits.
- The string parameters `num1` and `num2` don't have any leading zeros except for the zero itself.
- The length of the string parameters `num1` and `num2` is greater than or equal to 1 and is less than or equal to 10000.
- The string result is the sum of the string parameters `num1` and `num2` as a string.