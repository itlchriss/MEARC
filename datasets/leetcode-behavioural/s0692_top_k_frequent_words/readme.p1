- The integer parameter `k` is greater than or equal to 1 and is less than or equal to the number of unique words in the `words` array.
- The list of strings result is sorted by frequency from highest to lowest.
- Words with the same frequency are sorted by lexicographical order.
- The length of the list result is equal to the integer parameter `k`.
- The list result contains the `k` most frequent strings from the `words` array.