- The integer 2D array parameter `intervals` must not be null.
- The integer array parameter `intervals` must be sorted in ascending order by the first element of each interval.
- The integer array parameter `newInterval` must not be null.
- The integer array parameter `newInterval` must represent a valid interval where the first element is less than or equal to the second element.
- The integer 2D array result `intervals` after the insertion must be sorted in ascending order by the first element of each interval.
- The integer 2D array result `intervals` after the insertion must not have any overlapping intervals.