- The integer parameter `s` is a balanced string.
- The integer result is the maximum amount of split balanced strings.
- Each split balanced string contains an equal quantity of 'L' and 'R' characters.