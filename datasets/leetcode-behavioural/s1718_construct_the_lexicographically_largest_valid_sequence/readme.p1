- The integer parameter `n` is greater than or equal to 1 and is less than or equal to 20.
- The integer array result is a valid sequence that satisfies the given conditions.
- The integer array result is lexicographically largest among all valid sequences.
- The integer `1` occurs once in the sequence.
- Each integer between `2` and `n` occurs twice in the sequence.
- For every integer `i` between `2` and `n`, the distance between the two occurrences of `i` is exactly `i`.