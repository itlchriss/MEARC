Method behavioural requirements:
- The 2D integer array parameter `logs` must not be null.
- The integer parameter `k` must be greater than or equal to 1.
- The integer array result `answer` must have a size equal to the integer parameter `k`.
- For each index `j` in the integer array result `answer`, `answer[j]` must be the number of users whose UAM equals `j`.