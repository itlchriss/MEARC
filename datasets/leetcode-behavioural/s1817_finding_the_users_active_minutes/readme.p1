- The integer array parameter `logs` must not be null.
- The integer parameter `k` is greater than or equal to 1.
- The integer array result `answer` has a size of `k`.
- Each element in the integer array result `answer` represents the number of users whose UAM equals the index of that element.
- The UAM for a user is defined as the number of unique minutes in which the user performed an action.
- A minute can only be counted once for a user, even if multiple actions occur during it.