- The integer array parameter `arr` must not be null.
- The integer parameter `k` must be greater than 0 and less than or equal to `arr.length * (arr.length - 1) / 2`.
- The integer array result is of size 2, where `result[0]` is equal to `arr[i]` and `result[1]` is equal to `arr[j]`.
- The result is the `k`-th smallest fraction considered when iterating over all possible fractions `arr[i] / arr[j]` where `0 <= i < j < arr.length`.