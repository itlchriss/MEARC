- The integer parameter `k` is greater than or equal to 2 and is less than or equal to half the length of the string `s`.
- The string parameter `s` consists only of lowercase English letters.
- The integer result is the minimum number of letter changes required to partition the string `s` into `k` substrings such that each substring is a semi-palindrome.