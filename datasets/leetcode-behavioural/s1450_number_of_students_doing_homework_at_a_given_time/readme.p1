- The integer arrays `startTime` and `endTime` must have the same length.
- The length of the integer arrays `startTime` and `endTime` is greater than or equal to 1 and is less than or equal to 100.
- All values in the integer arrays `startTime` and `endTime` are greater than or equal to 1 and are less than or equal to 1000.
- The integer parameter `queryTime` is greater than or equal to 1 and is less than or equal to 1000.
- The integer result is the number of students where `queryTime` is within the interval `[startTime[i], endTime[i]]` inclusive.