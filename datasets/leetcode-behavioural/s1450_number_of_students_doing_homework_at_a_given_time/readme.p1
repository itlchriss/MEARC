- The integer array parameter `startTime` and `endTime` must not be null.
- The integer array parameter `startTime` and `endTime` must have the same length.
- The integer parameter `queryTime` must be within the range of 1 to 1000.
- The integer result is the number of students where `queryTime` is within the interval `[startTime[i], endTime[i]]` inclusive.