- The integer array parameter `position` must not be null.
- The integer array parameter `position` contains the positions of the chips.
- The integer result is the minimum cost needed to move all the chips to the same position.
- The cost of moving a chip from position `i` to `i+2` or `i-2` is 0.
- The cost of moving a chip from position `i` to `i+1` or `i-1` is 1.