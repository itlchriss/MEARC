- The integer array parameter `nums` must not be null.
- The integer array parameter `nums` must consist of distinct positive integers.
- The integer array parameter `operations` must not be null.
- The integer array parameter `operations` must have a length of `m`.
- Each element in the `operations` array must be an array of length 2.
- The first element of each sub-array in the `operations` array must exist in the `nums` array.
- The second element of each sub-array in the `operations` array must not exist in the `nums` array.
- The integer array result is obtained after applying all the operations specified in the `operations` array to the `nums` array.