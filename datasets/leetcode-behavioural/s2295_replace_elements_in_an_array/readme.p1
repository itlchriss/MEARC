- The integer array parameter `nums` must not be null.
- The integer array parameter `nums` consists of distinct positive integers.
- The integer array parameter `operations` must not be null.
- The integer array parameter `operations` consists of m operations, where each operation is represented by a pair of integers `[operations[i][0], operations[i][1]]`.
- In each operation, the first integer `operations[i][0]` exists in the `nums` array, and the second integer `operations[i][1]` does not exist in the `nums` array.
- The integer array result is obtained after applying all the operations to the `nums` array.