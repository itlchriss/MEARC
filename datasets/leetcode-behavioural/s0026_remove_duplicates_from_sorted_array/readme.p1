- The integer array parameter `nums` is sorted in non-decreasing order.
- The integer array parameter `nums` is modified in-place to remove duplicates such that each unique element appears only once.
- The relative order of the elements in the integer array parameter `nums` is kept the same.
- The first `k` elements of the integer array parameter `nums` hold the final result after removing duplicates.
- The integer result is `k` after placing the final result in the first `k` slots of the integer array parameter `nums`.
- No extra space is allocated for another array during the removal of duplicates.
- The modification of the input array `nums` is done in-place with O(1) extra memory.