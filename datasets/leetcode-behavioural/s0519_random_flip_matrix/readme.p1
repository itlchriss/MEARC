Method behavioural requirements:
- The integer parameters `nRows` and `nCols` must be greater than or equal to 1.
- The method `flip()` must return a random index `[i, j]` where `matrix[i][j] == 0` and flips it to `1`.
- The method `reset()` must reset all values of the matrix to be `0`.