- If the integer matrix parameter `mat` can be reshaped into a new matrix with `r` rows and `c` columns, the integer matrix result contains all elements of the original matrix in the same row-traversing order as they were.
- If the integer matrix parameter `mat` cannot be reshaped into a new matrix with `r` rows and `c` columns, the integer matrix result is the original matrix `mat`.