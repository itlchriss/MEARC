- The integer matrix parameter `grid` must not be null.
- The integer result is the least time until the bottom right square `(n - 1, n - 1)` can be reached starting from the top left square `(0, 0)` in the integer matrix parameter `grid`.