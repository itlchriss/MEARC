- The integer parameter `n` must be greater than or equal to 1.
- The integer array parameter `blacklist` must not be null.
- The integer array parameter `blacklist` must contain unique integers.
- The integer array parameter `blacklist` must have a length less than or equal to the minimum of 10^5 and `n - 1`.
- The integer elements in the array parameter `blacklist` must be in the range from 0 to `n - 1`.
- The integer result of the `pick()` method must be in the range from 0 to `n - 1`.
- The integer result of the `pick()` method must not be in the `blacklist`.
- The integer result of the `pick()` method must be equally likely to be any integer from the range `[0, n - 1]` that is not in the `blacklist`.