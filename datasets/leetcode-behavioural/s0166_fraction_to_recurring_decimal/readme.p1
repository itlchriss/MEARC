- The string result is the fraction in string format representing the division of the integer parameter `numerator` by the integer parameter `denominator`.
- If the fractional part is repeating, the repeating part is enclosed in parentheses.
- The length of the string result is less than 10000.
- If multiple answers are possible, any valid answer is acceptable.
- The integer parameters `numerator` and `denominator` are within the range of -2^31 to 2^31 - 1.
- The integer parameter `denominator` is not equal to 0.