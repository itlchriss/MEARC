- If the integer parameter `denominator` is not equal to 0, the string result is the fraction of the integer parameters `numerator` and `denominator` in decimal format.
- If the fractional part of the decimal representation is repeating, the repeating part is enclosed in parentheses.
- The length of the string result is guaranteed to be less than 10^4 for all given inputs.