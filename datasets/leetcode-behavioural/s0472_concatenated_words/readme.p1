- If the string array parameter `words` contains concatenated words, the list result contains all the concatenated words in the given list of words.
- A concatenated word is defined as a string that is comprised entirely of at least two shorter words in the given array.