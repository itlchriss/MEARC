- The integer matrix parameter `grid` must not be null.
- The integer matrix parameter `grid` must have a length of `m` and each row must have a length of `n`.
- The integer matrix parameter `grid` must have dimensions of at least 3x3 and at most 150x150.
- All elements in the integer matrix parameter `grid` must be greater than or equal to 0 and less than or equal to 10^6.
- The integer result is the maximum sum of the elements of an hourglass in the matrix `grid`.