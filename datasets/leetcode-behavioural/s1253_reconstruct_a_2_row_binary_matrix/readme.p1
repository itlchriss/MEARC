Method behavioural requirements:
- The integer parameter `upper` represents the sum of elements in the 0-th row.
- The integer parameter `lower` represents the sum of elements in the 1-st row.
- The integer array parameter `colsum` represents the sum of elements in each column.
- The 2-D integer array result must have 2 rows and `colsum.length` columns.
- Each element in the result array must be either 0 or 1.
- The sum of elements in each column of the result array must match the corresponding element in the `colsum` array.
- If there are multiple valid solutions, any of them can be accepted.
- If no valid solution exists, the result must be an empty 2-D array.