- The maximum number of operations needed to delete all characters in the string `s` must be returned.
- In each operation, the entire string `s` can be deleted, or the first `i` letters of `s` can be deleted if they are equal to the following `i` letters in `s`, for any `i` in the range `1 <= i <= s.length / 2`.
- The input string `s` must consist only of lowercase English letters.
- The length of the input string `s` must be between 1 and 4000, inclusive.