- The integer array parameter `a` must not be null.
- The integer array parameter `a` contains only 0s and 1s.
- The integer parameter `k` is greater than or equal to 0 and is less than or equal to the length of the integer array parameter `a`.
- The integer result is the maximum number of consecutive 1s in the integer array parameter `a` if at most `k` 0s are flipped.