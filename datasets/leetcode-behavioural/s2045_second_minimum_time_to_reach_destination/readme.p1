- The integer parameter `n` is greater than or equal to 2 and is less than or equal to 10000.
- The length of the 2D integer array parameter `edges` is greater than or equal to `n - 1` and is less than or equal to the minimum of 20000 and `n * (n - 1) / 2`.
- Each element in the 2D integer array parameter `edges` has a length of 2.
- The values in each element of the 2D integer array parameter `edges` are greater than or equal to 1 and are less than or equal to `n`.
- The values in each element of the 2D integer array parameter `edges` are not equal to each other.
- There are no duplicate edges in the 2D integer array parameter `edges`.
- Each vertex in the graph can be reached directly or indirectly from every other vertex.
- The integer parameters `time` and `change` are greater than or equal to 1 and are less than or equal to 1000.