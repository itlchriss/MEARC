Method behavioural requirements:
- The integer array parameter `target` must not be null.
- The boolean result is true if it is possible to construct the integer array parameter `target` from an array consisting of `n` 1's using the given procedure.
- The boolean result is false if it is not possible to construct the integer array parameter `target` from an array consisting of `n` 1's using the given procedure.