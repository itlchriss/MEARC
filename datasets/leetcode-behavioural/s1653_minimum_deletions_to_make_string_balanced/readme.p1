- The string parameter `s` consists only of characters 'a' and 'b'.
- The integer result is the minimum number of deletions needed to make the string `s` balanced.
- The string `s` is balanced if there is no pair of indices (i,j) such that i < j and s[i] = 'b' and s[j] = 'a'.
- The length of the string `s` is greater than or equal to 1 and is less than or equal to 100000.