- The integer matrix parameter `mat` is a 2D grid with dimensions `m x n`.
- All elements in the integer matrix parameter `mat` are unique.
- The integer matrix parameter `mat` is surrounded by an outer perimeter with the value -1 in each cell.
- The integer result is a length 2 array `[i, j]` representing a peak element in the matrix `mat`.
- The algorithm must run in `O(m log(n))` or `O(n log(m))` time.