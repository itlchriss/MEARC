Method behavioural requirements:
- The 2D integer array parameter `graph` must not be null.
- The integer array parameter `graph[u]` must not contain the integer `u`.
- The integer array parameter `graph[u]` must not contain duplicate values.
- If integer `v` is in integer array parameter `graph[u]`, then integer `u` must be in integer array parameter `graph[v]`.
- The boolean result is true if and only if the graph is bipartite.