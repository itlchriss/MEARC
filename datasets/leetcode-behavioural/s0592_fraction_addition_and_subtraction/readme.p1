- The string parameter `expression` must only contain valid characters: '0' to '9', '/', '+', and '-'.
- The fractions in the input expression must have the format ±numerator/denominator.
- If the first input fraction or the output is positive, the '+' sign must be omitted.
- Each fraction in the input and output must be an irreducible fraction.
- The numerator and denominator of each fraction in the input expression will always be in the range [1, 10].
- If the denominator of a fraction is 1, it means the fraction is actually an integer in a fraction format.
- The number of given fractions in the input expression will be in the range [1, 10].
- The numerator and denominator of the final result must be valid and within the range of a 32-bit integer.