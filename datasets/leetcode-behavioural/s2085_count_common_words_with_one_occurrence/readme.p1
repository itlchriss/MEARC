- If a string appears exactly once in both `words1` and `words2`, it is counted towards the result.
- If a string appears more than once in either `words1` or `words2`, it is not counted towards the result.
- The result is an integer representing the number of strings that appear exactly once in both `words1` and `words2`.
- The length of the string arrays `words1` and `words2` is between 1 and 1000.
- The length of each string in `words1` and `words2` is between 1 and 30 characters.
- The strings in `words1` and `words2` consist only of lowercase English letters.