- The integer matrix parameter `matrix` must not be null.
- The integer matrix parameter `matrix` must have a length `m` and each row must have a length `n`.
- The integers in each row of the integer matrix parameter `matrix` are sorted from left to right.
- The first integer of each row of the integer matrix parameter `matrix` is greater than the last integer of the previous row.
- The integer parameter `target` is within the range of -10000 to 10000.
- The boolean result is true if the integer parameter `target` is found in the integer matrix parameter `matrix`, otherwise false.