- The integer array parameter `changed` must not be null.
- The integer array result is equal to the original array `original` if the `changed` array is a doubled array.
- The integer array result is an empty array if the `changed` array is not a doubled array.
- The elements in the original array `original` may be returned in any order.