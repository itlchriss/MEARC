- The string parameter `boxes` must not be null.
- The integer array result has a size equal to the length of the string parameter `boxes`.
- For each index `i` in the integer array result, the integer result at index `i` is the minimum number of operations needed to move all the balls to the `i`-th box considering the initial state of the boxes.