Method behavioural requirements:
- The integer parameter `batchSize` must be between 1 and 9, inclusive.
- The integer array parameter `groups` must have a length between 1 and 30, inclusive.
- Each element in the integer array parameter `groups` must be between 1 and 10^9, inclusive.
- The integer result is the maximum possible number of happy groups after rearranging the groups.