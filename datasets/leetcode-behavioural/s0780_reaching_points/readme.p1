- If the integer parameters `sx` and `sy` are equal to the integer parameters `tx` and `ty`, the boolean result is equal to the true literal.
- If the integer parameters `sx` and `sy` are not equal to the integer parameters `tx` and `ty`, the boolean result is equal to the false literal.
- If the integer parameters `sx` and `sy` can be converted to the integer parameters `tx` and `ty` through a series of moves where each move transforms the point `(x, y)` to either `(x, x + y)` or `(x + y, y)`, the boolean result is equal to the true literal.