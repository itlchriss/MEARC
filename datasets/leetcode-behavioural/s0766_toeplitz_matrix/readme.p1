- For the integer 2D array parameter `matrix`, if every diagonal from top-left to bottom-right has the same elements, the boolean result is true.
- For the integer 2D array parameter `matrix`, if any diagonal from top-left to bottom-right has different elements, the boolean result is false.