- The integer array parameter `p1` must have a length of 2.
- The integer array parameter `p2` must have a length of 2.
- The integer array parameter `p3` must have a length of 2.
- The integer array parameter `p4` must have a length of 2.
- The boolean result is true if the four points `p1`, `p2`, `p3`, and `p4` construct a square with four equal sides and four equal 90-degree angles.
- The boolean result is false if the four points `p1`, `p2`, `p3`, and `p4` do not construct a square.