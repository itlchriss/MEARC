- The integer array parameters `p1`, `p2`, `p3`, and `p4` must have a length of 2.
- The integer array parameters `p1`, `p2`, `p3`, and `p4` must represent the coordinates of points in 2D space.
- The boolean result is equal to the true literal if the four points construct a square with four equal sides and four equal 90-degree angles.
- The boolean result is equal to the false literal if the four points do not construct a square.