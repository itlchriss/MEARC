- The integer matrix parameter `grid` must not be null.
- The integer matrix parameter `grid` must have a length `m` and each row in the matrix must have a length `n`.
- The integer matrix parameter `grid` must contain values greater than or equal to 1 and less than or equal to 100000.
- The integer result is the number of strictly increasing paths in the grid.
- The integer result must be modulo 10^9 + 7.