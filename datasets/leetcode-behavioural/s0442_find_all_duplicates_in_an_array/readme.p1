- The integer array parameter `nums` contains integers in the range `[1, n]`.
- Each integer in the integer array parameter `nums` appears once or twice.
- The length of the integer array parameter `nums` is equal to `n`.
- The integer collection result contains integers that appear twice in the integer array parameter `nums`.
- The algorithm runs in `O(n)` time.
- The algorithm uses only constant extra space.