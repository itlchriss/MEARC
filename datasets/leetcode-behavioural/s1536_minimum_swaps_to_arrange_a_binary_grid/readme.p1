- If the integer 2D array parameter `grid` is already valid (all cells above the main diagonal are zeros), the integer result is 0.
- If it is possible to make the integer 2D array parameter `grid` valid by swapping adjacent rows, the integer result is the minimum number of steps needed to make the grid valid.
- If it is not possible to make the integer 2D array parameter `grid` valid by swapping adjacent rows, the integer result is -1.