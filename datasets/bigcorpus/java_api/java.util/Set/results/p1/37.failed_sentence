The code provided can be utilized to empty the set into a freshly allocated array of String: String[] y = x.toArray(new String[0]); It should be noted that toArray(new Object[0]) performs the same function as toArray().