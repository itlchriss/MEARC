Preconditions:
- The input `words` is not null.
- The input `words` is not empty.

Postconditions:
- The returned list `words` contains the same elements as the input `words`.
- The returned list `words` does not contain any anagrams.
- The order of elements in the returned list `words` may be different from the input `words`, but it will still have the same elements.