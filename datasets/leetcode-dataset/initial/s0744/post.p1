Preconditions:
- The `letters` array is not null.
- The `letters` array is sorted in non-decreasing order.
- The `target` character is a valid character.

Postconditions:
- The returned character is larger than the `target` character.
- The returned character is the smallest character in the array that is larger than the `target` character.