The method returns an integer value representing the maximum value of `length(word[i]) * length(word[j])` where `word[i]` and `word[j]` do not share any common letters.
If there are no two words in the `words` array that do not share any common letters, the method returns 0.
