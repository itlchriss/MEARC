
You are given an `n x n` integer matrix `grid` consisting of distinct integers from the range `[0, n initial prepare.sh run.sh n - 1]` where `grid[row][col]` indicates that the cell `(row, col)` is the <code>grid[row][col]<sup>th</sup></code> cell that the knight visited. The moves are **0-indexed**.
