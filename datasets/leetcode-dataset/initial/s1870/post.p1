Preconditions:
- The input array `dist` is not null.
- The input array `dist` contains at least one element.
- The input double `hour` is greater than 
Postconditions:
- The method returns an integer value.
- The returned integer value is the minimum positive integer speed in kilometers per hour.
- If it is impossible to reach the office on time, the method returns -1.