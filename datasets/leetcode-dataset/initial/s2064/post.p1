Preconditions:
- The input parameter `n` must be a positive integer.
- The input array `q` must not be null and must contain at least one element.

Postconditions:
- The returned value `x` must be an integer.
- The returned value `x` must be the minimum possible value.