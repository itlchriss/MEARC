The `public int next(int n)` method should return the last element exhausted after exhausting `n` elements from the sequence.
If there are no elements left to exhaust, the `public int next(int n)` method should return `-1`.
After calling the `public int next(int n)` method, the remaining sequence should be updated accordingly.
If the last term exhausted does not exist, the `public int next(int n)` method should return `-1`.
