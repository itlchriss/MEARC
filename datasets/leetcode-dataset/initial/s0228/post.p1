Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.

Postconditions:
- The returned list is not null.
- The returned list is sorted in ascending order.
- The returned list covers all the numbers in the input array `nums` exactly.
- Each element of the input array `nums` is covered by exactly one range in the returned list.
- There is no integer `x` such that `x` is in one of the ranges in the returned list but not in the input array `nums`.