The returned queue `queue` is a 2D array.
Each element in the returned queue `queue` is an array of length - The first element of each subarray in the returned queue `queue` represents the height of a person.
The second element of each subarray in the returned queue `queue` represents the number of people in front of the person.
The order of the elements in the returned queue `queue` represents the correct order of the people in the queue.
