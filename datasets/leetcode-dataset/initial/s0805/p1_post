The method should return `true` if it is possible to split the array into two subarrays with the same average.
The method should return `false` if it is not possible to split the array into two subarrays with the same average.
