Preconditions:
- The input array `nums` is not null.
- The input array `nums` contains at least two elements.

Postconditions:
- The method returns an integer value representing the number of good pairs in the input array.
- A good pair is defined as a pair of elements (nums[i], nums[j]) where i < j and nums[i] == nums[j].
- The method does not modify the input array `nums`.