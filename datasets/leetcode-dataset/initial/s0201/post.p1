Preconditions:
The method should take two integer parameters `left` and `right`.
The value of `left` should be less than or equal to the value of `right`.

Postconditions:
The method should return an integer value representing the bitwise AND of all numbers in the range `[left, right]`.
The returned value should be inclusive of both `left` and `right`.