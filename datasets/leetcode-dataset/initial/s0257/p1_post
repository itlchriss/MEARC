The method returns a list of strings representing all root-to-leaf paths in the binary tree.
The order of the paths in the returned list can be in any order.
Each path in the returned list is represented as a string.
Each path in the returned list starts from the `root` node and ends at a leaf node.
Each path in the returned list includes all the nodes along the path from the `root` to the leaf node.
Each path in the returned list is represented as a sequence of node values separated by "->" symbols.
