Given a **0-indexed** 2D integer matrix `grid` of size `n initial prepare.sh run.sh m`, we define a **0-indexed** 2D matrix `p` of size `n initial prepare.sh run.sh m` as the **product** matrix of `grid` if the following condition is met:

