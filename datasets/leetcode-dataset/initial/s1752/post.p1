Preconditions:
- The input array `nums` is not null.

Postconditions:
- The method returns `true` if the array `nums` was originally sorted in non-decreasing order and then rotated some number of positions (including zero).
- The method returns `false` if the array `nums` was not originally sorted in non-decreasing order and then rotated some number of positions (including zero).