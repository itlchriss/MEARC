Preconditions:
- The `encoded` array is not null.
- The `encoded` array contains at least one element.

Postconditions:
- The returned array `perm` is not null.
- The length of the returned array `perm` is equal to the length of the `encoded` array plus one.
- The elements in the returned array `perm` are unique.
- The elements in the returned array `perm` are a permutation of the original array.