initial prepare.sh run.sh `int get(int key)` returns the `value` to which the specified `key` is mapped, or `-1` if this map contains no mapping for the `key`. myHashMap.get(1); // return 1, The map is now [[1,1], [2,2]] myHashMap.get(3); // return -1 (i.e., not found), The map is now [[1,1], [2,2]] myHashMap.get(2); // return 1, The map is now [[1,1], [2,1]] myHashMap.get(2); // return -1 (i.e., not found), The map is now [[1,1]]

