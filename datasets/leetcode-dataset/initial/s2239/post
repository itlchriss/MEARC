Given an integer array `nums` of size `n`, return _the number with the value **closest** to_ `0` _in_ `nums`. If there are multiple answers, return _the number with the **largest** value_. **Explanation:** 1 and -1 are both the closest numbers to 0, so 1 being larger is returned.

