Preconditions:
- The `nums` array is not null.
- The `nums` array is not empty.
- The `nums` array is sorted in non-decreasing order.

Postconditions:
- If `target` is found in the `nums` array, return an array containing the starting and ending indices of the range of `target` in the `nums` array.
- If `target` is not found in the `nums` array, return `[-1, -1]`.