The method `allPossibleFBT` returns a list of `TreeNode` objects.
Each `TreeNode` object in the returned list represents the root node of a full binary tree.
Each `TreeNode` object in the returned list has `Node.val == 0`.
The returned list contains all possible full binary trees with `n` nodes.
The order of the trees in the returned list can be arbitrary.
