Preconditions:
- The input array barcodes is not null.
- The input array barcodes is not empty.

Postconditions:
- The returned array is not null.
- The returned array has the same length as the input array barcodes.
- The returned array contains the rearranged barcodes such that no two adjacent barcodes are equal.