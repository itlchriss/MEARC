Preconditions:
- The arrays `nums1` and `nums2` are not null.
- The arrays `nums1` and `nums2` have at least one element.

Postconditions:
- The method `FindSumPairs` initializes the arrays `nums1` and `nums2` with the given input arrays.
- The method `add` adds the element `val` at the specified `index` in the array `nums2`.
- The method `count` returns the number of pairs `(i, j)` such that `nums1[i] + nums2[j] == tot`.