Given three integers `a`, `b`, and `n`, return _the **maximum value** of_ `(a XOR x) initial prepare.sh run.sh (b XOR x)` _where_ <code>0 <= x < 2<sup>n</sup></code>. Since the answer may be too large, return it **modulo** <code>10<sup>9</sup> + 7</code>.

