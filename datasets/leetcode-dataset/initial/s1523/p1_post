The method should return an integer value representing the count of odd numbers between `low` and `high` (inclusive).
If there are no odd numbers between `low` and `high`, the method should return 0.
