The method should return a string representation of the fraction.
If the fraction is a whole number, the string representation should not include a decimal point.
If the fraction is a proper fraction, the string representation should include a decimal point.
If the fraction is an improper fraction, the string representation should include both the whole number part and the decimal part.
If multiple answers are possible, the method can return any of them.
