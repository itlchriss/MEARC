Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is greater than or equal to 
Postconditions:
- The method returns the maximum value over all triplets of indices `(i, j, k)` such that `i < j < k`.
- If all such triplets have a negative value, the method returns 0.