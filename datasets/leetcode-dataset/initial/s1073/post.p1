Preconditions:
- The input arrays `arr1` and `arr2` must not be null.
- The input arrays `arr1` and `arr2` must only contain 0s and 1s.

Postconditions:
- The method should return an array of 0s and 1s representing the sum of `arr1` and `arr2`.
- The returned array should have no leading zeros.