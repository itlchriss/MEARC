//@ requires(0 <= nums1.length <= 1000);
//@ requires(0 <= nums2.length <= 1000);
//@ requires(1 <= nums1.length + nums2.length <= 2000);
//@ requires(\forall int i; 0 <= i < nums1.length; nums1[i] >= -1000000);
//@ requires(\forall int i; 0 <= i < nums2.length; nums2[i] <= 1000000);