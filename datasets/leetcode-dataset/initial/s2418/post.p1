Preconditions:
- The `names` array should not be null.
- The `heights` array should not be null.
- The length of the `names` array should be equal to the length of the `heights` array.

Postconditions:
- The returned `names` array should be sorted in descending order based on the corresponding heights in the `heights` array.