Preconditions:
The method should take two string parameters, `text1` and `text2`.
The strings `text1` and `text2` should not be null.

Postconditions:
The method should return an integer value representing the length of the longest common subsequence.
If there is no common subsequence between `text1` and `text2`, the method should return 0.