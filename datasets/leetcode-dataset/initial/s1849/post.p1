Preconditions:
The input string `s` must not be null.
The input string `s` must not be empty.

Postconditions:
If it is possible to split the string `s` as described above, the method should return `true`.
If it is not possible to split the string `s` as described above, the method should return `false`.