Preconditions:
- The input strings `s1` and `s2` must have the same length.

Postconditions:
- The method should return `true` if `s2` is a scrambled string of `s1`.
- The method should return `false` if `s2` is not a scrambled string of `s1`.