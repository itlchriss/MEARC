The method returns a list of lists, where each inner list represents a level in the binary tree.
The values of the nodes in each level are ordered from left to right.
The order of the levels in the returned list is from top to bottom.
The returned list should be empty if the `root` is null.
