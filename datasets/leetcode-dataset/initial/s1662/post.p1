If the two string arrays `word1` and `word2` represent the same string, the method `arrayStringsAreEqual` should return `true`. Otherwise, if the arrays do not represent the same string, the method should return `false`. The method should only return `true` if the strings in `word1` and `word2` are identical.
