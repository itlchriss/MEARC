Preconditions:
- The `times` array is not null.
- The `times` array has a length greater than - The `n` value is greater than - The `k` value is a valid node index within the `times` array.

Postconditions:
- The method returns an integer value representing the time it takes for all `n` nodes to receive the signal.
- If it is impossible for all `n` nodes to receive the signal, the method returns -1.