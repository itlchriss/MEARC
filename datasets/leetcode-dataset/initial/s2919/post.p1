Preconditions:
- The input array `nums` is not null.
- The input array `nums` contains at least one element.
- The input array `nums` contains only non-negative integers.
- The input integer `k` is greater than or equal to 
Postconditions:
- The method returns an integer denoting the minimum number of increment operations needed to make `nums` beautiful.