The product names should be suggested from the `products` list by the system.After each character of `searchWord` is typed, the suggestions should be made.The products in the parameter array should have a common prefix with the searchWord.If the parameter array has more than three products with a common prefix, the system should return the three lexicographically minimum products.After each character of `searchWord` is typed, a list of lists of the suggested products should be returned by the system.