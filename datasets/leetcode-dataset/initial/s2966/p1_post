The method should return a 2D array.
The returned 2D array should contain arrays that satisfy the conditions.
If it is impossible to satisfy the conditions, the method should return an empty array.
If there are multiple answers, the method can return any of them.
