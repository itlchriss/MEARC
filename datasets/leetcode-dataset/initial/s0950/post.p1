Preconditions:
- The input deck array must not be null.
- The input deck array must not be empty.
- The input deck array must contain only positive integers.

Postconditions:
- The returned array must have the same length as the input deck array.
- The returned array must contain all the elements from the input deck array.
- The returned array must be in increasing order.