When an integer `rowIndex` is provided, this algorithm returns the `rowIndexth` row of the **Pascal's triangle** as a list of integers. The `rowIndex` is 0-indexed, meaning that the first row is represented by `rowIndex = 0`.
