The method should return a List of Integers representing the `rowIndexth` row of Pascal's triangle.
The List should contain `rowIndex + 1` elements.
The first element of the List should be - Each subsequent element in the List should be the sum of the two elements directly above it in the previous row of Pascal's triangle.
