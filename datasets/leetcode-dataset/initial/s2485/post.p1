Preconditions:
- The input integer `n` must be a valid integer.

Postconditions:
- The method should return an integer value.
- If there is a pivot integer `x` in the input, the method should return that integer.
- If there is no pivot integer in the input, the method should return -1.