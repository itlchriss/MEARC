Preconditions:
- The input array `nums` is not null.
- The input array `nums` has at least one element.

Postconditions:
- The returned array `result` has the same length as the input array `nums`.
- Each element `result[i]` in the returned array `result` is equal to the summation of absolute differences between `nums[i]` and all the other elements in the array.