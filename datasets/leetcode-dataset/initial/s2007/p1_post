If `changed` is a doubled array, the method should return an array `original` that is the original array before doubling.
If `changed` is not a doubled array, the method should return an empty array.
The elements in the returned array `original` may be in any order.
