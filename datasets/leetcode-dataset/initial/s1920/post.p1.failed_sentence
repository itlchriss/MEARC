Construct an array `ans` of the same length as the zero-based permutation `nums` (0-indexed) in the Java method `buildArray(int[] nums)`. Each element `ans[i]` in the constructed array should be equal to `nums[nums[i]]` for every 0 <= i < nums.length. Finally, return the constructed array `ans`.
