If there is no way to reach the target, the method should return -If the target position is already occupied by an obstacle, the method should return -If the target position is the starting position, the method should return If the target position is reachable from the starting position, the method should return the minimum number of moves required to reach the target.
