Preconditions:
- The input array nums is not null.
- The input array nums contains at least one element.

Postconditions:
- The returned array is not null.
- The returned array contains the same elements as the input array nums.
- The returned array satisfies the condition of having all even elements appear before all odd elements.