The method should return an integer value representing the largest possible overlap between img1 and imgThe returned value should be non-negative.
If there is no overlap between img1 and img2, the method should return 0.
