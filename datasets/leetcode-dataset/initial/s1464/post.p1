Preconditions:
- The input array `nums` must not be null.
- The input array `nums` must have at least two elements.

Postconditions:
- The method should return an integer value.
- The returned value should be the maximum value of `(nums[i]-1)*(nums[j]-1)`, where `i` and `j` are different indices of the input array `nums`.