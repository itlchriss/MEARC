Preconditions:
- The input array `nums` is not null.
- The input array `nums` has at least two elements.

Postconditions:
- The method returns an integer value.
- The returned value is the maximum value of `(nums[i]-1)*(nums[j]-1)`, where `i` and `j` are different indices of the input array `nums`.