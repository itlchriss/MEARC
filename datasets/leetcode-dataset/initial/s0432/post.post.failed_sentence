Design a data structure to store the strings' count with the ability to return the strings with minimum and maximum counts. initial prepare.sh run.sh `getMaxKey()` Returns one of the keys with the maximal count. If no element exists, return an empty string `""`. initial prepare.sh run.sh `getMinKey()` Returns one of the keys with the minimum count. If no element exists, return an empty string `""`. allOne.getMaxKey(); // return "hello" allOne.getMinKey(); // return "hello" allOne.getMaxKey(); // return "hello" allOne.getMinKey(); // return "leet"
initial prepare.sh run.sh `getMaxKey()` Returns one of the keys with the maximal count. If no element exists, return an empty string `""`. initial prepare.sh run.sh `getMinKey()` Returns one of the keys with the minimum count. If no element exists, return an empty string `""`.
