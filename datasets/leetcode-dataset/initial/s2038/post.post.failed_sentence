Assuming Alice and Bob play optimally, return `true` _if Alice wins, or return_ `false` _if Bob wins_. Thus, Alice wins, so return true. Thus, Bob wins, so return false. Thus, Bob wins, so return false.

