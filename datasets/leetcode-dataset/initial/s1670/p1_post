The queue is modified after each push operation.
The queue is modified after each pop operation.
The popFront() method returns the front element of the queue and removes it. If the queue is empty, it returns -- The popMiddle() method returns the middle element of the queue and removes it. If the queue is empty, it returns -- The popBack() method returns the back element of the queue and removes it. If the queue is empty, it returns -- Popping the middle from [1, 2, 3, 4, 5, 6] returns 3 and results in [1, 2, 4, 5, 6].
After popping the front element, the queue becomes [4, 3, 2].
After popping the middle element, the queue becomes [4, 2].
After popping the middle element again, the queue becomes [2].
After popping the back element, the queue becomes empty ([]).
If the queue is empty and popFront() is called, it returns -1 and the queue remains empty ([]).
