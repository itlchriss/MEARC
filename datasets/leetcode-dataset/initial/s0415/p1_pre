The method should take two non-negative integers represented as strings, `num1` and `num2`, as input.
The input strings should only contain numeric characters (0-9).
The input strings should not be empty.
