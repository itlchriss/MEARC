The method returns a list of lists, where each list represents a root-to-leaf path.
Each path in the returned list has a sum of node values equal to the `targetSum`.
Each path in the returned list is represented as a list of node values, not node references.
