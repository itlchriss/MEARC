Preconditions:
The `root` of the binary tree is not null.
The binary tree has unique values.
The values of `x` and `y` are different.
The values of `x` and `y` exist in the binary tree.

Postconditions:
If the nodes corresponding to the values `x` and `y` in the tree are cousins, return `true`.
If the nodes corresponding to the values `x` and `y` in the tree are not cousins, return `false`.