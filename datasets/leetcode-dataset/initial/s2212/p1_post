The method should return an array of integers `bobArrows`.
The `bobArrows` array should have a length of 12, with each element representing the number of arrows Bob shot on the corresponding scoring section.
The sum of the values in the `bobArrows` array should equal `numArrows`.
