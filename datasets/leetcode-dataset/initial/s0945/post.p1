Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.

Postconditions:
- The returned value is an integer representing the minimum number of moves required to make every value in `nums` unique.
- After the method is executed, every value in `nums` is unique.