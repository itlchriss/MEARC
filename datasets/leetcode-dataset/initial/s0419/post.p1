Preconditions:
The input matrix `board` is not null.
The input matrix `board` has at least one row and one column.
The input matrix `board` contains only the characters `'X'` and `'.'`.

Postconditions:
The method returns an integer value representing the number of battleships on the board.
The input matrix `board` remains unchanged after the method is executed.