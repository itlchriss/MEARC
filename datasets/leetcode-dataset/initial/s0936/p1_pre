The method takes two string parameters, `stamp` and `target`.
The length of `stamp` is less than or equal to the length of `target`.
The length of `target` is greater than The method is called within 10 turns.
