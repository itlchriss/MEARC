Preconditions:
- The input array `points` is not null.
- The input array `points` is not empty.
- The length of each subarray in `points` is - The integer `k` is greater than - The integer `k` is less than or equal to the length of `points`.

Postconditions:
- The returned array `result` is not null.
- The length of the returned array `result` is equal to `k`.
- Each subarray in the returned array `result` has a length of - Each subarray in the returned array `result` represents a point on the X-Y plane.
- The returned array `result` contains the `k` closest points to the origin (0, 0).
- The order of the points in the returned array `result` is not specified.