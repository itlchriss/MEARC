Preconditions:
- The input `words` is not null.
- The input `words` is an array of strings.

Postconditions:
- If it is possible to make every string in `words` equal to "abc" using any number of operations, the method should return `true`.
- If it is not possible to make every string in `words` equal to "abc" using any number of operations, the method should return `false`.