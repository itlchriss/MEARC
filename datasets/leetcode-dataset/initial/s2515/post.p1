Preconditions:
- The input array `words` is not null.
- The input string `target` is not null.
- The input integer `startIndex` is a valid index within the `words` array.

Postconditions:
- The method returns an integer value representing the shortest distance needed to reach the string `target`.
- If the string `target` does not exist in the `words` array, the method returns -1.