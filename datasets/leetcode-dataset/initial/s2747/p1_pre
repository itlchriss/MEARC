The input integer `n` represents the total number of servers.
The input 2D array `logs` represents the logs of server requests, where each row represents a request and the first element is the server number and the second element is the timestamp of the request.
The input integer `x` represents the time interval.
The input integer array `qs` represents the queries, where each element is a timestamp.
