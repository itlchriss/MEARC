The input `tickets` is a non-null list of lists, where each inner list contains two strings representing the departure and arrival airports of a flight.
The departure airport for all tickets is "JFK".
