The method should return an integer value representing the maximum value of `F(0), F(1), ..., F(n-1)`.
The returned value should be the maximum value obtained by rotating the array `nums` and calculating the corresponding function `F` for each rotation.
