The method should return a 2D integer array representing the intersection of the two input interval lists.
The returned intersection array should contain intervals that are common to both input lists.
If there is no intersection between the two input lists, the method should return an empty 2D integer array.
