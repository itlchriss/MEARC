Preconditions:
The method should take two string parameters, `s` and `part`.
The string `s` should not be null.
The string `part` should not be null.

Postconditions:
The method should return a string after removing all occurrences of `part` from `s`.
The returned string should not contain any occurrences of `part`.
The returned string should have the same length or a shorter length than the original string `s`.
The order of the characters in the returned string should be the same as the original string `s`, except for the removed occurrences of `part`.