Preconditions:
- The `sentence` parameter must not be null.
- The `searchWord` parameter must not be null.

Postconditions:
- The method should return an integer representing the index of the word in the sentence where the searchWord is a prefix.
- If the searchWord is a prefix of more than one word, the method should return the index of the first word (minimum index).
- If there is no word in the sentence where the searchWord is a prefix, the method should return -1.