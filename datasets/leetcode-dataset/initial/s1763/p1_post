The returned substring is a nice substring of `s`.
The returned substring is the longest nice substring of `s`.
If there are multiple longest nice substrings, the returned substring is the one that occurs earliest in `s`.
If there are no nice substrings in `s`, the returned string is empty.
