Preconditions:
- The input array `words` is not null.
- The input integer `k` is greater than - The input array `words` contains at least `k` unique strings.

Postconditions:
- The method returns a list of strings.
- The list contains `k` most frequent strings from the input array `words`.
- The list is sorted by the frequency of the strings, from highest to lowest.
- If two strings have the same frequency, they are sorted lexicographically.