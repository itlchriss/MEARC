The `answer` array is returned.
The length of the `answer` array is the same as the length of the `queries` array.
Each element in the `answer` array is the answer to the corresponding query in the `queries` array.
