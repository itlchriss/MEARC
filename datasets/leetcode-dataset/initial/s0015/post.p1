Preconditions:
The input array `nums` must not be null.
The input array `nums` must have a length greater than or equal to 
Postconditions:
The method should return a list of lists, where each inner list represents a triplet of integers.
Each triplet in the result list should satisfy the following conditions:
   a. The triplet should have three distinct indices `i`, `j`, and `k` such that `i != j`, `i != k`, and `j != k`.
   b. The sum of the three integers in the triplet should be equal to The result list should not contain any duplicate triplets.