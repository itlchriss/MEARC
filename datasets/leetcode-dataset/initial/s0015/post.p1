Preconditions:
The input array `nums` must not be null.
The input array `nums` must have a length greater than or equal to 
Postconditions:
The method should return a list of lists, where each inner list represents a triplet of integers that satisfy the given conditions.
Each triplet `[nums[i], nums[j], nums[k]]` in the returned list should have distinct indices `i`, `j`, and `k`.
The sum of the integers in each triplet `[nums[i], nums[j], nums[k]]` should be equal to 0.