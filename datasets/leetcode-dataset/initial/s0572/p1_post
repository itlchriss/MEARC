The method should return a boolean value indicating whether there is a subtree of `root` with the same structure and node values of `subRoot`.
If there is a subtree with the same structure and node values, the method should return `true`.
If there is no subtree with the same structure and node values, the method should return `false`.
