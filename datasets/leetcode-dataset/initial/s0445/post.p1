Preconditions:
The input linked lists l1 and l2 are non-empty.
Each node in the linked lists contains a single digit.
The most significant digit comes first in both linked lists.

Postconditions:
The method returns a linked list representing the sum of the two input linked lists.
The sum linked list contains the digits of the sum in the same order as the input linked lists.
The sum linked list does not have any leading zeros.
If the sum of the two numbers results in a carry, the carry is propagated to the next digit in the sum linked list.