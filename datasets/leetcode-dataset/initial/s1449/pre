Given an array of integers `cost` and an integer `target`, return _the **maximum** integer you can paint under the following rules_:
initial prepare.sh run.sh The cost of painting a digit `(i + 1)` is given by `cost[i]` (**0-indexed**). Since the answer may be very large, return it as a string. If there is no way to paint any integer given the condition, return `"0"`.
