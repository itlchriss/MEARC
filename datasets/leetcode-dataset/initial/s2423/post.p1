Preconditions:
- The input `word` is a non-null string.

Postconditions:
- The method returns `true` if it is possible to remove one letter from `word` so that the frequency of all letters in `word` are equal.
- The method returns `false` if it is not possible to remove one letter from `word` so that the frequency of all letters in `word` are equal.