Preconditions:
- The method takes in two parameters `headA` and `headB`, which are the heads of two singly linked-lists.
- The linked lists must retain their original structure after the function returns.

Postconditions:
- If the two linked lists have no intersection at all, the method should return `null`.
- If the two linked lists intersect, the method should return the node at which the two lists intersect.