Preconditions:
- The input array `edges` is not null.
- The input array `edges` contains at least one element.

Postconditions:
- The method returns an integer representing the node with the highest edge score.
- If multiple nodes have the same edge score, the method returns the node with the smallest index.