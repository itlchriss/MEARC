Return _the node with the highest **edge score**_. If multiple nodes have the same **edge score**, return the node with the **smallest** index. Node 7 has the highest edge score so return 7. Nodes 0 and 2 both have an edge score of 3. Since node 0 has a smaller index, we return 0.
Return _the node with the highest **edge score**_. If multiple nodes have the same **edge score**, return the node with the **smallest** index.
