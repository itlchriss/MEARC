// So, we return 0. server.longest(); // The prefix [1] is the longest uploaded prefix, so we return 1. server.longest(); // The prefix [1,2,3] is the longest uploaded prefix, so we return 3.
initial prepare.sh run.sh `int longest()` Returns the length of the **longest uploaded prefix** defined above.
