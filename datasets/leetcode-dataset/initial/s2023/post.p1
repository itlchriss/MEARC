Preconditions:
- The input array `nums` is not null.
- The input array `nums` contains only digit strings.
- The input string `target` is not null.
- The input string `target` is a digit string.

Postconditions:
- The method returns an integer value representing the number of pairs of indices `(i, j)` where `i != j` and the concatenation of `nums[i] + nums[j]` equals `target`.