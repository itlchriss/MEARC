Preconditions:
- The input array `bloomDay` must not be null.
- The length of the input array `bloomDay` must be greater than or equal to `m * k`.
- The values in the input array `bloomDay` must be non-negative integers.

Postconditions:
- The method should return an integer representing the minimum number of days needed to make `m` bouquets.
- If it is impossible to make `m` bouquets, the method should return -1.