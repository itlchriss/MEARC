If the array `stoneValue` represents the scores of Alice and Bob in a stone game, the algorithm should return the string `str_Alice` if Alice will win, the string `"Bob"` if Bob will win, or the string `"Tie"` if Alice and Bob will end the game with the same score.
