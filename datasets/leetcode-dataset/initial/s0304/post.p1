Preconditions:
- The `matrix` parameter passed to the `NumMatrix` constructor must be a valid 2D integer array.
- The `row1`, `col1`, `row2`, and `col2` parameters passed to the `sumRegion` method must be valid indices within the `matrix` array.

Postconditions:
- The `sumRegion` method should return the sum of the elements of the `matrix` array inside the rectangle defined by the given upper left and lower right corners.