The `sumRegion` method should return the sum of the elements of the `matrix` array inside the rectangle defined by the given upper left and lower right corners.
The `sumRegion` method should return 8 when called with the parameters (2, 1, 4, 3).
The `sumRegion` method should return 11 when called with the parameters (1, 1, 2, 2).
The `sumRegion` method should return 12 when called with the parameters (1, 2, 2, 4).
