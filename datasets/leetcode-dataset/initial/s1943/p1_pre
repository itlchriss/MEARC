The input parameter `segments` is a 2D array of integers.
The elements in `segments` represent segments of a painting.
Each segment is represented by a 2-element array, where the first element is the starting point of the segment and the second element is the ending point of the segment.
The starting point of a segment is inclusive and the ending point is exclusive.
The segments in `segments` may be in any order.
