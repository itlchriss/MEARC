Preconditions:
The `scores` array and `ages` array must have the same length.
The `scores` array and `ages` array must not be empty.
Each element in the `scores` array must be a non-negative integer.
Each element in the `ages` array must be a positive integer.

Postconditions:
The method returns an integer representing the highest overall score of all possible basketball teams.
The highest overall score is calculated by summing the scores of players in each team.
Each team must consist of players with consecutive ages.
Each team must have at least one player.
The highest overall score is the maximum sum of scores among all possible teams.