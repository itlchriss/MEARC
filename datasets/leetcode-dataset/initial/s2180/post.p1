Preconditions:
The input `num` must be a positive integer.

Postconditions:
The method should return an integer value representing the number of positive integers less than or equal to `num` whose digit sums are even.