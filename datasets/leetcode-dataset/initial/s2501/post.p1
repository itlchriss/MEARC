Preconditions:
- The input array `nums` must not be null.

Postconditions:
- The method should return an integer value representing the length of the longest square streak in the `nums` array.
- If there is no square streak in the `nums` array, the method should return -1.