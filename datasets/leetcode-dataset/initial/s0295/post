initial prepare.sh run.sh `double findMedian()` returns the median of all elements so far. Answers within <code>10<sup>-5</sup></code> of the actual answer will be accepted. medianFinder.findMedian(); // return 1.5 (i.e., (1 + 2) / 2) medianFinder.findMedian(); // return 2.0

