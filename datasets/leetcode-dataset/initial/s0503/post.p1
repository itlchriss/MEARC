Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.

Postconditions:
- The method returns an array of integers.
- The length of the returned array is the same as the length of the input array `nums`.
- For each element `x` in the input array `nums`, the corresponding element in the returned array is the next greater number of `x` in the array.
- If the next greater number does not exist for an element `x`, the corresponding element in the returned array is -1.