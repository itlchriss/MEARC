Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.

Postconditions:
- The method returns an array of integers.
- The length of the returned array is the same as the length of the input array `nums`.
- For each element `x` in the input array `nums`, the corresponding element in the returned array is the next greater number to `x` in the array.
- If there is no next greater number for an element `x`, the corresponding element in the returned array is -1.