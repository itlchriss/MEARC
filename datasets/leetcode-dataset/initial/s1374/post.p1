Preconditions:
- The input `n` must be a positive integer.

Postconditions:
- The returned string must have a length of `n`.
- The returned string must consist of only lowercase English letters.
- Each character in the returned string must occur an odd number of times.