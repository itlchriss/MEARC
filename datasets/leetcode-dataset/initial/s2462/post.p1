Determine the overall expense for employing precisely `k` workers by implementing an algorithm that takes an array of `costs`, an integer `k`, and an integer array `candidates` as input and returns a `long` value as the result.
