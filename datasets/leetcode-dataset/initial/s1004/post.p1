Preconditions:
- The input array `a` is not null.
- The input array `a` contains only binary values (0's and 1's).
- The input integer `k` is non-negative.

Postconditions:
- The method returns an integer representing the maximum number of consecutive 1's in the array `a` after flipping at most `k` 0's.
- The original array `a` remains unchanged after the method execution.