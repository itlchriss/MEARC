Preconditions:
- The input array `nums` must not be null.
- The input array `nums` must have at least two elements.

Postconditions:
- The method should return an integer value representing the maximum difference between any two elements in the input array `nums`.
- If there is no pair of elements `i` and `j` in the input array `nums` such that `nums[i] - nums[j]` is the maximum difference, the method should return -1.