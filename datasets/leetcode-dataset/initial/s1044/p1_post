The method should return a string.
If `s` does not have a duplicated substring, the method should return an empty string.
If `s` has a duplicated substring, the method should return any one of the duplicated substrings with the longest possible length.
