Return _the index with the **minimum average difference**_. If there are multiple such indices, return the **smallest** one. The average difference of index 3 is the minimum average difference so return 3. The only index is 0 so return 0.
Return _the index with the **minimum average difference**_. If there are multiple such indices, return the **smallest** one.
