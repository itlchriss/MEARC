Preconditions:
- The input array `nums` must not be null.
- The input array `nums` must contain unique elements.

Postconditions:
- The method should return a list of lists, where each inner list represents a subset of the input array `nums`.
- The solution set should not contain any duplicate subsets.
- The order of the subsets in the solution set can be arbitrary.