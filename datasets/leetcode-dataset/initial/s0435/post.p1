Preconditions:
The input array `intervals` is not null.
The input array `intervals` is not empty.
Each interval in the input array `intervals` is a valid interval represented by an array of size The start and end values of each interval in the input array `intervals` are non-negative integers.

Postconditions:
The method returns an integer representing the minimum number of intervals that need to be removed to make the rest of the intervals non-overlapping.