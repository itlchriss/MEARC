The input array `intervals` is not null.
Each interval in `intervals` is represented by an array of size 2, where the first element is the start point and the second element is the end point.
The start point of each interval is less than or equal to the end point.
