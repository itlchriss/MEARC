Preconditions:
- The input array "mountain" is not null.
- The input array "mountain" is not empty.

Postconditions:
- The returned list is not null.
- The returned list contains only integers.
- The returned list contains indices of peaks in the given array.
- The returned list may contain duplicate indices if there are multiple peaks with the same value.
- The order of indices in the returned list can be any order.