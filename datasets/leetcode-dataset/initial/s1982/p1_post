The returned array `ans` must have a length of `n`.
The elements of the returned array `ans` must be integers.
The returned array `ans` must represent the unknown array.
If multiple answers exist, the returned array `ans` can be any valid answer.
