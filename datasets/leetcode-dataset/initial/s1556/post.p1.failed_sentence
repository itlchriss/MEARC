If the input is an integer, the system should accept the input.Upon receiving the integer, the system should append a dot as the thousands separator to the integer.The integer must be modified before the system can convert it into a string format.The integer parameter, representing the modified integer, should be returned in string format by the system as the final result.