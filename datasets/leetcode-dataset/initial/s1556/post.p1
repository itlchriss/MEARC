Preconditions:
- The input integer `n` must be a non-negative number.

Postconditions:
- The returned string should contain the input integer `n` with a dot (".") added as the thousands separator.
- The returned string should be in string format.