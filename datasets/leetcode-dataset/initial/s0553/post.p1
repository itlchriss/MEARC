Preconditions:
- The input array nums is not null.
- The input array nums contains at least one element.

Postconditions:
- The returned string expression is in the format "a/(b/c/d/...)" where a, b, c, d, ... are the elements of the input array nums.
- The returned string expression represents the corresponding expression that has the maximum value.
- The returned string expression is in string format.