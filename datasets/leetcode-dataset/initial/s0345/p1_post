The returned string should have the same length as the input string `str`.
The returned string should have all the vowels reversed while keeping the non-vowel characters in their original positions.
The returned string should have the same characters as the input string `str`, except for the reversed vowels.
