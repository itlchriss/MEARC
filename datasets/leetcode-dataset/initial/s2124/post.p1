Preconditions:
- The input string `s` is not null.

Postconditions:
- The method returns `true` if every 'a' appears before every 'b' in the string.
- The method returns `false` if not every 'a' appears before every 'b' in the string.