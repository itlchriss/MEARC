The method should return an integer value representing the minimum number of operations needed to make `s` equal to `target`.
The returned value should be non-negative.
If the input string `target` is already equal to `s`, the method should return 0.
