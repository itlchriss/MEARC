Preconditions:
- The NumberContainers object `nc` has been initialized.
- The NumberContainers object `nc` has been populated with numbers.

Postconditions:
- The method `find(int number)` returns the smallest index for the given number in the system.
- If there is no index that is filled with the given number, the method `find(int number)` returns -1.