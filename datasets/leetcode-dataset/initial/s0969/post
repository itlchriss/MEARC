
Return _an array of the_ `k`_\-values corresponding to a sequence of pancake flips that sort_ `arr`. Any valid answer that sorts the array within `10 initial prepare.sh run.sh arr.length` flips will be judged as correct.
