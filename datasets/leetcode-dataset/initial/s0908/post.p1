Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.
- The input array `nums` contains only integers.
- The input integer `k` is non-negative.

Postconditions:
- The returned value is an integer.
- The returned value is the minimum score of `nums` after applying the mentioned operation at most once for each index in it.