Preconditions:
- The input `n` must be an integer.

Postconditions:
- The method should return a boolean value indicating whether `n` is an ugly number or not.
- If `n` is an ugly number, the method should return `true`.
- If `n` is not an ugly number, the method should return `false`.