Preconditions:
The method should take two string parameters, `s` and `t`.
The strings `s` and `t` should not be null.

Postconditions:
The method should return an integer value representing the minimum number of steps required to make `s` and `t` anagrams of each other.
If `s` and `t` are already anagrams of each other, the method should return The method should handle both uppercase and lowercase characters in the strings.
The method should consider all characters in the strings, including special characters and spaces.
The method should not modify the original strings `s` and `t`.