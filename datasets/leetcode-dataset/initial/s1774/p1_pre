The arrays `baseCosts` and `toppingCosts` are not null.
The arrays `baseCosts` and `toppingCosts` are not empty.
The elements in `baseCosts` and `toppingCosts` are non-negative integers.
The value of `target` is a non-negative integer.
