The method should return an integer representing the number of 4-directional walks from the starting square to the ending square.
The walks should cover every non-obstacle square exactly once.
If there is no valid walk from the starting square to the ending square, the method should return 0.
