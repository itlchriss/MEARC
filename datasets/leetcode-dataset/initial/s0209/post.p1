Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.
- The input integer `target` is a positive integer.

Postconditions:
- The method returns an integer representing the minimal length of a contiguous subarray whose sum is greater than or equal to the target.
- If there is no such subarray, the method returns 0.