Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.

Postconditions:
- The returned value is an integer.
- The returned value represents the size of the longest non-empty subarray containing only `1`'s in the resulting array.
- If there is no subarray containing only `1`'s in the resulting array, the returned value is `0`.