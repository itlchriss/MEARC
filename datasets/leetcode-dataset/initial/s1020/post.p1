Preconditions:
The input grid `a` is a 2D array of integers.
The grid `a` is not null.
The grid `a` has at least one row and one column.

Postconditions:
The method returns an integer value representing the number of land cells in the grid for which we cannot walk off the boundary of the grid in any number of moves.