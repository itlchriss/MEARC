Preconditions:
- The input array `words` is not null.
- The input array `words` is not empty.
- The character `x` is not null.

Postconditions:
- The returned list is not null.
- The returned list contains only integers.
- The returned list may be empty if no words contain the character `x`.
- The returned list contains the indices of the words that contain the character `x`.
- The order of the indices in the returned list may be arbitrary.