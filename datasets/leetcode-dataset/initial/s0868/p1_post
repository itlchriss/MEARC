The method should return an integer value representing the longest distance between any two adjacent `1`'s in the binary representation of `n`.
If there are no two adjacent `1`'s in the binary representation of `n`, the method should return `0`.
