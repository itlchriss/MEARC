The method returns a list of integers representing the starting indices of substrings in `s` that are a concatenation of each word in `words` exactly once, in any order, and without any intervening characters.
The order of the indices in the returned list does not matter.
