The method should return a string representing the largest-valued odd integer that is a non-empty substring of `num`.
If no odd integer exists in `num`, the method should return an empty string `""`.
