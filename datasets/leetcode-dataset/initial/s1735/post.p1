Preconditions:
- The input parameter `queries` is a 2D array of integers.

Postconditions:
- The returned array `answer` has a length equal to the length of `queries`.
- Each element `answer[i]` in the returned array is the answer to the `i`th query.