Preconditions:
The input arrays `arr1` and `arr2` are not null.
The lengths of `arr1` and `arr2` are greater than or equal to The elements in `arr1` and `arr2` are integers.

Postconditions:
The method returns an integer value representing the minimum number of operations needed to make `arr1` strictly increasing.
If there is no way to make `arr1` strictly increasing, the method returns -1.