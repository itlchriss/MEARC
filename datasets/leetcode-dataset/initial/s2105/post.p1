Preconditions:
- The input array `plants` is not null.
- The input array `plants` has at least one element.
- The input array `plants` contains only non-negative integers.
- The input integers `capacityA` and `capacityB` are positive.

Postconditions:
- The method returns an integer representing the number of times Alice and Bob have to refill their watering cans to water all the plants.