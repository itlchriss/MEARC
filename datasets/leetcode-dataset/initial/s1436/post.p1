Preconditions:
- The input `paths` is a non-null list of lists.
- Each inner list in `paths` contains exactly two elements.
- Each element in `paths` is a non-null string representing a city.

Postconditions:
- The method returns a non-null string representing the destination city.
- The destination city is a city that does not have any outgoing paths to another city.