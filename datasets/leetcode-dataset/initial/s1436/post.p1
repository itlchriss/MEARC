Preconditions:
- The input `paths` is a non-null list of lists.
- Each inner list in `paths` contains exactly two elements.
- Each element in `paths` is a non-null string representing a city.

Postconditions:
- The returned value is a non-null string representing the destination city.
- The destination city does not have any outgoing paths to another city.