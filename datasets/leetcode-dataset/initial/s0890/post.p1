Preconditions:
- The `words` array is not null.
- The `pattern` string is not null.

Postconditions:
- The returned list contains only strings from the `words` array that match the `pattern`.
- The order of the strings in the returned list can be in any order.