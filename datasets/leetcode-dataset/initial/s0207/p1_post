The method should return a boolean value indicating whether it is possible to finish all courses.
If it is possible to finish all courses, the method should return `true`.
If it is not possible to finish all courses, the method should return `false`.
