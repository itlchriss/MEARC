We return [2,3,0]. **Explanation:** Every string satisfies the conditions, so we return [3,2,1].
Return _an array_ `ans` _of size_ `queries.length`_, where_ `ans[i]` _is the answer to the_ `i`<sup>th</sup> _query_.
