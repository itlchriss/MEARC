Preconditions:
- The input array `nums` must not be null.
- The input array `nums` must have at least two elements.

Postconditions:
- The method should return the maximum sum of any pair of elements in the input array.
- If no pair of elements in the input array can be summed, the method should return -1.