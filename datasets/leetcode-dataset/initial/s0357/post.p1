Preconditions:
- The input `n` must be a non-negative integer.

Postconditions:
- The method should return an integer representing the count of all numbers with unique digits.
- The returned count should include the number 0 if `n` is - The returned count should not include any numbers with repeating digits.
- The returned count should not include any numbers with leading zeros.