Preconditions:
- The input arrays `nums1` and `nums2` are not null.
- The input arrays `nums1` and `nums2` are not empty.

Postconditions:
- The returned list `answer` is not null.
- The returned list `answer` has a size of - The integers in the returned list `answer` can be in any order.