The method should return a boolean value indicating whether it is possible to split everyone into two groups in the given way.
If it is possible to split everyone into two groups, the method should return `true`.
If it is not possible to split everyone into two groups, the method should return `false`.
