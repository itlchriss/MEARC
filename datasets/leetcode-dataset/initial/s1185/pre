Given a date, return the corresponding day of the week for that date.
The input is given as three integers representing the `day`, `month` and `year` respectively. initial prepare.sh run.sh The given dates are valid dates between the years `1971` and `2100`.
