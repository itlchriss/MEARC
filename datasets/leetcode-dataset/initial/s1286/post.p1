Preconditions:
- The input string `characters` must not be null.
- The input string `characters` must not be empty.
- The input integer `combinationLength` must be greater than - The input integer `combinationLength` must be less than or equal to the length of the input string `characters`.

Postconditions:
- The method `next()` returns a string representing the next combination of length `combinationLength` in lexicographical order.
- The method `hasNext()` returns `true` if and only if there exists a next combination.