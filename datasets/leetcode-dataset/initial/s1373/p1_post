The method returns an integer representing the maximum sum of all keys of any sub-tree which is also a Binary Search Tree (BST).
If all values in the tree are negatives, the method returns If the tree is empty, the method returns 0.
