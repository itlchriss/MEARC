Preconditions:
- The input matrix `grid` is not null.
- The input matrix `grid` is a square matrix, i.e., the number of rows is equal to the number of columns.
- The input matrix `grid` is a 0-indexed matrix, i.e., the indices of rows and columns start from 
Postconditions:
- The method returns an integer value representing the number of pairs `(Ri, Cj)` where row `Ri` and column `Cj` are equal.