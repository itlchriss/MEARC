The method returns an integer value.
If there is a non-empty subarray in `nums` with a sum of at least `k`, the method returns the length of the shortest subarray.
If there is no such subarray in `nums`, the method returns -1.
