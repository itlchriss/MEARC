The returned array `result` is not null.
The length of the returned array `result` is equal to `k`.
The elements in the returned array `result` are valid indices of the matrix `mat`.
The elements in the returned array `result` are in ascending order, representing the weakest rows in the matrix `mat`.
