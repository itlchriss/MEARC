Preconditions:
- The input arrays `words1` and `words2` are not null.
- The input arrays `words1` and `words2` are not empty.
- The elements in `words1` and `words2` are not null.
- The elements in `words1` and `words2` are not empty strings.

Postconditions:
- The method returns a List of strings.
- The returned List contains all the universal strings in `words1`.
- The order of the strings in the returned List can be any order.