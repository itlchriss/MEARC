Preconditions:
- The method takes in a list of integers `nums` and an integer `target` as parameters.
- The list `nums` is not null.
- The list `nums` is not empty.
- The integer `target` is not null.

Postconditions:
- The method returns an integer value representing the minimum number of operations needed to perform.
- If it is impossible to obtain a subsequence in `nums` whose elements sum to `target`, the method returns -1.