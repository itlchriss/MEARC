Preconditions:
The value of `n` must be a non-negative integer.

Postconditions:
The method should return an integer value representing the value of T<sub>n</sub>.
The returned value should be the sum of the previous three terms in the Tribonacci sequence, T<sub>n-1</sub> + T<sub>n-2</sub> + T<sub>n-3</sub>.
If `n` is 0, the method should return If `n` is 1 or 2, the method should return 1.