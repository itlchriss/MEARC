Preconditions:
- The input string `word` is not null.
- The input string `word` consists only of English vowels.

Postconditions:
- The method returns an integer value representing the length of the longest beautiful substring of `word`.
- If no beautiful substring exists, the method returns 0.