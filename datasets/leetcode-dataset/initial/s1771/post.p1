Preconditions:
The method should take two string parameters, word1 and wordThe method should be public and return an integer value.

Postconditions:
The method should return the length of the longest palindrome that can be constructed using the characters from word1 and wordIf no palindromes can be constructed, the method should return 0.