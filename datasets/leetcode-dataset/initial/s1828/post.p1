Preconditions:
- The method takes in two parameters: `points` and `queries`.
- `points` is a 2D array of integers.
- `queries` is a 2D array of integers.
- The length of `points` is greater than or equal to - The length of `queries` is greater than or equal to 
Postconditions:
- The method returns an array of integers.
- The length of the returned array is equal to the length of `queries`.
- Each element in the returned array is the answer to the corresponding query in `queries`.