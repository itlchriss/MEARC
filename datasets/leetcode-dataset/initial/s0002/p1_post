The method should return a linked list representing the sum of the two input linked lists.
The digits in the resulting linked list should be stored in reverse order.
Each node in the resulting linked list should contain a single digit.
The resulting linked list should represent a non-negative integer.
