Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.

Postconditions:
- The method returns `true` if it is possible to reach the last index of the array.
- The method returns `false` if it is not possible to reach the last index of the array.