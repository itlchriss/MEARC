The method `makeIntegerBeautiful` returns a long integer `x` such that `n + x` is beautiful.
The returned value `x` is the minimum non-negative integer that makes `n + x` beautiful.
The sum of digits of `n + x` is equal to the input `target`.
The returned value `x` is non-negative.
