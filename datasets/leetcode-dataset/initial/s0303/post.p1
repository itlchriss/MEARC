Preconditions:
- The `nums` array must be initialized and contain integer values.

Postconditions:
- The `sumRange` method should return the sum of the elements in the `nums` array between the `left` and `right` indices, inclusive.