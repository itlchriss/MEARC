Preconditions:
- The `nums` array must be initialized and contain integer values.

Postconditions:
- The `sumRange` method returns the sum of the elements of `nums` between the `left` and `right` indices, inclusive.