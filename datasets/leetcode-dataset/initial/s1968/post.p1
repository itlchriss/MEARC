Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.

Postconditions:
- The returned array is a rearrangement of the input array `nums`.
- The returned array has the same length as the input array `nums`.
- The elements in the returned array are the same as the elements in the input array `nums`, but in a different order.