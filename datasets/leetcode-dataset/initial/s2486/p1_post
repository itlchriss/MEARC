The method should return an integer value representing the minimum number of characters that need to be appended to the end of `s`.
The returned value should be greater than or equal to If `t` is already a subsequence of `s`, the returned value should be If `t` is not a subsequence of `s`, the returned value should be the minimum number of characters that need to be appended to `s` to make `t` a subsequence.
The original strings `s` and `t` should not be modified or changed by the method.
