Preconditions:
- The input array `packages` is not null.
- The input array `boxes` is not null.
- The length of the input array `packages` is greater than - The length of the input array `boxes` is greater than - The elements in the input array `packages` are positive integers.
- The elements in the input array `boxes` are positive integers.
- The number of elements in each subarray of `boxes` is 
Postconditions:
- The method returns an integer value representing the minimum total wasted space.
- If it is impossible to fit all the packages inside boxes, the method returns -- The returned value is modulo 10^9 + 7.