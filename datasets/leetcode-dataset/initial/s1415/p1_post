The method should return a string.
If there are less than `k` happy strings of length `n`, the method should return an empty string.
If there are `k` or more happy strings of length `n`, the method should return the kth string from the list of happy strings.
