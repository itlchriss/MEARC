The input `circles` is a 2D integer array.
Each element in `circles` is an array of size 3, representing the center `(x_i, y_i)` and radius `r_i` of a circle.
The values of `x_i`, `y_i`, and `r_i` are integers.
The values of `x_i` and `y_i` are within the range of integer values.
The values of `r_i` are positive integers.
