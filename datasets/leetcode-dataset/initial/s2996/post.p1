Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.

Postconditions:
- The method returns an integer `x`.
- The integer `x` is the smallest integer missing from `nums`.
- The integer `x` is greater than or equal to the sum of the longest sequential prefix of `nums`.