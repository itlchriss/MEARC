Preconditions:
- The input integer `n` must be a positive integer.

Postconditions:
- The method should return a string representing the corresponding column title of the input integer `n` in an Excel sheet.
- The returned string should consist of uppercase English letters only.
- The length of the returned string should be between 1 and 7 characters.
- The returned string should be in the format of Excel column titles, where each character represents a column digit.