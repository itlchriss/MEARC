The method should return `true` if it is possible to partition the array into three non-empty parts with equal sums.
The method should return `false` if it is not possible to partition the array into three non-empty parts with equal sums.
