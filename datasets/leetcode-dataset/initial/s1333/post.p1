Preconditions:
- The input `restaurants` is a 2D array of integers representing the restaurants.
- The input `veganFriendly` is an integer representing the vegan-friendly filter.
- The input `maxPrice` is an integer representing the maximum price filter.
- The input `maxDistance` is an integer representing the maximum distance filter.

Postconditions:
- The method returns a List of Integers representing the filtered restaurant IDs.
- The returned list is ordered by rating from highest to lowest.
- If two restaurants have the same rating, they are ordered by ID from highest to lowest.
- The returned list only includes restaurants that meet the vegan-friendly filter (if applicable).
- The returned list only includes restaurants that have a price less than or equal to the maximum price filter.
- The returned list only includes restaurants that have a distance less than or equal to the maximum distance filter.