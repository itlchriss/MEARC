Preconditions:
- The input strings `s` and `t` are not null.
- The lengths of `s` and `t` are greater than or equal to - The length of `s` is greater than or equal to the length of `t`.

Postconditions:
- The method returns a string that represents the minimum window substring of `s` that contains every character in `t`.
- If there is no such substring, the method returns an empty string "".