Preconditions:
- The input matrix must be a square matrix (n x n).
- The input matrix must contain only integer values.

Postconditions:
- The method should return a boolean value indicating whether the matrix is valid or not.
- If the matrix is valid (every row and column contains the numbers 1, 2, and 3), the method should return true.
- If the matrix is not valid (at least one row or column does not contain the numbers 1, 2, and 3), the method should return false.