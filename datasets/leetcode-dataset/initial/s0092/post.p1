Preconditions:
The `head` parameter must not be null.
The `left` parameter must be less than or equal to the `right` parameter.
The linked list must have at least `right` number of nodes.

Postconditions:
The nodes of the linked list from position `left` to position `right` are reversed.
The reversed list is returned as the result.