Preconditions:
- The input `nums` is a 2D integer array.
- Each element `nums[i]` in `nums` is a non-empty array of distinct positive integers.

Postconditions:
- The method returns a list of integers.
- The list contains integers that are present in each array of `nums`.
- The integers in the list are sorted in ascending order.