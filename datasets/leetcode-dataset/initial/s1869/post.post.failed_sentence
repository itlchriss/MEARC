Given a binary string `s`, return `true` _if the **longest** contiguous segment of_ `1`'_s is **strictly longer** than the **longest** contiguous segment of_ `0`'_s in_ `s`, or return `false` _otherwise_. The segment of 1s is longer, so return true. The segment of 1s is not longer, so return false. The segment of 1s is not longer, so return false.

