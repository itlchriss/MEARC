The value of `k` must be a positive integer.
The value of `w` must be a non-negative integer.
The lengths of the `profits` and `capital` arrays must be equal.
The `profits` array must contain only non-negative integers.
The `capital` array must contain only non-negative integers.
