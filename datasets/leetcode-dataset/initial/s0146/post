initial prepare.sh run.sh `int get(int key)` Return the value of the `key` if the key exists, otherwise return `-1`. lRUCache.get(1); // return 1 lRUCache.get(2); // returns -1 (not found) lRUCache.get(1); // return -1 (not found) lRUCache.get(3); // return 3 lRUCache.get(4); // return 4
initial prepare.sh run.sh `int get(int key)` Return the value of the `key` if the key exists, otherwise return `-1`.
