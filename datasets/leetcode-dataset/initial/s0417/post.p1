Preconditions:
- The input matrix must not be null.
- The input matrix must have at least one row and one column.

Postconditions:
- The returned list must not be null.
- The returned list must contain a list of grid coordinates.
- Each grid coordinate in the returned list must be a list of two integers, representing the row and column indices.
- The returned list must contain all the grid coordinates where rain water can flow from the cell to both the Pacific and Atlantic oceans.