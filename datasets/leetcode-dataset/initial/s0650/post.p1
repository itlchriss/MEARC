Preconditions:
- The input `n` must be a non-negative integer.

Postconditions:
- The method returns an integer representing the minimum number of operations required to get the character 'A' exactly `n` times on the screen.