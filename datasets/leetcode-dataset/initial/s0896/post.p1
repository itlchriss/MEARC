Preconditions:
- The input array `nums` is not null.

Postconditions:
- The method returns `true` if the given array `nums` is monotonic.
- The method returns `false` if the given array `nums` is not monotonic.