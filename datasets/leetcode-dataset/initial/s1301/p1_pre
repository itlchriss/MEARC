The input `board` is not null.
The input `board` is a list of strings.
The strings in `board` are not null.
The strings in `board` contain only numeric characters and 'X' characters.
The length of each string in `board` is equal to the length of `board`.
The length of `board` is greater than
