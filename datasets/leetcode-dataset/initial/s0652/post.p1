Preconditions:
The `root` of the binary tree is not null.

Postconditions:
The method returns a list of `TreeNode` objects representing the root nodes of all duplicate subtrees found in the binary tree.
The returned list may contain duplicate `TreeNode` objects if there are multiple duplicate subtrees in the binary tree.
The returned list may be empty if no duplicate subtrees are found in the binary tree.