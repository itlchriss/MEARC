Preconditions:
- The input array `words` is not null.
- The input array `words` is not empty.

Postconditions:
- The method returns a list of strings.
- The returned list contains all strings in `words` that are substrings of another word in the array.
- The order of the strings in the returned list can be in any order.