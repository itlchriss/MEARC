The method should return an integer representing the maximum k-booking between all the previous events.
The maximum k-booking should be updated after each event is added.
If an event intersects with a previous event, the maximum k-booking should be updated accordingly.
The maximum k-booking should remain the same if an event does not intersect with any previous events.
