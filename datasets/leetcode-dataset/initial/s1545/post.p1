Preconditions:
- The value of `n` is a positive integer.
- The value of `k` is a positive integer.
- The value of `k` is less than or equal to the length of `S_n`.

Postconditions:
- The method returns a character representing the `k`th bit in `S_n`.