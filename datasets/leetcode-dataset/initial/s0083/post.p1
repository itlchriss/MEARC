Preconditions:
The input linked list is sorted in ascending order.
The linked list is not empty.

Postconditions:
All duplicate elements in the linked list are removed.
Each element in the linked list appears only once.
The resulting linked list is sorted in ascending order.