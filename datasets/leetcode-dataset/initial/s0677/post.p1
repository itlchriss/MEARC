Preconditions for public MapSum():

- The method should be called before any insert or sum operations are performed.

Postconditions for public MapSum():

- The MapSum object is initialized and ready to perform insert and sum operations.

Preconditions for public void insert(String key, int val):

- The MapSum object must be initialized.
- The key parameter must be a non-null string.
- The val parameter must be a non-negative integer.

Postconditions for public void insert(String key, int val):

- The key-value pair is inserted into the MapSum object.

Preconditions for public int sum(String prefix):

- The MapSum object must be initialized.
- The prefix parameter must be a non-null string.

Postconditions for public int sum(String prefix):

- The method returns the sum of all the values whose key starts with the prefix.