The method should take in three parameters: `s` (the initial string), `t` (the target string), and `k` (the maximum number of moves allowed).
The strings `s` and `t` should not be null.
The value of `k` should be a non-negative integer.
