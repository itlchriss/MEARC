The algorithm should return the original array, str_arr, which is the result of decoding the encoded array using the first element as a starting point. The existence and uniqueness of the decoded array can be proven.
