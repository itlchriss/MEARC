Given an array of strings `words` and a character `separator`, **split** each string in `words` by `separator`.
initial prepare.sh run.sh The resulting strings must maintain the same order as they were initially given.
