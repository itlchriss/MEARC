Preconditions:
The method should take a TreeNode object `root` as input, which represents the root of the binary search tree.
The method should take an integer `val` as input, which represents the value to be searched in the binary search tree.

Postconditions:
If a node with value `val` exists in the binary search tree, the method should return the subtree rooted with that node.
If a node with value `val` does not exist in the binary search tree, the method should return `null`.