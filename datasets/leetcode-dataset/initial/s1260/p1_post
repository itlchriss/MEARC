The returned grid must have the same dimensions as the input grid.
The values in the returned grid must be shifted k times to the right.
The values in the returned grid must wrap around to the beginning of each row if they exceed the number of columns in the grid.
The values in the returned grid must wrap around to the top of each column if they exceed the number of rows in the grid.
The original input grid must remain unchanged.
