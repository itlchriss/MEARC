 `creator_i` and `id_i`. Here, `creator_i` refers to the creator at index `i` in the `creators` array, and `id_i` refers to the id of their most popular video, which can be found at index `i` in the `ids` array.

The order of the elements in the `answer` array can be arbitrary.
