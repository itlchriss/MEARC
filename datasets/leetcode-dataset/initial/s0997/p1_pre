The method takes two parameters: an integer n representing the number of people in the town and a 2D integer array trust representing the trust relationships between people.
The trust array must have a length greater than or equal to The trust array must have exactly two columns.
The trust array must contain only positive integers.
The trust array must not contain any duplicate rows.
