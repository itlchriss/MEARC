Preconditions:
- The method `compareVersion` takes two parameters `version1` and `version2` of type String.
- The values of `version1` and `version2` are not null.
- The values of `version1` and `version2` are valid version numbers.

Postconditions:
- If `version1` is less than `version2`, the method returns `-1`.
- If `version1` is greater than `version2`, the method returns `1`.
- If `version1` is equal to `version2`, the method returns `0`.