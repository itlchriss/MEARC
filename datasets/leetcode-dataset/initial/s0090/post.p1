Preconditions:
- The input array `nums` is not null.

Postconditions:
- The returned list `subsets` is not null.
- The returned list `subsets` contains all possible subsets of the input array `nums`.
- The returned list `subsets` does not contain any duplicate subsets.
- The order of subsets in the returned list `subsets` can be in any order.