The method returns a list of lists, where each inner list represents an interval of a large group.
The intervals are sorted in increasing order by the start index.
Each inner list contains two integers: the start index and the end index of the large group interval.
