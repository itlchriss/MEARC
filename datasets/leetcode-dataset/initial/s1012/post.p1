Preconditions:
- The input `n` is a positive integer.

Postconditions:
- The method returns an integer representing the number of positive integers in the range `[1, n]` that have at least one repeated digit.