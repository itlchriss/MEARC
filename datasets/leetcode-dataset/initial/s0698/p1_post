The method should return `true` if it is possible to divide the input array `nums` into `k` non-empty subsets whose sums are all equal.
The method should return `false` if it is not possible to divide the input array `nums` into `k` non-empty subsets whose sums are all equal.
