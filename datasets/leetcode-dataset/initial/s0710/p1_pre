The integer `n` must be a positive integer.
The array `blacklist` must not be null.
The array `blacklist` must only contain unique integers.
The integers in the array `blacklist` must be within the range `[0, n - 1]`.
