Preconditions:
- The input array `nums` is not null.
- The input array `nums` has at least three elements.

Postconditions:
- The method returns `true` if there exists a triple of indices `(i, j, k)` such that `i < j < k` and `nums[i] < nums[j] < nums[k]`.
- The method returns `false` if no such indices exist.