The method returns an array of integers.
The returned array contains the biggest three distinct rhombus sums in descending order.
If there are less than three distinct values, the returned array contains all of them.
