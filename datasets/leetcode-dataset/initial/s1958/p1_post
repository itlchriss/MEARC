If changing the cell at `(rMove, cMove)` to `color` is a legal move, the method should return `true`.
If changing the cell at `(rMove, cMove)` to `color` is not a legal move, the method should return `false`.
