The method should return an integer value.
If there is a positive integer `k` in the array `nums`, the method should return that value.
If there is no positive integer `k` in the array `nums`, the method should return -1.
