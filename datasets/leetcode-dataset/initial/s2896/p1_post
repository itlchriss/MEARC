The method should return an integer value representing the minimum cost needed to make the strings s1 and s2 equal.
If it is impossible to make the strings s1 and s2 equal, the method should return -1.
