Preconditions:
- The input array `nums` is not null.
- The length of the input array `nums` is greater than or equal to 
Postconditions:
- The method returns `true` if the array `nums` can be made strictly increasing after removing exactly one element.
- The method returns `false` if the array `nums` cannot be made strictly increasing after removing exactly one element.
- If the array `nums` is already strictly increasing, the method returns `true`.