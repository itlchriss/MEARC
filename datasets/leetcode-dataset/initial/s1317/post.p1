Preconditions:
- The input `n` is a positive integer.

Postconditions:
- The output is an array of two integers `[A, B]`.
- The sum of `A` and `B` is equal to `n`.
- Both `A` and `B` are non-zero integers.
- There is at least one valid solution.
- If there are multiple valid solutions, any of them can be returned.