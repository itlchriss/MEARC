Preconditions:
- The method takes two integer parameters, `n` and `k`.
- `n` is a positive integer.
- `k` is a positive integer.

Postconditions:
- The method returns an integer value.
- If `n` has less than `k` factors, the method returns -- If `n` has at least `k` factors, the method returns the `k`th factor in the list of factors of `n`.
- The list of factors of `n` is sorted in ascending order.