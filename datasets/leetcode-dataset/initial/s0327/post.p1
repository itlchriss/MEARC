Preconditions:
- The `nums` array is not null.
- The `nums` array is not empty.
- The `lower` integer is less than or equal to the `upper` integer.

Postconditions:
- The method returns an integer representing the number of range sums that lie in the inclusive range `[lower, upper]`.