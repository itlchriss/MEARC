Preconditions:
- The input string `s` is not null.
- The input integer `k` is greater than - The length of the input string `s` is greater than or equal to `k`.

Postconditions:
- The method returns a string.
- The returned string is the lexicographically smallest beautiful substring of `s` with length equal to `k`.
- If `s` does not contain a beautiful substring, the method returns an empty string.