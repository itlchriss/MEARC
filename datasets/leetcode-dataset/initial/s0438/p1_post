The method should return a list of integers representing the start indices of `p`'s anagrams in `s`.
The list should contain all the start indices of `p`'s anagrams in `s`.
The order of the start indices in the list can be in any order.
