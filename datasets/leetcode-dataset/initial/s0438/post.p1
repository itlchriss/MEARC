Preconditions:
The method should take two string parameters, `s` and `p`.
The strings `s` and `p` should not be null.
The string `p` should not be empty.
The length of string `s` should be greater than or equal to the length of string `p`.

Postconditions:
The method should return a list of integers representing the start indices of `p`'s anagrams in `s`.
The list should contain all the start indices of `p`'s anagrams in `s`.
The order of the start indices in the list can be in any order.