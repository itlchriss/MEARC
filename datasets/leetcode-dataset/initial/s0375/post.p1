Preconditions:
- The input `n` must be a positive integer.

Postconditions:
- The method should return an integer representing the minimum amount of money needed to guarantee a win regardless of the number chosen.
- The returned amount should be greater than or equal to zero.