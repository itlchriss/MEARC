The input array `intervals` is not null.
Each element in the `intervals` array is an array of size 2, representing the start and end points of an interval.
The start and end points of each interval are non-negative integers.
The intervals in the `intervals` array are not sorted.
