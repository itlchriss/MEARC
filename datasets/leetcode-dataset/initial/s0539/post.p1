Preconditions:
- The input list `timePoints` is not null.
- The input list `timePoints` contains at least two elements.

Postconditions:
- The method returns an integer value representing the minimum minutes difference between any two time-points in the list.