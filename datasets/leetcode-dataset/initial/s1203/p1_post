The method returns an int array representing a sorted list of items.
If there is more than one solution, the method can return any valid solution.
If there is no solution, the method returns an empty list.
