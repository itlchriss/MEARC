initial prepare.sh run.sh `int pop()` Pops and returns the top of stack or **\-1** if the stack is empty. customStack.pop(); // return 2 --> Return top of the stack 2, stack becomes [1] customStack.pop(); // return 103 --> Return top of the stack 103, stack becomes [201, 202] customStack.pop(); // return 202 --> Return top of the stack 102, stack becomes [201] customStack.pop(); // return 201 --> Return top of the stack 101, stack becomes [] customStack.pop(); // return -1 --> Stack is empty return -1.
customStack.pop(); // return 2 --> Return top of the stack 2, stack becomes [1] customStack.pop(); // return 103 --> Return top of the stack 103, stack becomes [201, 202] customStack.pop(); // return 202 --> Return top of the stack 102, stack becomes [201] customStack.pop(); // return 201 --> Return top of the stack 101, stack becomes []
