Preconditions:
The method `compareTo` should take a parameter of type `Pair`.
The `aliceValues` array should contain the values of Alice's stones.
The `bobValues` array should contain the values of Bob's stones.

Postconditions:
If Alice wins the game, the method should return `1`.
If Bob wins the game, the method should return `-1`.
If the game results in a draw, the method should return `0`.