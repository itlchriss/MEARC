Preconditions:
- The input array arr is not null.
- The input array arr contains at least one element.

Postconditions:
- The returned value is an integer representing the largest number of chunks that can be made to sort the array.