Preconditions:
The input arrays `rec1` and `rec2` must have a length of The elements of `rec1` and `rec2` must be integers.
The elements of `rec1` and `rec2` must represent the coordinates of the top-left and bottom-right corners of the rectangles.
The coordinates of the rectangles must be axis-aligned.

Postconditions:
If the rectangles overlap, the method should return `true`.
If the rectangles do not overlap, the method should return `false`.