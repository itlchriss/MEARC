Preconditions:
The input array `items` must not be null.
The input array `items` must have at least one element.
The input array `items` must have at least `k` elements.

Postconditions:
The method should return an integer denoting the maximum elegance of a subsequence of `items` with size exactly `k`.
The returned maximum elegance value should be a valid elegance value present in the input array `items`.
The returned maximum elegance value should be the maximum possible elegance value among all subsequences of `items` with size exactly `k`.