Preconditions:
The input strings `s1` and `s2` should not be null.
The input strings `s1` and `s2` should consist of lowercase English letters and digits 1-9 (inclusive).

Postconditions:
If there exists an original string that could be encoded as both `s1` and `s2`, the method should return true.
If there does not exist an original string that could be encoded as both `s1` and `s2`, the method should return false.