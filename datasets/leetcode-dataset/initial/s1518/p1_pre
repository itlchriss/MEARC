The method should take two integer parameters `numBottles` and `numExchange`.
`numBottles` should be a non-negative integer representing the initial number of water bottles.
`numExchange` should be a positive integer representing the number of empty bottles required to exchange for a new bottle.
