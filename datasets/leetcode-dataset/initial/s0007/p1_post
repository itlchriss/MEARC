The method returns an integer value.
The returned value is the reverse of the input `x` with its digits reversed.
If reversing `x` causes the value to go outside the signed 32-bit integer range `[-2^31, 2^31 - 1]`, then the method returns `0`.
