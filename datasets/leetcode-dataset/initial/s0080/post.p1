Preconditions:
- The input array `nums` is not null.
- The input array `nums` is not empty.

Postconditions:
- The first `k` elements of the array `nums` are the unique elements from the original array.
- The remaining elements of the array `nums` (beyond the first `k` slots) are not modified and can be any value (represented by underscores).