Preconditions:
- The input array `nums` is not null.
- The input array `nums` has at least one element.

Postconditions:
- The returned value is an integer representing the minimum number of operations needed to make `nums` strictly increasing.
- The input array `nums` remains unchanged after the method is executed.