Preconditions:
The input strings `a` and `b` are not null.
The input strings `a` and `b` are anagrams of each other.

Postconditions:
The method returns an integer value representing the smallest `k` for which `s1` and `s2` are `k`-similar.