Preconditions:
The `root` parameter must not be null.

Postconditions:
The method should return an integer value representing the maximum depth of the binary tree.
If the `root` is null, the method should return If the `root` has no children (leaf node), the method should return If the `root` has only one child, the method should return the maximum depth of that child plus If the `root` has two children, the method should return the maximum depth of the child with the greater depth plus 1.