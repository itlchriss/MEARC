initial prepare.sh run.sh `int pop()` Removes the element from the front of the queue and returns it. myQueue.peek(); // return 1 myQueue.pop(); // return 1, queue is [2] myQueue.empty(); // return false
initial prepare.sh run.sh `int peek()` Returns the element at the front of the queue. initial prepare.sh run.sh `boolean empty()` Returns `true` if the queue is empty, `false` otherwise.
